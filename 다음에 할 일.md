# 바로 할 일

최종 갱신: 2026-09-13 (본문까지) · 자세한 배경은 `CLAUDE.md` 참고

---

## 0. 지금 상황 한 줄

**화면 개편이 끝났습니다.** 패널 3개(S&P 낙폭 / 나스닥 낙폭 / 공포탐욕 게이지)를
합친 차트 1개로 바꿨고, 그 아래 성적표는 그대로입니다. 다국어도 저장소에 올라갔습니다.
**옛 화면 백업**: `backup/v1-3panel` 브랜치.

지난 문서에 "다국어 파일 3개를 올려야 한다"고 적혀 있었는데 **이미 다 올라갔습니다.**
그 항목은 지웠습니다.

---

## 1. 다음 후보 — 무엇부터 할지 고르기

바로 시작할 수 있는 순서대로 적었습니다. **위 셋은 반나절짜리, 아래 둘은 큰 일입니다.**

| | 할 일 | 크기 | 왜 |
|---|---|---|---|
| A | **야간 모드** | 반나절 | 지금 **아예 없습니다.** 밤에 열면 흰 화면 그대로 |
| B | **영어 검수** | 30분 | ⓘ 설명과 긴 문단의 영어는 어시스턴트가 쓴 것 |
| C | ~~실시간·파일 정리~~ | — | **끝.** 아래 C·C-2 참고 |
| D | ~~도메인 + 머리글~~ | — | **끝.** 본문 심기는 D-2 |
| E | **대가들의 선택** | 큰 일 | 별도 페이지로 만들어 나중에 붙일 계획 |

### A. 야간 모드

`prefers-color-scheme` 이 코드에 **한 번도 안 나옵니다.**
어두운 팔레트 한 벌을 만드는 것 자체는 간단한데, **차트가 문제입니다** —
공포탐욕·VIX 색 띠를 흰 바탕 기준으로 옅게 깔아 둬서 어두운 바탕에서는
안 보이거나 탁해집니다. 빗금과 투명도도 다시 봐야 합니다.

### B. 영어 검수

합친 차트의 ⓘ 툴팁 4개와 그 아래 긴 설명 4개. **한국어는 사용자가 직접 쓴 것**이고
영어는 그걸 옮긴 것입니다. 영어 화면으로 바꿔서(`?lang=en`) 어색한 곳을 알려 주시면 고칩니다.

### C. 안 쓰는 파일 정리 — 절반 완료

**지운 것 (2026-09-10).** `scripts/fetch_quote.py`, `.github/workflows/update-quote.yml`,
`data/backtest.py`(중복본), 그리고 `index.html` 안의 관련 죽은 코드.
화면 글자는 **한 글자도 바뀌지 않았습니다**(렌더링 결과 비교로 확인).

지우기 전에 실측한 것: 그 워크플로는 "10분마다"라고 적혀 있었지만 **한 번도 지켜진 적이
없었습니다** — 연속 실행 간격 중앙값 16분, 최근 사흘은 하루 3~4회. `CLAUDE.md` 10번.

**아직 남은 것 하나.** `data` 브랜치의 `quote.json` — 이제 아무도 안 씁니다.
GitHub 웹에서 Branches → data → 휴지통으로 지우면 됩니다.

**취소된 것.** `market.json` 의 종가 시계열을 빼자고 적어 뒀었는데 **그러면 안 됩니다.**
2026-09-12 부터 화면의 최근 며칠이 바로 거기서 옵니다. `CLAUDE.md` 3번.

---

## C-2. 실시간 — 끝났습니다 (2026-09-13)

**위 차트가 주 1회 갱신되던 것을 평일 매일로 고쳤고**, 그 위에 **장중 실시간 중계소**까지
붙였습니다. 클라우드플레어 중계소가 돌아가고 있고 넷을 다 받아옵니다.

```
주소  https://yellow-salad-ec67.dysan1000.workers.dev/
```

**가장 궁금했던 것 — CNN 공포탐욕도 됩니다.** GitHub 은 막았는데 클라우드플레어는
안 막았습니다. 넷 중 하나만 어제 값이 되는 상황은 피했습니다.

**미국장이 열렸을 때 한 번만 봐 주세요.** 한국시간 밤 10:30 ~ 새벽 5시 사이에
사이트를 열면, 오른쪽 위 시각이 `데이터 기준` 대신 **`실시간`** 으로 바뀌어 있어야 합니다.
공포탐욕 숫자가 낮에 본 값과 다르면 장중에도 움직인다는 뜻입니다(그것만 아직 미확인).

돈은 안 듭니다 — 무료 한도가 하루 10만 요청인데, 60초 캐시를 걸어 두어 방문자가
아무리 많아도 야후에는 분당 1회만 나갑니다.

### D. 도메인 + 머리글 — 끝났습니다 (2026-09-13)

```
itpaidoff.com   Namecheap · 첫해 $11.28 · 갱신 $14.98 · 자동갱신 ON · WHOIS 비공개 ON
DNS             A 4개 + CNAME www → GitHub Pages   (조회로 확인)
CNAME 파일      저장소 루트                          (머지됨)
```

**머리글에 넣은 것** — description, Open Graph(공유 카드), canonical, hreflang,
파비콘, 애플 아이콘, robots.txt, sitemap.xml, 구조화 데이터.

**공유 카드는 언어별로 두 장**입니다. 정적 기본값이 영어라,
**한국에서 카톡으로 공유하실 때는 주소 뒤에 `?lang=ko` 를 붙이세요.**

```
https://itpaidoff.com/?lang=ko
```

그러면 카드가 한국어판으로 뜹니다.

**바로 해 두면 좋은 것 (각 5분)**

- **구글**: `search.google.com/search-console` → 도메인 등록 → `sitemap.xml` 제출
- **네이버**: `searchadvisor.naver.com` → 사이트 등록 → 사이트맵 제출
- 둘 다 소유 확인을 시키는데, **HTML 파일 업로드 방식**을 고르시면
  파일을 제가 저장소에 넣어 드리겠습니다. 파일 이름만 알려 주세요.

### D-2. SEO 본문 — 끝났습니다 (2026-09-13)

구글이 **"크롤링됨 - 현재 색인이 생성되지 않음"** 이라고 했던 이유가 숫자로 확인됐습니다.
크롤러가 받던 본문이 **304자**였고 그중 둘이 `Loading…` 이었습니다.

```
전   304자
후 4,418자
```

차트 아래·각주 위에 **접이식 설명 글**을 넣었습니다. 처음엔 접혀 있고,
구글은 접혀 있어도 읽습니다. 한국어·영어 두 벌이 HTML 에 다 들어 있어서
네이버처럼 JS 를 안 돌리는 크롤러에게도 한국어가 보입니다.

**글에 쓴 숫자는 전부 그날 다시 계산했습니다.** 문서에 적힌 값을 그냥 베끼지 않고
`backtest.json` 으로 재 봤는데, 세 개가 달라져 있었습니다.

**남은 건 기다리는 일입니다.** 재판정까지 며칠~몇 주 걸립니다.
`URL 검사` 에서 다시 `색인 생성 요청` 을 한 번 눌러 두세요.

**확인해 주실 것 하나** — Search Console 왼쪽 `보안 및 직접 조치`.
이 도메인은 **2026년 4월 5일에 구글이 크롤링한 기록**이 있습니다. 어제 사셨는데도요.
전에 누가 쓰다 놓은 도메인이라는 뜻입니다. 대부분 무해하지만, 이전 주인이 스팸에
썼다면 거기 뜹니다. **"문제가 감지되지 않았습니다"** 가 나오면 깨끗한 겁니다.

### E. 대가들의 선택

대가들이 무엇을 샀고 **그 선택이 지금 어떤 결과를 낳았는지.**
데이터는 **13F 공시(SEC)** — 미국 정부 저작물이라 퍼블릭 도메인입니다.
**이 프로젝트에서 가장 큰 이점**입니다. 지금 대시보드를 괴롭히는 데이터 라이선스
문제가 여기서는 통째로 빠집니다.

한계는 화면에 밝혀야 합니다 — **45일 지연 공시**, **미국 주식 롱 포지션만** 보입니다.
아직 아주 초기 단계이고 설계는 시작하지 않았습니다.

---

## 2. 알고만 있으면 되는 것

- **320px 가로 넘침** — 아이폰 SE 같은 좁은 화면에서 성적표 쪽에 가로 스크롤이 생깁니다.
  **이번 개편 때문이 아니라 원래 있던 문제**입니다.
- **두 잣대가 엇갈릴 때** — 공포탐욕은 공포인데 VIX 는 잠잠한 날이 2011년 이후 15.5%.
  사용자가 직접 발견한 지점. 이제 한 차트에서 두 선을 같이 보니 만들기 쉬워졌습니다.
- **텔레그램 주간 보고** — Umami Hobby 는 API 가 막혀 있어 보류.
  셀프호스팅(Vercel + **Neon**)으로 옮기면 가능해집니다. `CLAUDE.md` 5번.

---

## 3. 결정 완료 — 다시 묻지 마세요

- **VIX 구간 경계** → 관행 숫자(12/15/20/30/40). `CLAUDE.md` 4번
- **공포탐욕 3Y·5Y 버튼** → 그대로 둠
- **다국어 색 규칙** → 언어 따라 전환 (한국어 상승=빨강 / 영어 상승=초록)
- **언어 기억 방식** → 주소(`?lang=`) + localStorage 둘 다
- **통계 도구** → Umami. API 는 Hobby 에서 막힘
- **SPY 유지** (VOO 아님) → VOO 는 2010년 상장이라 2008년이 빠짐
- **위 차트 왼쪽 축은 가격** (낙폭 아님) → 전고점 수위선·빗금은 그림 안에 남김
- **기간 버튼은 한 줄** → 2줄 금지. 더 과거는 좌우 드래그
- **성적표에는 숫자만** → 자동 판정 문장·빗금·설명 문단은 넣었다가 뺐음
- **ETF 전환** → 완료. `market-long.json` 이 SPY·QQQ 기준

---

## 4. 일하는 방식

어시스턴트가 브랜치에 올리고 PR 을 만듭니다.
**사용자가 하는 일은 GitHub 에서 초록색 Merge 버튼을 누르는 것뿐입니다.**

**머지한 뒤의 작업은 새 PR 로 갑니다.** 닫힌 PR 에는 아무리 커밋을 쌓아도
사이트에 반영되지 않습니다. 실제로 세 번 그런 일이 있었습니다.
PR 을 받으실 때 **몇 번 PR 인지, 커밋이 몇 개인지** 같이 적어 드립니다.
