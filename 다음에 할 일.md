# 바로 할 일

최종 갱신: 2026-09-23

---

## 이 파일은 **사용자가 직접 해야 하는 일**만 적습니다

어시스턴트가 못 하는 것들입니다 — 브라우저로 눌러 봐야 하거나, 계정에
로그인해야 하거나, 판단이 필요한 것.

**나머지는 여기 안 적습니다.** 끝난 일·정해진 것·왜 그렇게 했나는 전부
`CLAUDE.md` 에 있고, **그것이 사실의 단일 출처**입니다. 여기에 같은 말을
한 벌 더 두면 갈라집니다 — 실제로 이 파일이 9일 동안 *"대가들의 선택은 아직
시작 안 했다"* 고 적고 있었습니다(그 사이에 다 만들어져 돌고 있었습니다).

```
지금 무엇이 있나 · 왜 그렇게 정했나     CLAUDE.md
지난 세션이 뭘 했고 뭘 넘겼나            docs/worklog/claude.md · codex.md
화면을 새로 만들기 전에 읽을 재료표      docs/design-system.md
작업 원칙 (Claude·Codex 둘 다)          AGENTS.md
```

---

## 1. 지금 기다리는 것 — Search Console

**이게 제일 급합니다.** 다른 일의 방향이 여기서 갈립니다.

`itpaidoff.com` Search Console 에서 **`URL 검사`** 에 아래 둘을 넣어 보세요.

```
https://itpaidoff.com/                      ← 대시보드
https://itpaidoff.com/titans/berkshire/     ← 대가들의 선택
```

| 나오는 말 | 뜻 | 그다음 |
|---|---|---|
| `색인 생성됨` | 통과 | `실적 → 검색어` 에 실제 검색어가 쌓이기 시작합니다 |
| `크롤링됨 - 현재 색인이 생성되지 않음` | **내용이 얇다는 판정** | 본문을 더 늘려야 합니다 (`CLAUDE.md` 6-2) |

**`보안 및 직접 조치` 도 한 번 보세요.** 이 도메인은 2026-04-05 에 구글이
크롤링한 기록이 있습니다 — **전에 누가 쓰다 놓은 도메인**입니다. 대부분
무해하지만 이전 주인이 스팸에 썼다면 여기 뜹니다.
`문제가 감지되지 않았습니다` 가 나오면 깨끗한 것입니다.

**결과를 알려 주시면 SEO 쪽을 이어서 합니다.** 그전에는 추측이 됩니다.

---

## 2. 후원 — 주소가 살아 있는지 (각 5분)

| | 무엇 | 왜 제가 못 하나 |
|---|---|---|
| A | `https://buymeacoffee.com/snoringprince` 를 눌러 보기 | 개발 환경에서 그 주소 접속이 막혀 있습니다(프록시 403). 형식은 맞는데 **진짜 살아 있는지**를 볼 수가 없습니다 |
| B | Buy Me a Coffee 페이지의 `Name · About · Website` 칸 채우기 | 계정 로그인이 필요합니다 |

**후원 0건은 기본값입니다.** 개인 도구 사이트 전환율은 방문자 1,000명당 1명
아래가 보통이고, 이 사이트는 아직 색인도 안 됐습니다. 그래서 `support` 이벤트로
**누른 횟수**를 따로 셉니다 — 그게 있어야 *"안 좋아해서"* 인지 *"본 사람이
없어서"* 인지 나눌 수 있습니다.

---

## 3. 장중에 `실시간` 이 뜨는지 (한 번만)

**밤 10:30 ~ 새벽 5시**(미국장 시간)에 사이트를 열어 보세요.
오른쪽 위 시각이 `데이터 기준` 대신 **`실시간`** 으로 바뀌어 있고,
공포탐욕 숫자가 낮에 본 값과 다르면 움직이는 것입니다.

**이것만 아직 확인이 안 됐습니다.** 중계소가 CNN 에 닿는 것은 확인됐는데
(일요일에 받아 봄), **장중에 그 숫자가 실제로 움직이는지**는 미국장이 열렸을 때
봐야 압니다. SPY·QQQ·VIX 는 예전 워크플로에서 140회 확인했으므로 확실합니다.

---

## 4. 머지된 브랜치 치우기 (선택, 2분)

지금 브랜치가 15개인데 **11개는 이미 머지된 것**입니다.
`backup/v1.0` 과 `backup/v1-3panel` 이 그 사이에 묻혀 있습니다 —
되돌릴 자리를 한눈에 보려고 `backup/…` 으로 이름을 맞춰 둔 것이라,
치워 두면 그 두 개만 남습니다.

```
GitHub → Branches → 머지된 것 옆 휴지통
남길 것:  main · backup/v1.0 · backup/v1-3panel · claude/modest-albattani-9s5dxe
```

**`backup/` 두 개는 지우지 마세요.** 되돌아갈 자리입니다(`CLAUDE.md` 0-4).

---

## 5. 제가 들고 있는 것 (사용자가 하실 일 없음)

여기 적는 이유는 **"왜 아직 안 됐나"가 궁금하실 때 보시라고**입니다.

- **두 번째 투자자** — 틀은 만들었지만 실제로 두 명으로 돌려 본 적이 없습니다.
- **연차보고서 대조** — 추정 매수가가 맞는지 버크셔 연차보고서로 확인하는 일.
  **그전까지 그 숫자는 화면에도 문서에도 '추정'입니다.**
- **영어 `--up` 대비 4.49** — 비중 막대 위에서 AA 에 0.01 모자랍니다.
  대시보드와 같이 쓰는 색이라 두 화면을 함께 다시 재야 합니다.
- **2013년 이전 43분기 정정 미반영** — 텍스트 공시라 자동으로 못 합칩니다.
  일부러 그렇게 두고 자료에 `amend_gap` 으로 표시해 두었습니다.

---

## 6. 일하는 방식

어시스턴트가 브랜치에 올리고 PR 을 만듭니다.
**사용자가 하는 일은 GitHub 에서 초록색 Merge 버튼을 누르는 것뿐입니다.**

**머지한 뒤의 작업은 새 PR 로 갑니다.** 닫힌 PR 에 아무리 커밋을 쌓아도
사이트에 반영되지 않습니다 — 실제로 세 번 그런 일이 있었습니다.
PR 을 넘길 때 **몇 번 PR 인지, 커밋이 몇 개인지, 무엇이 바뀌는지**를 같이 적습니다.
