# 바로 할 일

최종 갱신: 2026-09-09 · 자세한 배경은 `CLAUDE.md` 참고

---

## 1. 다국어 파일 3개 올리기 (아직 안 됨)

**순서를 지켜 주세요.** 구간 key 가 든 데이터가 먼저 올라가야 화면이 새 이름을 찾습니다.
순서가 바뀌어도 폴백이 있어 깨지진 않고, 잠깐 한국어 구간 이름이 보일 뿐입니다.

| 순서 | 파일 | 올릴 위치 |
|---|---|---|
| 1 | `backtest.json` | `data/` 폴더 |
| 2 | `backtest.py` | `scripts/` 폴더 |
| 3 | `index.html` | 저장소 **루트** |

GitHub Actions 는 돌릴 필요 없습니다. `backtest.json` 에 구간 key 를 직접 넣어 뒀고,
`backtest.py` 는 다음 주간 자동 실행(토요일) 때 같은 결과가 나오도록 맞춰 둔 것뿐입니다.
직렬화 결과가 현재 JSON 과 한 글자도 다르지 않은 것을 확인했습니다.

### 올린 뒤 확인

1. 주소에 `?v=아무거나` 붙여 열기 (Pages 캐시 회피)
2. 오른쪽 위 `한국어 | English` 탭이 보이는지
3. 영어로 바꿨을 때 **상승이 초록**으로 바뀌는지
4. 시크릿 창이나 영어 브라우저로 열면 처음부터 영어로 뜨는지
5. **어색한 영어 표현이 있으면 알려 주세요** — 번역은 어시스턴트가 쓴 것이라 검수가 필요합니다

---

## 2. 결정 완료 — 다시 묻지 마세요

- **VIX 구간 경계** → 관행 숫자(12/15/20/30/40). `CLAUDE.md` 4번
- **공포탐욕 3Y·5Y 버튼** → 그대로 둠. `CLAUDE.md` 4번
- **다국어 색 규칙** → 언어 따라 전환. `CLAUDE.md` 6번
- **언어 기억 방식** → 주소(`?lang=`) + localStorage 둘 다
- **통계 도구** → Umami. API 는 Hobby 에서 막힘. `CLAUDE.md` 5번
- **작업 순서** → UI(완료) → 도메인 → SEO

---

## 3. 다음 순서 (사용자가 정한 것)

1. **도메인 구입** — 연 1~2만원. GitHub Pages 에 무료로 연결됩니다.
   **SEO 이력은 도메인에 쌓이므로** `github.io` 에 쌓아 두고 옮기면 대부분 날아갑니다.
2. **SEO 기본기** — `CLAUDE.md` 9-2 참고. 지금 `<head>` 에 아무것도 없습니다.
   카톡·SNS 미리보기(Open Graph)가 없어 링크를 공유해도 밋밋한 주소만 나갑니다.
3. **정적 설명 글** — 숫자가 전부 JS 로 그려져서 검색엔진이 읽을 게 없습니다.
4. 그다음 후보는 `CLAUDE.md` 9번.

---

## 4. 미뤄 둔 것

- **텔레그램 주간 보고** — Umami Hobby 는 API 가 막혀 있어 보류.
  사용자는 봇을 이미 갖고 있고 **주 1회 월요일 아침**을 원합니다.
  셀프호스팅(Vercel + **Neon**)으로 옮기면 가능해집니다. `CLAUDE.md` 5번.
  Supabase 는 무료 프로젝트가 조직당 2개뿐이라 사용자가 이미 다 쓰고 있을 수 있습니다.
- **ETF 전환 실측** — 지수 대비 낙폭 차이(%p)를 아직 못 쟀습니다.
  개발 환경에서 야후가 막혀서, 사용자 브라우저로 재야 합니다. `CLAUDE.md` 8번·9-1번.
- **`market.json` 종가 시계열 정리** — 1,276일치 공개 재배포를 줄이는 작업. 비용 0.

---

## 5. 작업 환경이 바뀝니다

Cowork 세션에서는 **저장소에 push 를 못 합니다**(git 프록시가 막고, Cowork 에는
저장소를 세션에 추가하는 UI 가 없습니다 — anthropics/claude-code 이슈 #76248).
그래서 지금까지는 "어시스턴트가 파일을 만들어 주면 사용자가 GitHub 웹에 끌어다 놓는" 방식이었습니다.

**Claude Code on the web(claude.ai/code)으로 옮기면** 어시스턴트가 브랜치를 push 하고
PR 을 만들 수 있어, 사용자는 Merge 버튼만 누르면 됩니다.
이 문서와 `CLAUDE.md` 를 저장소에 넣는 이유가 그것입니다 — 새 세션이 맥락을 이어받게.
