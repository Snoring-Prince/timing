# 바로 할 일

최종 갱신: 2026-09-14 (대가들의 선택 — 정찰 추가) · 자세한 배경은 `CLAUDE.md` 참고

---

## 0. 지금 상황 한 줄

**대시보드 쪽은 사실상 다 끝났습니다.** 화면 개편(패널 3개 → 합친 차트 1개),
다국어, 도메인, 머리글, 본문, 실시간 중계소, 실패 알림, 후원 한 줄까지 저장소에
올라가 있습니다. **옛 화면 백업**: `backup/v1-3panel` 브랜치.

**남은 것은 두 가지뿐입니다** — 사용자가 손으로 해 주실 잔일 몇 개(아래 1번),
그리고 아직 시작하지 않은 **대가들의 선택**(아래 3번).

---

## 1. 사용자가 해 주실 것 (각 5분)

| | 무엇 | 어디서 |
|---|---|---|
| A | **후원 주소가 살아 있는지 눌러 보기** | `https://buymeacoffee.com/snoringprince` |
| B | **Buy Me a Coffee 페이지 채우기** | Name · About · Website 칸 |
| C | **`quote.json` 지우기** | GitHub → Branches → `data` → 휴지통 |
| D | **장중에 `실시간` 이 뜨는지 보기** | 밤 10:30~새벽 5시에 사이트 열기 |
| E | **Search Console 확인** | `보안 및 직접 조치` · 색인 생성 요청 |
| F | **`SEC_CONTACT` 비밀값 넣기** | Settings → Secrets and variables → Actions |
| G | **정찰 한 번 돌리기** (F 다음) | Actions → `Probe SEC` → Run workflow |

**A·B 는 제가 못 합니다.** 이 개발 환경에서 `buymeacoffee.com` 접속이 막혀 있어
(프록시 403) 주소가 진짜 살아 있는지를 볼 수가 없습니다. 형식은 맞습니다.

**D 가 유일하게 아직 확인 안 된 것입니다.** 중계소가 CNN 공포탐욕에 닿는 것은
확인됐는데(일요일에 받아 봄), **장중에 그 숫자가 실제로 움직이는지**는 미국장이
열렸을 때 봐야 압니다. 오른쪽 위 시각이 `데이터 기준` 대신 **`실시간`** 으로
바뀌어 있고, 공포탐욕 숫자가 낮에 본 값과 다르면 움직이는 것입니다.
(SPY·QQQ·VIX 는 예전 워크플로에서 140회 확인했으므로 확실합니다.)

**F·G 가 대가들의 선택을 시작하는 열쇠입니다.** 아래 3번에 자세히 적었습니다.

**E** — 이 도메인은 **2026년 4월 5일에 구글이 크롤링한 기록**이 있습니다. 어제 사셨는데도요.
전에 누가 쓰다 놓은 도메인이라는 뜻입니다. 대부분 무해하지만, 이전 주인이 스팸에
썼다면 거기 뜹니다. **"문제가 감지되지 않았습니다"** 가 나오면 깨끗한 겁니다.

---

## 2. 끝난 것 (다시 제안하지 마세요)

| 언제 | 무엇 |
|---|---|
| 09-09 | 화면 개편 — 패널 3개를 합친 차트 1개로 |
| 09-09 | 다국어 · 색 규칙이 언어를 따라 뒤집힘 |
| 09-10 | 안 쓰는 장중 시세 파이프라인 삭제 (`quote.json` 만 남음 → 1-C) |
| 09-12 | 위 차트 갱신 주 1회 → **평일 매일** (`mergeDaily`) |
| 09-13 | 도메인 `itpaidoff.com` · 머리글 · 공유 카드 · 사이트맵 |
| 09-13 | **실시간 중계소** (클라우드플레어) — 넷 다 받아옴 |
| 09-13 | SEO 본문 심기 — 크롤러가 받는 글 304자 → 4,418자 |
| 09-13 | 검색어 방향을 "투자 타이밍"으로 · 제목 태그 계단 정리 |
| 09-13 | 성적표 레이아웃 (구분선·정렬·여백·기간 버튼 연동) |
| 09-13 | 네 칸 계기판 복구 |
| 09-13 | **영어 검수 전부** — ⓘ 툴팁 8개 + 설명 글 7개 절 |
| 09-13 | **설명 글 숫자 대조 봇** (`check_about.py`) |
| 09-13 | **실패 알림을 텔레그램으로** — 예약 워크플로 셋 다 |
| 09-14 | **후원 한 줄** — 주소 넣고 켬 |

### 안 하기로 한 것

- **야간 모드** — 사용자가 "패스". `CLAUDE.md` 9-1
- **두 잣대 엇갈림 표시** — 사용자가 "패스". `CLAUDE.md` 9-4-3

  **여기 예전에 "이제 한 차트에서 두 선을 같이 보니 만들기 쉬워졌다"고
  적혀 있었는데 틀렸습니다.** 공포탐욕과 VIX 는 `mainOn()` 때문에 **한 번에
  하나만 켜집니다.** 지금 화면에서는 엇갈림이 아예 안 보이고, 만들려면
  오른쪽 축 하나가 0~100 과 10~45 를 동시에 써야 해서 "선은 최대 2개"라는
  설계 결정을 뒤집어야 합니다. **쉬워진 게 아니라 오히려 비쌉니다.**

---

## 3. 다음 큰 일 — 대가들의 선택

**사용자가 생각하는 진짜 메인 제품입니다.** 지금 대시보드는 곁가지입니다.

대가들이 무엇을 샀고 **그 선택이 지금 어떤 결과를 낳았는지.** 한국어로 이미
서비스하는 앱이 있지만(사용자 것 아님), **차별점은 "지금 뭘 들고 있나"가 아니라
"그래서 어떻게 됐나"입니다.** 도메인 이름(`itpaidoff` = 결실을 맺었다)이 그 뜻입니다.

데이터는 **13F 공시(SEC)** — 미국 정부 저작물이라 **퍼블릭 도메인**입니다.
**이 프로젝트에서 가장 큰 이점**입니다. 지금 대시보드를 괴롭히는 데이터 라이선스
문제(`CLAUDE.md` 11번)가 여기서는 통째로 빠집니다.

한계는 화면에 밝혀야 합니다 — **45일 지연 공시**, **미국 주식 롱 포지션만** 보입니다.

**살아 있는 투자자의 이름을 도메인이나 간판에 넣지 마세요.** 13F 데이터를 쓰는 것과
그 사람 이름을 거는 것은 완전히 다른 이야기입니다(퍼블리시티권·상표).

### 지금 어디까지 됐나 (2026-09-14)

**누구부터 할지는 정해졌습니다 — 버크셔(버핏) 한 명입니다.** 여덟 명을 고르는
기준과 뺀 사람들의 이유는 `docs/masters-13f.md` 에 적어 뒀습니다(어시스턴트용 문서라
읽으실 필요는 없습니다). 버크셔가 첫 번째인 이유는 유명해서가 아니라 **답을 맞춰 볼 수
있는 유일한 곳**이기 때문입니다 — 연차보고서에 보유 종목이 직접 나오므로, 우리가
계산한 값이 맞는지 대조할 수 있습니다.

**이번에 만든 것: 정찰(Probe) 한 개.**

SEC 자료가 실제로 어떻게 생겼는지를 **여기서는 볼 수가 없습니다.** 야후와 똑같이
개발 환경에서 접속이 막혀 있습니다(오늘 다시 재 봤고 여전히 막혀 있습니다).
그런데 이 프로젝트에는 **짐작으로 진단했다가 틀린 기록이 여러 번** 있습니다.
그래서 코드를 짜기 전에 **GitHub 안에서 한 번 받아 와서 원본을 그대로 올려 두는
심부름꾼**을 먼저 만들었습니다. 해석은 한 글자도 하지 않습니다 — 받아서, 크기와
개수를 세고, 원본을 파일로 남깁니다.

**그걸 돌리려면 두 가지가 필요합니다.**

1. **SEC 가 요구하는 연락처 메일 주소** — SEC 는 이름 없는 요청을 거절합니다.
   누가 받아 가는지 메일 주소를 밝혀야 합니다. 이 저장소는 **공개**라 주소를 코드에
   적으면 긁어가는 프로그램에 그대로 노출됩니다. 그래서 텔레그램 토큰과 똑같이
   **비밀값**으로 넣습니다(위 1-F).
2. **Actions 탭에서 버튼 한 번** (위 1-G).

**주소를 코드에 넣지 않고 여쭙는 이유**는, 그 주소가 SEC 쪽으로 실제로 나가기
때문입니다. 쓰시던 메일을 쓸지, 이 용도로 하나 새로 만들지는 사장님이 정하실
일이지 제가 정할 일이 아닙니다.

돌리고 나면 그 결과(원본)를 읽고 **그때 비로소** 데이터 구조를 설계합니다.
그 뒤 순서는 이렇습니다 — 받아 오는 스크립트 → 연차보고서와 대조 → 화면.
**대조가 맞기 전에는 두 번째 사람으로 넘어가지 않습니다.**

---

## 4. 알고만 있으면 되는 것

- **텔레그램 주간 방문자 보고** — Umami Hobby 는 API 가 막혀 있어 보류입니다.
  **실패 알림과는 다른 이야기입니다** — 그쪽은 이미 돕니다. 셀프호스팅
  (Vercel + **Neon**)으로 옮기면 가능해집니다. `CLAUDE.md` 5번.
- **후원은 0건이 기본값입니다.** 개인 도구 사이트 전환율은 방문자 1,000명당
  1명 아래가 보통이고, 이 사이트는 아직 색인도 안 됐습니다. 그래서 `support`
  이벤트로 **누른 횟수**를 따로 셉니다 — 그게 있어야 "안 좋아해서"인지
  "본 사람이 없어서"인지를 나눌 수 있습니다.
- **색인은 기다리는 일입니다.** 재판정까지 며칠~몇 주 걸립니다.
  `URL 검사` 에서 계속 "크롤링됨 - 색인 생성 안 됨" 이 나오면 본문을 더 늘려야
  한다는 뜻입니다(`CLAUDE.md` 9-4-4 에 아직 안 쓴 근거가 남아 있습니다).

---

## 5. 결정 완료 — 다시 묻지 마세요

- **VIX 구간 경계** → 관행 숫자(12/15/20/30/40). `CLAUDE.md` 4번
- **공포탐욕 3Y·5Y 버튼** → 그대로 둠
- **다국어 색 규칙** → 언어 따라 전환 (한국어 상승=빨강 / 영어 상승=초록)
- **언어 기억 방식** → 주소(`?lang=`) + localStorage 둘 다
- **통계 도구** → Umami. API 는 Hobby 에서 막힘
- **SPY 유지** (VOO 아님) → VOO 는 2010년 상장이라 2008년이 빠짐
- **위 차트 왼쪽 축은 가격** (낙폭 아님) → 전고점 수위선·빗금은 그림 안에 남김
- **기간 버튼은 한 줄** → 2줄 금지. 더 과거는 좌우 드래그
- **성적표에는 숫자만** → 자동 판정 문장·빗금·설명 문단은 넣었다가 뺐음
- **ETF 전환** → 완료. `market-long.json`·`market.json` 둘 다 SPY·QQQ 기준
- **알림은 텔레그램** → 메일은 계정 설정·cron 편집자에 묶여 조용히 끊김
- **`.about` 글은 HTML 에 직접** → 사전으로 옮기면 크롤러가 빈 페이지를 받음

---

## 6. 일하는 방식

어시스턴트가 브랜치에 올리고 PR 을 만듭니다.
**사용자가 하는 일은 GitHub 에서 초록색 Merge 버튼을 누르는 것뿐입니다.**

**머지한 뒤의 작업은 새 PR 로 갑니다.** 닫힌 PR 에는 아무리 커밋을 쌓아도
사이트에 반영되지 않습니다. 실제로 세 번 그런 일이 있었습니다.
PR 을 받으실 때 **몇 번 PR 인지, 커밋이 몇 개인지** 같이 적어 드립니다.
