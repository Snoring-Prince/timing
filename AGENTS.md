# AGENTS.md

이 저장소는 **어시스턴트 둘이 번갈아 일한다** — Claude Code 와 ChatGPT(Codex).
사용자는 비개발자이고, **둘 사이의 인수인계에는 끼지 않는다.**
그래서 서로에게 넘길 말은 사용자에게 말하지 말고 **저장소에 적는다.**

## 문서 위계

| 파일 | 무엇이 들어 있나 | 누가 읽나 |
|---|---|---|
| `CLAUDE.md` | **사실과 이력의 단일 출처** — 무엇이 어디 있고, 왜 그렇게 돼 있고, 무엇을 재 봤나 | 둘 다 |
| `AGENTS.md` | **작업 원칙** — 어떻게 일하나 (이 파일) | 둘 다 |
| `docs/worklog/` | **세션 사이의 인수인계** — 짧은 일지 두 권 | 둘 다 |
| `docs/design-system.md` | 화면의 재료표 (색·글꼴·선·여백) | 화면을 고칠 때 |
| `docs/masters-13f.md` | 대가들의 선택 인수인계 (영어) | 13F 를 만질 때 |

**사실이 어긋나면 `CLAUDE.md` 가 이긴다.** 이 파일에 적힌 프로젝트 사실
(구조·이름·주소)이 `CLAUDE.md` 와 다르면 `CLAUDE.md` 가 맞고, 이 파일을 고친다.
원칙(어떻게 일하나)이 부딪히면 **사용자가 지금 시킨 일을 먼저 하고, 부딪힌 것을 숨기지 말고 알린다.**

**같은 사실을 두 파일에 적지 않는다.** 두 벌이 되면 갈라지고, 갈라지면 둘 다 못 믿게 된다.
이 프로젝트는 그 실패를 이미 여러 번 겪었다(`CLAUDE.md` 9-3 의 *"사람이 붙어야 하는 표는 결국 낡습니다"*).

---

## 세션을 시작할 때 (셋)

```
1  git fetch origin main        지난 PR 이 머지됐는지 본다. 머지됐으면 새 main 위에서 새 브랜치.
2  열려 있는 PR 을 본다          다른 에이전트가 지금 무엇을 잡고 있는지는 여기에 있다.
3  docs/worklog/ 두 권의 맨 위 3개를 읽는다
```

**2번을 일지로 대신하지 않는다.** "지금 작업 중" 같은 표식을 손으로 적어 두면
그 세션이 죽었을 때 그대로 남아 거짓말이 된다. **GitHub 의 열린 PR 이 이미 그걸 말한다.**
같은 파일을 건드리는 PR 이 열려 있으면, 그 PR 이 머지되기를 기다리거나
**겹치지 않는 파일부터** 한다.

---

## 0. 프로젝트 정체성

이 저장소는 `itpaidoff.com`의 웹서비스 프로젝트다.

현재 첫 번째 서비스는 **"다니엘의 타이밍"**이다.

이 프로젝트의 장기적인 목표는
주린이, 테마주·유행·주변 사람의 의견 등에 휩쓸리다가
"나는 어떤 기준으로 투자해야 할까?"라고 고민하기 시작한 사람에게

- 실제 시장 데이터
- 과거의 기록
- 통계
- 시각화
- 읽는 재미가 있는 스토리

를 결합해서 **투자 방법을 탐구할 수 있는 정보 서비스**를 만드는 것이다.

현재는 작고 단순한 첫 번째 서비스지만 앞으로 다음과 같은 여러 서비스를 추가한다.

1. 다니엘의 타이밍
2. 사무엘의 결정 (가제)
3. 기타 투자 방법론
4. 대가들의 선택 (13F 기반)

앞으로 여러 언어로 서비스할 예정이다.

따라서 현재의 작은 기능을 수정할 때도
**미래에 다른 투자 방법과 언어가 추가될 수 있다는 점을 고려한다.**

---

## 1. 사용자

사용자는 비개발자다.

사용자와의 대화는 기본적으로 **한국어로 한다.**
개발 용어를 사용할 때는 필요한 만큼만 사용하고,
가능하면 "무엇을 왜 바꾸는지"를 먼저 설명한다.

사용자는 코드를 직접 다루는 사람이 아니다.
따라서 사용자가 해야 할 작업을 최소화한다.

가능하면 다음과 같은 형태로 작업한다.

- 실제 파일을 읽는다.
- 실제 동작을 확인한다.
- 원인을 확인한다.
- 변경 범위를 최소화한다.
- 테스트한다.
- 브랜치를 만들고 PR을 만든다.
- 사용자는 PR을 확인한 뒤 merge한다.

---

## 2. 가장 중요한 작업 원칙

### 2-1. 추측하지 않는다

이 프로젝트에서는 추측으로 버그를 진단하지 않는다.

문제가 있다고 생각되면 먼저:

1. 실제 파일을 읽고
2. 실제 데이터를 확인하고
3. 실제 코드를 따라가고
4. 가능하면 직접 계산하거나 테스트해서
5. 확인된 사실과 추측을 구분한다.

"아마 이럴 것이다"를 사실처럼 보고하지 않는다.

특히 검색엔진, 데이터 계산, 차트 위치, 날짜, 통계 등은
실제 결과를 확인한다.

---

### 2-2. 기존 구조를 존중한다

기존 코드가 복잡해 보인다는 이유만으로
리팩터링하거나 구조를 갈아엎지 않는다.

현재 구조에는 과거 버그와 시행착오를 통해 만들어진 규칙이 많다.

따라서 먼저:
- 현재 구조를 이해하고
- 왜 그렇게 되어 있는지 확인한 뒤
- 정말 필요한 부분만 수정한다.

작은 수정에 대규모 리팩터링을 하지 않는다.

---

### 2-3. "좋아 보이는 변경"과 "필요한 변경"을 구분한다

사용자가 요청하지 않은 기능을 임의로 추가하지 않는다.

특히 다음을 함부로 하지 않는다.

- 새로운 프레임워크 도입
- 빌드 시스템 도입
- 데이터 구조 변경
- 파일 대량 분리
- URL 구조 변경
- 기존 화면 구성 변경
- 투자 판단을 유도하는 새로운 문구
- 새로운 추정/판정 로직

필요성이 확인될 때만 제안하고,
중요한 구조 변경은 먼저 사용자에게 설명한다.

---

## 3. 현재 배포 구조

현재 사이트는 GitHub Pages로 운영한다.

- Repository: `Snoring-Prince/timing`
- Branch: `main`
- Domain: `https://itpaidoff.com`
- Hosting: GitHub Pages
- `main` 브랜치의 변경사항이 곧 사이트 배포로 이어진다.
- 저장소 루트의 `CNAME`이 `itpaidoff.com` 연결을 담당한다.

### 절대 잊지 말 것

**`CNAME`을 삭제하거나 임의로 변경하지 않는다.**

이 파일은 도메인 연결에 필수적이다.

배포 서버를 새로 만들거나 다른 호스팅으로 옮기는 작업도
사용자의 명시적인 요청 없이는 하지 않는다.

---

## 4. 현재 저장소 구조

**정본은 `CLAUDE.md` 2번이다.** 거기에 파일마다 무엇을 하는지까지 적혀 있고,
데이터·워크플로가 늘 때 같이 갱신된다. 여기에 트리를 한 벌 더 두면
**두 벌이 갈라지고 결국 둘 다 못 믿게 된다.**

방향만 잡기 위한 요약:

```text
timing/
├── CNAME              도메인 연결 — 지우지 말 것
├── index.html         다니엘의 타이밍 (단일 파일)
├── titans/
│   ├── index.html         튕기는 자리 — /titans/berkshire/ 로 보냄
│   └── berkshire/
│       └── index.html     대가들의 선택 · 버크셔 (단일 파일)
├── workers/live.js    Cloudflare Worker (실시간 중계)
├── scripts/           데이터 수집 · 백테스트 · 검사 · 알림
├── data/              봇이 갱신하는 JSON
├── docs/              design-system.md · masters-13f.md · worklog/
└── .github/workflows/
```

**`titans/index.html` 과 `titans/berkshire/index.html` 은 다른 파일이다.**
화면은 뒤쪽이고, 앞쪽은 옛 주소에서 튕겨 보내는 빈 자리다.

### 현재 메인 화면

`index.html` 하나에 HTML + CSS + JavaScript가 들어 있다.

현재 구조를 유지한다.

파일 하나에 들어 있다는 이유만으로
임의로 여러 파일로 분리하지 않는다.

---

## 5. `.about` 본문은 특별 취급한다

### 매우 중요

`index.html`의 `.about` 섹션에 있는 설명 글은
**HTML에 직접 존재해야 한다.**

이 내용을 JavaScript 사전 `D` 등으로 옮기지 않는다.

이유:
검색엔진 크롤러가 JavaScript를 실행하지 않거나
정적 HTML만 읽을 경우에도
설명 본문이 실제 HTML에 존재해야 하기 때문이다.

따라서 다음은 금지한다.

```text
HTML에 있는 .about 본문 → JS 문자열로 이동
```

현재의 정적 `.about` 구조를 유지한다.

---

## 6. `check_about.py`를 항상 고려한다

`scripts/check_about.py`는 `.about`에 적힌 숫자와
`data/backtest.json`의 실제 계산 결과를 비교한다.

따라서 `.about`의 문장을 수정하면
다음 두 가지를 확인해야 한다.

1. 실제 문장과 최신 데이터가 일치하는가?
2. `check_about.py`가 그 문장을 계속 정상적으로 찾을 수 있는가?

설명 문구를 바꾸면서 숫자나 핵심 문장 구조를 변경했다면
관련 정규식도 검토한다.

검사를 우회하거나 삭제해서 "초록불"을 만드는 식으로 해결하지 않는다.

---

## 7. `meta keywords`는 추가하지 않는다

`<meta name="keywords">`를 추가하지 않는다.

검색 노출을 개선하기 위해 다른 SEO 요소를 검토할 수는 있지만,
`meta keywords`를 SEO 해결책으로 사용하지 않는다.

---

## 8. 다국어 전략

이 서비스는 장기적으로 여러 언어를 지원한다.

현재는 한국어와 영어가 있다.

현재 언어 데이터는 `D.ko`, `D.en` 등의 구조로 관리한다.

향후 일본어, 중국어, 스페인어 등 다른 언어가 추가될 수 있으므로
화면 로직과 언어 문구를 분리하는 현재 원칙을 유지한다.

### 중요한 원칙

언어별로 단순히 단어를 번역하는 것이 목적이 아니다.

각 언어권의 사용자가 실제로 이해하고 검색할 수 있도록
자연스러운 표현을 사용한다.

예:

한국어:
- 공포탐욕지수
- 투자 타이밍
- VIX
- S&P 500 수익률

영어:
- Fear & Greed Index
- market timing
- VIX
- S&P 500 returns

앞으로 언어가 추가되어도
하나의 데이터와 계산 로직을 여러 언어가 공유하는 구조를 유지한다.

---

## 9. SEO 원칙

검색엔진은 이 프로젝트에서 매우 중요하다.

Google과 Naver 모두를 고려한다.

### SEO 작업에서는 최소한 다음을 함께 확인한다.

- `<title>`
- `<meta name="description">`
- canonical
- hreflang
- Open Graph
- JSON-LD / structured data
- H1
- 정적 HTML 본문
- sitemap.xml
- robots.txt

SEO를 수정한다고 해서
무조건 새로운 파일이나 복잡한 프레임워크를 도입하지 않는다.

### 현재 원칙

검색엔진에 중요한 정보는
가능한 한 정적 HTML에서도 확인 가능해야 한다.

특히:
- 제목
- 설명
- H1
- 핵심 서비스 설명

등을 JavaScript에만 의존하지 않는다.

---

## 10. SEO 작업 시 주의사항

현재 `?lang=en`, `?lang=ko` 형태의 언어 URL 구조를 사용하고 있다.

언어 URL, canonical, hreflang, sitemap을 수정할 때는
서로 모순되지 않는지 반드시 확인한다.

예를 들어:
- canonical은 다른 URL을 가리키는데
- hreflang은 또 다른 URL을 가리키거나
- sitemap에는 한쪽만 들어가는

식의 불일치를 만들지 않는다.

URL 구조를
`?lang=ko` → `/ko/`
처럼 바꾸는 것은 단순 SEO 수정으로 취급하지 않는다.

그 작업은 URL migration으로 취급하고
사용자와 먼저 논의한다.

---

## 11. 투자 정보 서비스의 표현 원칙

이 사이트의 목적은 사용자가 데이터를 보고
자기 투자 방법을 생각할 수 있도록 정보를 제공하는 것이다.

사이트가 자동으로 다음과 같은 결론을 단정해서는 안 된다.

```text
"지금 사세요."
"지금이 저점입니다."
"이 조건이면 매수해야 합니다."
"이 방법이 가장 좋습니다."
```

과거 데이터에서 확인되는 사실과 통계는 보여주되,
과거 성과를 미래의 보장처럼 표현하지 않는다.

현재 서비스의 기본 철학:

> **예측이 아니라 기록.**

데이터에 기반한 사실, 계산 방법, 표본 범위, 한계를
가능한 한 명확히 설명한다.

---

## 12. 데이터 원칙

데이터를 변경하거나 계산 로직을 변경할 때는
원본 데이터와 계산 과정을 이해한다.

현재 주요 데이터:

- `market.json`
- `market-long.json`
- `backtest.json`
- `data/titans/...`

등.

데이터 파일은 GitHub Actions에 의해 자동으로 갱신될 수 있다.

따라서 자동 갱신되는 파일을 수동으로 덮어써서
다음 주 작업을 망가뜨리지 않는다.

---

## 13. GitHub Actions / 자동화

`.github/workflows/` 아래에는
데이터 수집과 백테스트 등의 자동화가 있다.

작업 전 관련 workflow를 확인한다.

특히:
- 데이터 갱신
- backtest
- `check_about.py`
- 13F
- 알림

사이에 어떤 의존 관계가 있는지 확인한다.

사용자가 단순히 화면 문구를 바꾸라고 했는데
workflow를 함께 수정할 필요가 없다면 건드리지 않는다.

---

## 14. Cloudflare Worker

실시간 데이터는 별도의 Cloudflare Worker를 사용한다.

코드는:

```text
workers/live.js
```

에 있다.

이 파일을 수정할 때는
실제 배포 환경과 저장소 코드가 어떻게 연결되어 있는지 먼저 확인한다.

Cloudflare 설정이나 비밀값을 추측하지 않는다.

---

## 15. 비밀정보

다음과 같은 정보는 저장소에 직접 넣지 않는다.

- API key
- token
- password
- private credential
- Telegram secret
- 외부 서비스의 private URL/token
- 기타 인증정보

`.env` 또는 GitHub Secrets 등 기존 방식이 있다면
그 방식을 따른다.

---

## 16. UI / 디자인 원칙

사용자는 다음과 같은 문제를 자주 정확하게 발견한다.

- 글자가 통일되지 않음
- 축이 무엇을 의미하는지 모호함
- 여백이나 폭이 어색함
- 모바일에서 읽기 어려움
- 색 대비 부족
- 날짜/연도 위치가 이상함
- 차트가 실제 데이터보다 오해를 불러일으킴

이런 피드백이 있으면
"디자인 취향"이라고 넘기지 말고
실제 화면에서 확인한다.

가능하면 픽셀, 폭, 실제 DOM 위치, 데이터 값 등을 측정한다.

---

## 17. 차트의 신뢰성

차트는 실제 데이터가 없는 것을 있는 것처럼 보이면 안 된다.

특히:
- 없는 거래시점
- 존재하지 않는 가격
- 잘못된 날짜
- 잘못된 축
- 분기말이라는 이유로 실제 거래일처럼 보이는 표시
- 전량 매도 후에도 이어지는 선

등을 만들어서는 안 된다.

시각적으로 예뻐지는 것보다
**데이터를 오해시키지 않는 것**이 우선이다.

---

## 18. 테스트

수정 후에는 가능한 한 실제 동작을 확인한다.

필요한 경우:
- JavaScript 오류
- 모바일 폭
- 언어 전환
- 차트
- 데이터 로딩
- 링크
- SEO 태그
- 자동화 스크립트

등을 확인한다.

특히 모바일은 최소한 다음 폭을 의식한다.

```text
320px
360px
390px
900px
```

언어가 여러 개일 경우
한국어와 영어 모두 확인한다.

---

## 19. 브랜치 / PR 규칙

### `main`에 직접 작업하지 않는다.

작업할 때는 새로운 브랜치에서 작업하고
PR을 만든다.

사용자는 PR을 확인한 뒤 직접 merge한다.

### 중요

이미 merge된 PR의 브랜치를 계속 재사용하지 않는다.

merge 이후 새로운 작업은
반드시 새로운 브랜치에서 시작한다.

작업 시작 전에 현재 `main`이 최신 상태인지 확인한다.

### 일지를 쓴다

PR 을 만들면 **자기 일지에 항목 하나**를 더한다 (`docs/worklog/` · 형식은 그 폴더의 `README.md`).
같은 PR 에 넣으므로 커밋이 따로 늘지 않는다.

---

## 20. 작업 보고 형식

작업이 끝나면 사용자에게 다음을 명확히 알려준다.

### 무엇을 바꿨는가

사용자가 이해할 수 있는 말로 설명한다.

### 왜 바꿨는가

실제 확인된 근거를 짧게 설명한다.

### 무엇을 테스트했는가

실제로 수행한 테스트만 적는다.

### PR

PR 번호와 제목을 알려준다.

### 주의사항

사용자가 merge 전에 알아야 할 문제가 있으면 명확히 적는다.

"테스트했습니다"라고 쓰려면
실제로 테스트한 경우에만 쓴다.

---

## 21. 기존 문서

작업 전에 관련 문서를 읽는다. 위계는 이 파일 맨 위의 표에 있다.

```text
docs/worklog/       ← 세션을 열면 먼저. 두 권의 맨 위 3개.
CLAUDE.md           ← 사실과 이력의 단일 출처
docs/design-system.md · docs/masters-13f.md
다음에 할 일.md
```

`CLAUDE.md`에 과거 시행착오와 현재 설계 결정이 많이 기록되어 있다.
**일지는 그것을 되풀이하지 않는다** — 일지는 "무엇을 했고 무엇을 넘긴다"이고,
"왜 그렇게 했나"는 `CLAUDE.md` 에 적고 일지에서는 가리키기만 한다.

`CLAUDE.md`와 `AGENTS.md`가 충돌하는 경우
현재 사용자가 명시적으로 요청한 작업을 우선하되,
충돌 내용을 숨기지 말고 사용자에게 알린다.

---

## 22. 미래 확장성

이 프로젝트는 앞으로 기능이 늘어날 예정이다.

예상되는 방향:

```text
itpaidoff.com

├── 다니엘의 타이밍
├── 사무엘의 결정
├── 기타 투자 방법론
└── 대가들의 선택
```

그리고 각 서비스는:

```text
데이터
→ 계산
→ 시각화
→ 설명
→ 출처
→ 한계
```

라는 공통 철학을 유지한다.

언어는 그 위에 추가된다.

따라서 새로운 기능을 만들 때
가능하면 하나의 서비스에만 종속되지 않는 구조를 선택한다.

---

## 23. 가장 중요한 한 문장

이 프로젝트에서는

**"코드를 많이 바꾸는 것"보다 "실제 데이터와 사용자가 보는 화면이 정확하게 일치하도록 만드는 것"을 우선한다.**
