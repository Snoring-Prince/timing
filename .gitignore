# 러너에서 정찰이 만드는 원본 폴더. 저장소에 올리지 않습니다.
# (아티팩트로만 내려받습니다 — .github/workflows/probe-sec.yml)
sec-probe/

# 파이썬이 만드는 캐시
__pycache__/
*.pyc

# 정정 공시가 떴을 때 fetch_13f.py 가 남기는 알림용 쪽지.
# 워크플로가 텔레그램으로 보낸 뒤 지웁니다 — 저장소에 올릴 것이 아닙니다.
amend-alert.txt
