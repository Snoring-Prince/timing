# Daniel's timing — 프로젝트 안내서

이 파일은 Claude Code 가 저장소를 열 때 자동으로 읽습니다.
작업을 시작하기 전에 **끝까지 읽으세요.** 여기 적힌 판단들은 대부분 실측으로 확인한 것이고,
몇 개는 추측으로 진단했다가 틀렸던 기록입니다.

> **먼저 `docs/worklog/` 두 권의 맨 위 3개를 읽으세요.** 이 저장소는 2026-09-18 부터
> **어시스턴트 둘이 번갈아 일합니다** — Claude Code 와 ChatGPT(Codex). 지난 세션이
> 무엇을 했고 무엇을 넘겼는지가 거기 있습니다. 작업 원칙은 `AGENTS.md`(둘 다 읽습니다).
> **이 파일은 사실과 이력의 단일 출처**이고, `AGENTS.md` 의 프로젝트 사실이 여기와
> 다르면 여기가 맞습니다. 자세한 위계는 `AGENTS.md` 맨 위 표에 있습니다.

```
사이트   https://itpaidoff.com          ← 2026-09-13 부터
옛 주소   https://snoring-prince.github.io/timing/  (자동으로 새 주소로 넘어감)
저장소   https://github.com/Snoring-Prince/timing  (Public)
호스팅   GitHub Pages (main 브랜치 루트) · 도메인은 Namecheap
실시간    Cloudflare Worker · 9-2
통계     Umami Cloud (Hobby) · dysan1000@gmail.com
```

**`CNAME` 파일(저장소 루트)이 도메인 연결의 전부입니다.** 지우면 옛 주소로 돌아갑니다.

최종 갱신: 2026-09-23 (대가들의 선택 — 팔린 종목을 방문자 파일에 안 쌓음)

---

## 0. 사용자에 대해

**비개발자입니다.** 터미널과 Git 명령어를 쓰지 않습니다. 이 점을 전제로 일하세요.

- 코드를 설명할 때 전문용어를 늘어놓지 말고, **무엇이 어떻게 바뀌는지**를 말하세요.
- **추측하지 말고 실제로 읽고 계산해서 확인하세요.** 이 프로젝트에서 추측 진단이 여러 번 틀렸습니다.
- **Pages 캐시에 속지 마세요.** raw 파일은 새것인데 사이트는 옛 화면일 수 있습니다.
  주소에 `?v=아무거나`를 붙여 다시 열어 보고 판단하세요.
- 판단을 맡기는 편이지만, 근거를 짧게 대고 추천안을 제시하면 잘 따라옵니다.
  UI 지적이 정확합니다 — "글자가 통일되지 않아", "세로축이 뭘 가리키는지 모르겠어",
  "2025가 두 개 있어", "흰 바탕에 글자 색깔이 눈에 띄지가 않아". **전부 실제 버그였습니다.**
  화면 지적이 들어오면 반박하기 전에 먼저 재 보세요.
- 스스로 발견한 것에 관심이 많습니다. 예: 공포탐욕과 VIX 가 엇갈리는 현상, ETF 상장 이전 데이터 문제.
  둘 다 좋은 지적이었고, 후자는 확인 결과 이 사이트에서는 문제가 아니었습니다.

### 일하는 방식 (2026-09-09 이후)

예전에는 어시스턴트가 파일을 만들어 주면 사용자가 GitHub 웹에 끌어다 올렸습니다.
지금은 **Claude Code on the web** 이라 어시스턴트가 직접 브랜치에 push 하고 PR 을 만듭니다.
**사용자가 하는 일은 GitHub 에서 초록색 Merge 버튼을 누르는 것뿐입니다.**

**PR 을 넘길 때는 반드시 함께 적으세요 — 몇 번 PR 인지, 커밋이 몇 개인지, 무엇이 바뀌는지.**

**머지 시점 사고가 세 번 났습니다.** 사용자가 PR 을 머지한 뒤에도 어시스턴트가
같은(이미 닫힌) 브랜치에 커밋을 계속 쌓아서, 그 작업이 사이트에 반영되지 않았습니다.
`main` 은 옛 화면인데 어시스턴트는 고쳤다고 보고하는 상태가 됩니다.

```
작업 전   git fetch origin main  →  머지됐는지 먼저 확인
머지됐으면 새 main 위로 rebase → force-with-lease → 새 PR 을 만든다
```

**머지 이후의 작업은 반드시 새 PR 입니다.** 닫힌 PR 은 다시 살아나지 않습니다.

### 0-3. 어시스턴트가 둘입니다 — 한 번에 한 쪽만 (2026-09-20 정리)

사용자가 **ChatGPT(Codex)와 함께 씁니다.** 쓰임새는 하나입니다 —
**Claude 의 사용량이 한도에 닿으면 Codex 가 이어받는 것.**

**둘이 서로 말을 주고받지 않습니다.** 2026-09-19 에 이슈 하나(#60)를 핫라인으로
두고 작업 배분·검수 요청을 주고받는 체계를 만들었는데, 사용자가 다음 날 끊었습니다 —
*"니네둘이 소통하게 한거는 내가 잘못인거 같아. 뭐하는지 내가 파악할수가 없어."*

**그 판단이 맞습니다.** 이 저장소의 주인은 비개발자이고, 어시스턴트 둘이 주고받는
말은 **사용자가 읽지 않는 자리에서 늘어납니다.** 늘어나면 사용자는 무엇이 정해졌는지
모른 채 PR 만 받게 됩니다. 이 프로젝트가 여러 번 겪은 *"두 벌이 되면 갈라진다"* 가
사람과 문서 사이에서 일어나는 꼴입니다.

**그래서 넘기는 길은 저장소 하나뿐입니다.** 이어받는 쪽이 읽을 것은 늘 같습니다 —
`AGENTS.md` → 이 파일 → `docs/worklog/` 맨 위 → 열려 있는 PR.
**상대를 부르지 않고, 답을 기다리지도 않습니다.**

```
AGENTS.md            작업 원칙 · 둘 다 읽습니다 (Codex 가 자동으로 읽는 파일)
CLAUDE.md            사실과 이력의 단일 출처 · 이 파일
docs/worklog/        인수인계 · claude.md 와 codex.md 두 권
```

**일지는 에이전트마다 한 권입니다.** 한 권에 둘이 쓰면 각자 다른 브랜치에서 같은
자리(맨 위)에 넣게 되어 **PR 마다 머지 충돌**이 나고, 비개발자인 사용자가 그걸
풀 수 없습니다. **자기 것만 쓰고 둘 다 읽으면 충돌이 0입니다.**
상대에게 할 말은 자기 일지의 `넘김` 줄에 적습니다.

**"지금 누가 뭘 잡고 있나"는 일지에 안 씁니다.** 손으로 적은 "작업 중" 표식은
그 세션이 죽으면 그대로 남아 거짓말이 됩니다 — **열려 있는 PR 목록이 이미 그걸
말합니다.** 세션을 열면 `git fetch origin main` 다음에 열린 PR 을 보세요.

**일지에 이유를 길게 쓰지 마세요.** 일지는 *무엇을 했고 무엇을 넘긴다*이고,
*왜 그렇게 했나*는 이 파일이 합니다. 두 곳에 같은 말을 적으면 갈라집니다 —
9-3 의 *"사람이 붙어야 하는 표는 결국 낡습니다"* 가 문서에도 그대로 걸립니다.
형식과 나머지 규칙은 `docs/worklog/README.md` 에 있습니다.

**검수를 상대에게 맡기지 않습니다.** 자기가 만든 것은 자기가 재고 PR 까지 냅니다.
남은 일·막힌 곳은 일지의 `넘김` 줄에 적어 두면 다음에 앉는 쪽이 읽습니다.
이어받는 절차는 `AGENTS.md` 의 **이어받기** 절이 정본입니다.

**주신 `AGENTS.md` 에서 두 가지를 이 파일에 맞췄습니다** —
`거장의 선택`→`대가들의 선택`, `사무엘의 전략`→`사무엘의 결정`. 둘 다 가제지만
**같은 것을 두 이름으로 부르면 둘이 다른 것을 만듭니다.** 저장소 구조 트리도
이 파일 2번을 가리키게 바꿨습니다(한 벌 더 두면 갈라집니다).

### 0-4. 되돌릴 자리 — `backup/v1.0` (2026-09-22)

**사용자가 정했습니다** — *"원래대로 돌아갈 수 있는 장치를 만들고 가자고 … 지금
버전을 v1.0으로 정하자."* 큰 것을 만들기 전에는 **먼저 지금 자리를 브랜치로
찍어 두고** 시작합니다.

```
backup/v1.0        2ba4c41   PR #70 까지 · 분기별 보유 변화 표를 넣기 전
backup/v1-3panel   그 전 세대 — 패널 3개짜리 옛 대시보드 (2026-09-09 이전)
```

**태그가 아니라 브랜치입니다.** 10번에 적힌 대로 어시스턴트는 태그를 push 할 수
없고(조직 정책 403), **2026-09-22 에 다시 해 봐도 그대로였습니다.**
브랜치는 됩니다. `backup/…` 로 이름을 맞춰 두면 목록에서 한눈에 모입니다.

**되돌리는 길은 사용자가 직접 할 일이 아닙니다.** 비개발자이므로 `git` 을 쓸 일이
없습니다. *"v1.0 으로 되돌려줘"* 라고 말하면 어시스턴트가 **그 브랜치의 파일로
되돌리는 PR** 을 만들고, 사용자는 늘 하던 대로 초록 버튼만 누릅니다.

**옛 기록을 지우지 않습니다.** `git revert` 나 브랜치 되감기가 아니라 *v1.0 의
파일 내용으로 덮는 새 커밋*입니다 — 그 사이에 봇이 저장한 데이터(13F·주가·
사이트맵)까지 같이 날아가면 안 되므로, **되돌릴 것은 화면 파일뿐**입니다
(`titans/shared/*`, `index.html` 등). 어느 파일을 되돌릴지는 그때 PR 에 적습니다.

**다음 자리를 찍을 때는 번호를 올리세요** — `backup/v1.1`, `backup/v2.0`.
같은 이름을 덮어쓰면 **돌아갈 자리가 하나 줄어듭니다.**

---

## 1. 지금 상태

**저장소에 전부 올라가 있습니다.** 다국어도 끝났습니다(2026-09-09).
화면은 2026-09-09~10 에 크게 개편됐습니다 — **패널 3개에서 합친 차트 1개로.**

### 화면 구성 (현재)

```
┌ 언어 탭 (한국어 | English)
│
├ 합친 차트  ── 하나의 그림에 가격선 1개 + 심리선 1개
│   제목      켜져 있는 선 이름 + 각각 ⓘ 설명 버튼
│   선 버튼   [S&P500] [NASDAQ100] [공포탐욕] [VIX]
│   기간 버튼 [30D 90D 180D 1Y 3Y 5Y]  ← 한 줄, 모바일에서는 옆으로 스크롤
│   왼쪽 축   가격 (SPY 또는 QQQ, 배당 재투자 기준)
│   오른쪽 축 심리 지수 (공포탐욕 또는 VIX) + 구간 색 띠 + 구간 이름
│   그림 안   전고점 수위선(점선) + 그 아래 낙폭 빗금 + 선 끝에 현재값
│   좌우 드래그로 과거를 넘겨봅니다. 세로축 범위가 그때그때 다시 잡힙니다.
│   네 칸 계기판  전고점 / 현재값 / 경과 일수 / 전고점 대비  ← 차트 바로 밑
│   차트 아래 켜 놓은 선의 자세한 설명 (주린이용 문단)
│
└ 공포와 탐욕 지수별 수익률
    [공포탐욕|VIX]  [30D 90D 180D 1Y 3Y 5Y]  ← 기간은 위 차트와 연동됨
    지금 공포탐욕                          ← 작은 캡션
    33  공포                               ← 38px + 19px
                          수익날 확률  중위 수익률
    여기서 사서 1년 뒤   S&P 500     80%   +13.56%   ← 숫자 칸은 오른쪽 맞춤,
    표본 451일/전체 3,854일 나스닥 100  82%   +17.24%     표 오른쪽 끝은 아래 그래프와 맞춤
    표본 기간 2011–2026
    위 칸: 수익률 중앙값 / 아래 칸: 수익날 확률 (가로축 공유)
    과거 성과일 뿐 미래를 보장하지 않습니다.
```

**없어진 것** (2026-09-09, 3단계): S&P 500 낙폭 패널, 나스닥 100 낙폭 패널,
공포탐욕 게이지 패널. 셋의 역할은 합친 차트가 흡수했습니다.
백업은 `backup/v1-3panel` 브랜치에 있습니다.

**되살린 것: 네 칸 계기판 (2026-09-13).** 옛 낙폭 패널 아래 있던
`전고점 / 현재값 / 경과 일수 / 전고점 대비` 네 칸을 합친 차트 밑으로 옮겨 왔습니다.
사용자가 화면을 보고 "옛날에 그래프 밑에 달려있던 건데 복구할 수 있니"라고 한 것입니다.
**옛 코드를 그대로 붙이지는 않았습니다** — 아래 4번에 고친 세 가지가 적혀 있습니다.

**한 번 만들었다가 사용자 판단으로 되돌린 것**: 표본이 적은 구간에 빗금 치기,
"지금 수준은 평균보다 낫다/못하다"는 자동 판정 문장, 그 아래 설명 문단.
"빗금이 잘 보이지도 않고, 주린이들은 이해를 못 할 것 같다" — 다시 넣지 마세요.

---

## 2. 저장소 구조

```
timing/
├── CNAME                            itpaidoff.com — 이 한 줄이 도메인 연결
├── .gitignore                       amend-alert.txt 등 — 저장소에 올릴 것이 아닌 것
├── robots.txt                       크롤러에게: 다 읽어도 됨 + 사이트맵 위치
├── sitemap.xml                      한 장짜리. 언어는 hreflang 으로 묶음
├── favicon.svg                      탭 아이콘 (무채색)
├── apple-touch-icon.png             180px. favicon.svg 를 크로미움으로 구워 만듦
├── og.png / og-ko.png               공유 카드 1200×630, 영어·한국어 두 장
├── CLAUDE.md                        이 문서 — 사실과 이력의 단일 출처
├── AGENTS.md                        작업 원칙 — Claude·Codex 둘 다 읽습니다 (0-3)
├── 다음에 할 일.md                   바로 할 일
├── docs/
│   ├── design-system.md             화면의 디자인 언어 — 새 화면을 만들기 전에 읽을 것
│   ├── masters-13f.md               대가들의 선택 인수인계 (다른 세션이 이어받는 문서)
│   └── worklog/                     에이전트끼리의 일지 — 세션을 열면 먼저 읽습니다
│       ├── README.md                    형식과 규칙
│       ├── claude.md                    Claude 만 씀
│       └── codex.md                     Codex 만 씀
├── index.html                       대시보드 전체 (단일 파일, HTML+CSS+JS)
├── titans/
│   ├── index.html                   튕기는 자리 — /titans/berkshire/ 로 보냄
│   ├── shared/
│   │   ├── investor.css             모든 투자자의 공통 디자인
│   │   └── investor.js              모든 투자자의 공통 화면·문구·차트·계산
│   └── berkshire/
│       ├── index.html               버크셔 고유 설정·SEO·소개 글만 둔 얇은 껍데기
│       └── portrait.webp            캐리커처 300×200(3:2) · 없으면 그림 칸만 빠짐
├── workers/
│   └── live.js                      실시간 중계소 (클라우드플레어에 붙여넣는 코드)
├── scripts/
│   ├── probe_sec.py                 SEC 13F 정찰 — 받아서 재고 원본만 저장 (손으로)
│   ├── probe_amendments.py          정정 공시 정찰 — 원문을 받아 태그만 셈 (손으로)
│   ├── probe_splits.py              액면분할 이력 정찰 — 야후 원문만 저장 (손으로)
│   ├── watch_13f.py                 최신 접수번호만 가볍게 감시 → 바뀐 투자자 수집
│   ├── fetch_13f.py                 등록된 투자자 한 명의 13F 원문 수집·검산·저장
│   ├── titans/
│   │   ├── __init__.py
│   │   ├── registry.py              투자자 목록 검증·공시책 읽기
│   │   └── sec.py                   SEC 요청·최신 제출 목록 파싱
│   ├── fetch_prices.py              공시 종목별 일별 종가 → 공유 가격 캐시 (평일 증분·주 1회 전체)
│   ├── fetch_tickers.py             CUSIP → 티커 (OpenFIGI·SEC 이름) — 로고용, 주 1회
│   ├── fetch_sectors.py             회사 이름 → SEC 업종코드(SIC) — 섹터 이름표, 주 1회
│   ├── onetime/
│   │   └── convert_13f_text.py      2013년 이전 텍스트 공시를 한 번 변환한 기록
│   ├── fetch_data.py                지수 + 공포탐욕 + VIX 현재값  (매일)
│   ├── fetch_long.py                장기 이력 SPY/QQQ/VIX/F&G     (주 1회)
│   ├── backtest.py                  잣대별 수익률·승률 곡선        (주 1회)
│   ├── check_about.py               설명 글 숫자가 아직 맞는지 대조
│   ├── notify.py                    실패하면 텔레그램으로 알림
│   └── tests/                       화면을 고쳤으면 반드시 돌릴 것 (8번)
│       ├── titans.test.cjs              대가들의 선택 화면 44건 (node --test)
│       ├── test_amendments.py           정정 병합 12건
│       ├── test_fetch_13f.py            13F 수집·보존·반쪽 실행 (SEC 요청 없이)
│       ├── test_13f_watch.py            새 접수번호 감시·작업 분배
│       └── test_prices.py               일별 종가 수집·증분·보존
├── .github/workflows/
│   ├── update-data.yml       cron "0 22 * * 1-5"      평일 하루 1회
│   ├── update-long.yml       cron "30 23 * * 6"       주 1회(토), 백테스트와 30분 간격
│   ├── update-backtest.yml   cron "0 23 * * 6"        주 1회(토) + 설명 글 대조
│   ├── check.yml             PR 때 설명 글 대조 · 텔레그램 시험 발송 버튼
│   │                         (예약 셋 다 실패하면 텔레그램으로 알림)
│   ├── check-titans.yml      PR 때 대가들의 선택 검사 (파이썬 57 · 화면 44)
│   ├── probe-sec.yml         손으로 누를 때만 — SEC 원본을 아티팩트로 (9-3)
│   ├── probe-amendments.yml  손으로 누를 때만 — 정정 원문을 아티팩트로 (9-3)
│   ├── probe-splits.yml      손으로 누를 때만 — 분할 이력 원문을 아티팩트로 (9-3)
│   ├── update-13f.yml        평일 2회 최신 목록 감시 + 일요일 전체 확인 — 13F
│   └── update-prices.yml     cron "23 2 * * 2-6"       미국 장 종료 후 — 일별 종가
└── data/
    ├── market.json                  최근 며칠 + 현재값 (매일)
    ├── market-long.json             1990년부터의 긴 이력 (주 1회)
    ├── backtest.json                곡선 데이터 + 잣대 정의
    └── titans/
        ├── investors.json           자동 수집할 투자자 단일 목록
        ├── berkshire.json           13F 분기별 보유 111개 분기 1998~2026 (달러)
        ├── tickers.json             CUSIP→티커 (ISIN 을 못 만드는 종목만) — 봇이 채움
        ├── sectors.json             CUSIP 앞6자리→SIC 업종코드 — 봇이 채움
        └── prices.json              CUSIP9→일별 종가 — 투자자들이 함께 쓰는 가격 캐시
```

`data` 브랜치는 **사용자가 지웠습니다 (2026-09-23 확인).** 거기 있던
`quote.json` 은 장중 시세 파이프라인의 잔재였고 아무도 안 읽었습니다.

### 앞으로의 주소 구조 (2026-09-14 결정)

사용자의 큰 그림은 **한 도메인 아래 화면 셋**이고, 첫 화면은 갈라지는 허브입니다.

```
itpaidoff.com/            허브 — 셋을 갈라 보여 줌          ← 아직 안 만듦
itpaidoff.com/timing/     다니엘의 타이밍 (지금 루트의 것)   ← 아직 안 옮김
itpaidoff.com/titans/            투자자 목록                ← 둘이 되면 그때 만듦
itpaidoff.com/titans/berkshire/  버크셔                     ← 지금 여기
itpaidoff.com/samuel/     사무엘의 결정 (가제)               ← 나중
```

**저장소는 하나로 갑니다.** Actions·비밀값·알림이 이미 여기 다 있고, 도메인 하나에는
저장소 하나만 물립니다.

**`/titans/` 입니다. `/masters/` 가 아닙니다** — 사용자가 고른 이름이고, 다른 사이트·
골프 대회와 겹치지 않게 하려는 것입니다. **주소는 소문자**로 씁니다(대소문자를 구분하므로
`/Titans/` 로 만들면 소문자로 친 사람이 못 들어옵니다).

**지금 허브를 만들면 안 됩니다.** 구글이 이 사이트를 아직 "크롤링됨 · 색인 생성 안 됨"
으로 두고 있고, 그 판정 이유가 **내용이 얇다**는 것이었습니다(6-2). 루트를 허브로
바꾸면 4,418자짜리 본문이 `/timing/` 으로 내려가고 루트에는 **링크 세 개짜리 더 얇은
페이지**가 올라옵니다 — 판정을 나쁘게 만드는 쪽입니다.

**그리고 GitHub Pages 는 주소 이사(301)를 제대로 못 합니다.** 정적 파일만 내보내는
방식이라 서버가 없습니다. 흉내는 낼 수 있지만(튕기는 빈 페이지) 검색엔진이 301만큼
확실하게 받아들이지 않습니다. (이 환경에서 `docs.github.com` 이 막혀 문서로 재확인은
못 했습니다 — 지식에서 온 것이니 옮기기 전에 한 번 확인하세요.)

순서는 이렇습니다.

```
1  대가들의 선택을 처음부터 /titans/ 폴더에 만든다   ← 루트를 안 건드림, 위험 0
2  색·글꼴·CSS 를 공용 파일로 빼낸다                 ← 세 화면이 같은 얼굴을 갖게
3  갈 곳이 실제로 둘이 되면 루트를 허브로 바꾼다     ← 그때는 허브가 얇지 않음
```

3번 허브에 **링크 세 개만 놓지 마세요.** "이 화면은 무슨 질문에 답하는가"를 각각
제대로 쓰면 그 자체가 본문이 됩니다.

### 장중 시세 파이프라인은 지웠습니다 (2026-09-10)

`fetch_quote.py` · `update-quote.yml` · `data/backtest.py` 중복본을 삭제했습니다.
화면이 읽지 않는 파일을 밤새 만들고 있었습니다. 되살릴 일이 있으면
`backup/v1-3panel` 브랜치와 이 문서 10번의 실측을 함께 보세요.

**`data` 브랜치의 `quote.json` 은 아직 남아 있습니다.** 이제 아무도 쓰지 않으므로
GitHub 웹에서 지워도 됩니다(Branches → data → 휴지통).

---

## 3. 데이터

브라우저는 CORS 때문에 외부 금융 사이트를 직접 못 부릅니다. GitHub Actions 가
대신 받아 저장소에 JSON 으로 두고, 페이지는 자기 집 파일만 읽습니다.

### 파일 세 개의 역할이 다릅니다

| 파일 | 누가 쓰나 | 화면에서 쓰는 곳 |
|---|---|---|
| `market-long.json` | 주 1회 `fetch_long.py` | **위 차트의 선을 전부 이 파일이 그립니다** |
| `market.json` | 매일 `fetch_data.py` | 갱신 시각, 성적표의 "지금 값" **딱 세 값** |
| `backtest.json` | 주 1회 `backtest.py` | 아래 성적표의 곡선 |

### 두 파일을 이어 붙입니다 (2026-09-12)

**긴 이력은 주 1회(토)만 갱신됩니다.** 그래서 금요일 종가가 다음 토요일까지 화면에
안 나왔습니다 — **최대 6일**. 사용자가 "하루에 한 번만 되는 것 같다"고 했는데
실제로는 그보다 나빴습니다(2026-09-12 확인: 화면이 09-08 종가를 그리고 있었음).

고칠 수 없었던 이유는 **기준이 달랐기 때문**입니다.

```
market.json        spx 7,656.98   ← S&P 500 지수
market-long.json   spx   765.96   ← SPY (ETF)
```

그대로 이으면 선이 10배로 솟습니다. 그래서 `fetch_data.py` 가 지수 대신
**SPY·QQQ 를, 그것도 배당 반영가(adjclose)로** 받도록 바꿨습니다. `fetch_long.py` 와
같은 기준이라 이음매에서 값이 튀지 않습니다.

이제 화면의 `mergeDaily()` 가 긴 이력 뒤에 매일 파일의 최근 며칠을 이어 붙입니다.
**결과: 주 1회 → 평일 매일.**

**가격선은 `ticker` 가 같을 때만 잇습니다.** 이 PR 이 머지된 직후부터 그날 밤
워크플로가 돌기 전까지는 저장소의 `market.json` 이 아직 옛 지수 기준입니다.
표식이 다르면 조용히 안 잇고 예전처럼 그립니다 — **이 가드를 지우면 그 몇 시간
동안 차트가 망가집니다.** VIX·공포탐욕은 두 파일이 늘 같은 출처라 표식 없이 잇습니다.

**그러니 `market.json` 의 종가 시계열을 지우지 마세요.** 예전 문서에 "아무도 안 읽으니
빼도 된다"고 적혀 있었는데, 지금은 **화면의 최신 며칠이 바로 거기서 옵니다.**

### market-long.json (2026-09-09 기준, 실측)

```
spx  SPY     1993-01-29 ~ 2026-09-08   2,747행
ndx  QQQ     1999-03-12 ~ 2026-09-08   2,428행
vix  ^VIX    1990-01-05 ~ 2026-09-09   2,910행
fng  CNN F&G 2011-01-07 ~ 2026-09-08   1,790행
                                파일 크기 204,952 bytes (200KB)
```

**최근 5년은 일봉, 그 이전은 주봉**입니다(`DAILY_YEARS = 5`, ISO 주의 마지막 거래일).
일봉을 다 담으면 601KB 인데, 20년 구간을 780px 폭에 그리면 1픽셀에 6.5일이 겹쳐
**화면에 드러나지 않습니다.** 파일만 세 배가 됩니다.

**이 파일로 통계를 내지 마세요.** 그림용이라 1990~2021 이 주봉입니다.
2026-09-13 에 이걸로 VIX 분위수를 재 봤더니 중앙값 17.54 가 나왔는데,
일봉 전체로 잰 참값은 17.59 입니다. 공포탐욕·VIX 가 엇갈리는 날 비율도
여기서는 11.0%, 일봉으로는 15.5% 로 **절반 가까이 어긋납니다.**
숫자가 필요하면 `backtest.json`(일봉으로 계산됨)이나 원본 CSV 를 쓰세요.

**지수(SPX/NDX)가 아니라 ETF(SPY/QQQ)를 담습니다.** 백테스트가 원래부터 SPY·QQQ
기준이었는데 화면의 낙폭만 지수 기준이라 기준이 갈려 있었습니다(옛 9-1 의 숙제).
`fetch_long.py` 를 만들면서 **그 문제는 해결됐습니다.** 배당 재투자 기준이라
낙폭이 지수보다 조금 더 크게 나오는데, 실제로 사는 것이 ETF 이므로 그쪽이 맞습니다.
각주에 `SPY·QQQ 배당 재투자 기준`을 적어 뒀습니다. 지우지 마세요.

### 실측으로 확인된 것 (추측 아님)

- **지수 종가**: Stooq→FRED→Yahoo 순서로 시도하는데 **러너에서는 Yahoo 만 성공**합니다.
  Stooq 는 자바스크립트 검증 페이지를 반환하고 FRED 는 타임아웃. 순서는 그대로 두세요.
- **공포탐욕 현재값**: CNN 직접 호출은 러너에서 418(봇 차단). **GitHub 미러가 실제 경로**입니다.
- **야후 일봉**: `range=max` 를 주면 **월봉으로 내려옵니다**. 일봉이 필요하면
  `period1`/`period2`(epoch 초)를 명시해야 합니다. `backtest.py` 와 `fetch_long.py` 가 그렇게 합니다.
- **CSV 줄바꿈**: 2011~2020 공포탐욕 CSV 는 CRLF 입니다. 헤더를 `split(',')` 한 뒤
  `.trim()` 하지 않으면 열을 못 찾습니다(파이썬 `csv.DictReader` 는 알아서 처리).
- **야후는 개발 환경에서 막힐 수 있습니다.** 2026-09-07 기준 클라우드 세션과 사용자 PC 셸
  양쪽 다 `query1.finance.yahoo.com` 접속이 차단됐습니다. GitHub Actions 러너에서는 됩니다.
  숫자를 직접 재야 하면 사용자 브라우저를 쓰세요(8번 참고).
- **한 소스가 막혀도 파일은 씁니다.** `fetch_long.py` 는 실패한 계열만 **옛 값을 그대로 두고**
  나머지를 갱신합니다. 전부 실패하면 아예 쓰지 않습니다. 반쪽짜리 파일이 올라가지 않게 하는 장치입니다.

### 백테스트 표본 (2026-09-07 기준)

| 잣대 | 출처 | 기간 | 겹치는 날 |
|---|---|---|---|
| 공포탐욕 | GitHub CSV 2개 병합 | 2011-01-03 ~ 2026-09-04 | 3,849일 |
| VIX (S&P 500) | Yahoo `^VIX` + SPY | 1993-01-29 ~ 2026-09-04 | 8,458일 |
| VIX (나스닥 100) | Yahoo `^VIX` + QQQ | 1999-03-10 ~ 2026-09-04 | 6,916일 |

공포탐욕 소스 두 개는 2020-09 ~ 2021-02 사이 약 4개월 공백이 있습니다(무해).
**두 소스 모두 현재까지 정상 갱신됩니다** — 한때 "하나가 6월에서 멈췄다"고 잘못 판단한
적이 있는데, 파일을 읽을 때 뒷부분이 잘려 보였던 것이었습니다.

### VIX 분위수 (1990~2026, 참고용)

```
10% 12.15   25% 13.99   50% 17.59   75% 22.70   90% 28.55
```

### 곡선 배열을 읽는 법 (한 번 틀렸습니다)

`backtest.json` 의 곡선 배열은 **길이 101 이고 첨자가 곧 그 잣대의 값**입니다.
`lo + i` 가 아닙니다. VIX 는 표시 범위가 10~45 여도 배열은 0~100 입니다.
이걸 착각해서 "두 잣대의 곡선이 정반대를 가리킨다"고 보고했다가 스스로 정정했습니다.
**실제로는 둘 다 U 자**입니다.

---

## 4. 설계 결정과 그 이유

**뒤집으려면 먼저 이유를 확인하세요.**

**새 화면(다른 주제의 그래프, 대가들의 선택 등)을 만들 때는 먼저
`docs/design-system.md` 를 읽으세요.** 이 4번은 *이 대시보드가 왜 지금 모습인가*의
기록이고, 그 문서는 *어느 화면에나 적용되는 재료표*입니다 — 색 토큰과 측정된 대비,
글꼴 세 벌의 역할, 선 굵기와 점선의 뜻, 여백 박자, 하지 말 것.
실제 CSS 에서 값을 뽑아 적은 것이라 추측이 없습니다.

### 합친 차트 (2026-09-09)

**패널 셋을 하나로 합친 이유.** 낙폭 차트 2개와 게이지 1개가 따로 놀아서
"공포가 높을 때 가격이 어땠나"를 화면에서 볼 수 없었습니다. 눈으로 겹쳐 보라는 셈이었습니다.

**왼쪽 축은 낙폭이 아니라 가격입니다.** 사용자가 고른 것입니다 —
"낙폭으로는 안 할 거야. 사용자들이 선호하지 않을 것 같아."
대신 **전고점 수위선과 그 아래 낙폭 빗금은 그림 안에 남겼습니다.**
"전고점 대비 보여주는 디자인 요소도 반드시 넣자"는 요청이었고, 이 사이트의 원래
디자인 언어이기도 합니다. 낙폭 %는 여전히 화면에 숫자로 나옵니다.

**선은 최대 2개, 가격선끼리는 배타입니다.** 원래 "최대 3개"로 시작했는데
SPY 와 QQQ 를 같이 켜니 **구분이 안 됐습니다**(사용자 지적). 지금은 가격선 하나 +
심리선 하나입니다. `MSHOW={price:"spx"|"ndx"|null, gauge:bool}` 이 그 규칙을 강제합니다 —
따로 검사하는 코드가 아니라 상태의 모양 자체로 막습니다.

**기간 버튼에서 10Y·20Y·전체를 뺐습니다.** 대신 **좌우 드래그**입니다.
버튼을 아홉 개로 늘리면 모바일에서 줄이 넘치는데, 사용자가 "2줄은 디자인이 너무
조잡해질 것 같다"고 못박았습니다. 지금은 **한 줄 + 옆으로 스크롤**이고,
더 과거는 끌어서 봅니다. 끌면 세로축 범위가 그 구간에 맞게 다시 잡힙니다.

**세로축 범위는 보이는 구간에서 다시 계산합니다.** 5년 버튼과 30일 버튼이 같은
축을 쓰면 30일 화면에서 선이 납작해집니다.

**색은 언어를 따라갑니다.** 한때 흑백으로 만들었다가 "흑백 테레비 같다"는
지적을 받고 되돌렸습니다. 6번 참고.

**선 끝에 현재값을 적습니다.** 처음엔 그림 안에 뒀는데 선 위에 겹쳤습니다.
지금은 **오른쪽 바깥**이고, 오른쪽 여백은 세 층입니다 — 현재값 / 축 숫자 / 구간 이름.
`labW`·`numW`·`nameW` 가 그 폭이고, `nameW` 는 **실제 이름 길이로 잽니다**
(영어 `EXTREME GREED` 가 한글 `극단적 탐욕` 보다 훨씬 깁니다).

**x축 눈금은 달력 경계에 놓습니다.** 예전에는 화면 폭을 균등하게 나눈 위치에
찍고 거기 걸린 연도를 적었습니다. 그래서 **`2025` 가 두 번 나왔고**, 끌면
연도가 안 따라오고 숫자만 바뀌었습니다. 사용자가 발견해서 원인까지 정확히 짚었습니다.
지금은 `tTicks()` 가 1월 1일 / 1일 / 날짜 경계에 찍습니다.

### 네 칸 계기판 (2026-09-13 복구)

`backup/v1-3panel` 의 `.readout` 을 합친 차트 밑으로 옮겼습니다. 다만 세 가지를
그대로 가져오면 지금 화면에서는 틀립니다.

**1. `현재 지수` → `현재값`.** 거기 찍히는 764.29 는 **S&P 500 지수(7,656)가 아니라
SPY 값**입니다. 7번의 툴팁 항목과 같은 병입니다 — 옛 판은 지수를 그리던 시절의
이름표였고, ETF 로 바꾼 뒤에는 거짓말이 됩니다.

**2. 낙폭 칸 바탕색을 `--down-rgb` 로.** 옛 CSS 는
`.cell-dd{background:rgba(31,95,169,.05)}` 로 **파랑을 박아** 뒀습니다.
6번의 색 규칙대로 영어 화면에서는 글자가 빨강으로 바뀌는데 바탕만 파랑으로 남아
있었습니다. 지금은 `rgba(var(--down-rgb),.05)` 라 같이 뒤집힙니다.

**3. 지금이 곧 전고점일 때.** 옛 코드는 `회복까지 +0.00%` 를 찍었습니다.
전고점이 **보이는 구간 안의 최고 종가**라서 끌다 보면 실제로 자주 걸립니다.
지금은 `0.00%` 에 `지금이 전고점` 을 달고 색과 바탕을 빼는 `cell-top` 으로 갑니다.

**가격선을 끄면 칸이 통째로 사라집니다.** 전고점은 가격선의 것이라 심리선만 켜면
쓸 값이 없습니다. `.readout:empty{border-top:0}` 이 위 경계선까지 지웁니다 —
7번의 "지운 블록의 CSS 가 남아 화면을 밀어냈다"와 같은 자리입니다.

**과거로 끌면 "현재값"이 아닙니다 (2026-09-13 검수에서 발견).**
끝까지 끌면 계기판이 `현재값 54.82 (2003.02.14)` 를 보여 줬습니다 — 2003년 값에
"현재"라는 이름표가 붙어 **숫자와 이름이 서로 어긋났습니다.** `회복까지 +36.87%` 도
이미 끝난 이야기를 아직 남은 일처럼 말했습니다. 지금은 `PAN>=1` 이면
`구간 끝값`/`Window end` 와 `전고점까지`/`back to the peak` 로 바뀝니다.
**계기판에 이름표를 더 붙일 때는 끌었을 때도 말이 되는지 보세요.**

**끌면 이 숫자들도 따라 움직입니다.** `renderMain()` 이 팬의 매 프레임마다 다시
그리므로 네 칸도 그 구간의 전고점으로 바뀝니다(실측: 1Y → 끌기 777.88/08-13 →
681.74/2025-10-29).

### 통계 (그대로)

**중앙값으로 그린다 (평균 아님).**
1년 보유·공포탐욕 0 근처의 평균은 +26.8% 인데 중앙값은 +11.3% 입니다. 2020년 3월
바닥에서 산 며칠이 +100% 씩 나오며 평균을 끌어올린 결과입니다. 평균만 그리면
"극단적 공포에서 사면 27% 번다"로 읽히는데 실제로 절반은 11% 아래였습니다.
평균은 버리지 않고 툴팁에 함께 넣었습니다.

**구간을 덩어리로 묶지 않고 눈금마다 창을 잡는다.**
'극단적 공포'(0~25) 한 칸에 지수 5(승률 98%)와 24(84%)가 같이 들어가 89% 로 뭉개졌습니다.
지금은 눈금 v 마다 v±window 인 날들을 모읍니다. 창은 공포탐욕 ±5, VIX ±1.5.
표본 30일 미만인 눈금은 비우고 화면에서 선을 끊습니다.
**구간(zones)은 색 띠와 이름표에만 쓰는 표시용 메타데이터**이고 통계 계산에는
관여하지 않습니다. 그래서 구간을 바꿔도 재계산이 필요 없습니다.

**0% 는 값이 실제로 마이너스일 때만 세로축에 넣는다.**
1년 수익률이 +12~+30% 인데 축을 -5~+35% 로 잡으니 칸의 절반이 빈 공간이 되어
선이 납작해 보였습니다. 사용자가 "그래프가 안 이쁘다"고 한 원인이 이것이었습니다.

**VIX 를 두 번째 잣대로 넣은 이유는 표본입니다.**
공포탐욕은 2011년부터라 2008년이 통째로 빠지고, 남은 기간이 거의 강세장이라
구간별 차이가 안 드러납니다.

```
1년 보유, S&P 500, VIX 23~28 구간
  1990~ 전체 : 승률 66%
  2011~ 만   : 승률 91%   ← 강세장만 본 착시
```

**VIX 구간 경계는 관행 숫자입니다 (2026-09-07 확정).**

```
12 미만  아주 잠잠   12~15 잠잠   15~20 보통
20~30    불안        30~40 공포   40 이상 패닉
```

이전에는 백분위 기준(13/17/22/30/40)이었고, 사용자가 "남들이 쓰는 숫자와 맞추는 쪽"을
골랐습니다. 절충으로 **설명문에 백분위 사실을 한 줄 남겼습니다** — "1990년 이후 절반이
17.6 아래였으니 20은 평균보다 조금 높은 정도입니다." 지우지 마세요.

**VIX 에는 공식 구간 분류가 없습니다.** CBOE 는 정의와 계산식만 공개합니다.
검색하면 "공식 구간"이라며 표가 나오는데 출처가 **TradingView 개인 스크립트**입니다.
인용하지 마세요.

**공포탐욕 잣대의 3Y·5Y 는 그대로 둡니다 (2026-09-07 확정).**
승률 칸이 100% 직선이 되는 현상은 남아 있습니다(2011년부터라 5년을 채운 진입일이
전부 상승장 시작점). 사용자가 "빼지 말고 두라"고 했습니다. 위 수익률 칸은 움직이므로
화면이 비지는 않습니다.

**성적표는 계기판처럼 읽힙니다 (2026-09-13).**
사용자가 "33이라는 숫자가 너무 강조되지 않아", "숫자 부분이 눈에 확 들어오지 않아"라고
했고, 재 보니 맞았습니다 — **캡션과 값이 전부 13px 한 덩어리**였고 숫자는 굵기(500)와
색만 달랐습니다. **고정폭 글꼴에서는 굵기 차이가 거의 안 보입니다.**

고친 방향은 둘입니다.
1. **작은 캡션 위, 큰 값 아래.** 현재값 38px, 성적 23px, 캡션 10.5px.
2. **줄글을 표로.** "수익날 확률 80% · 중위 수익 +13.56%"이 줄마다 되풀이되면서
   라벨이 숫자보다 자리를 더 차지했습니다. 지수 2개 × 잣대 2개는 원래 표이므로
   **열 머리에 한 번만** 씁니다.

그 과정에서 문구 두 개를 고쳤습니다. `buyHere` 앞의 `—` 는 줄 끝에 이어 붙일 때 쓰던
것이라 제 줄을 갖게 되면서 뗐고, 영어 `buy here, hold 1 year` 는 **혼자 서니 명령문이
되어** `bought here, held 1 year` 로 바꿨습니다(6번의 "사라는 쪽으로 새기 쉽다"가
레이아웃을 바꿀 때도 똑같이 일어납니다).

**여백은 글자 상자가 아니라 잉크로 재세요 (2026-09-13).**
구분선을 뺐는데도 사용자가 "전부 위로 좀 당겨줘"라고 다시 말했습니다. 재 보니
`33` 상자 아래와 표 머리글 상자 위 사이는 **6.4px** 인데, 눈에 보이는 흰 자리는
**14px** 이었습니다. 차이는 38px 글자의 줄 상자입니다 — `33` 에는 내려긋는 획이
없어서 글자 아래로 8px 쯤이 빈 채로 상자에 들어 있습니다.

**`getBoundingClientRect()` 만 보면 이 8px 이 안 보입니다.** 캔버스의
`actualBoundingBoxAscent/Descent` 로 잉크를 재야 나옵니다. 큰 글자 밑의 간격을
손볼 때는 상자가 아니라 이쪽을 보세요.

고친 것 셋, 합쳐서 성적표 높이 165.5px → 149.1px 입니다.
`.now{margin-bottom:-3px}`(상자 여백으로 그 8px 을 도로 먹음),
캡션과 열 머리글의 `line-height` 를 카드의 1.7 에서 1.35·1.25 로
(10.5px 글자가 18px 상자를 쓰고 있었습니다), 그만큼 `.lead` 의 위 여백도 25 → 20px.
**셋은 같이 움직입니다** — 머리글 상자를 줄이면 `.lead` 도 같은 만큼 줄여야
조건 문장이 표 첫 줄에서 벗어나지 않습니다(실측 0.5px 차이).

**성적표 안에는 가로줄을 긋지 않습니다 (2026-09-13).**
`33 공포` 아래에 구분선이 하나 있었는데 뺐습니다. 카드가 이미 흰 칸으로 떠 있어서
안에 줄을 하나 더 그으면 **한 장이 두 장처럼 읽히고**, 그 줄의 위아래 여백까지
24px 이 통째로 표를 아래로 밀어냅니다. 사용자가 "분리막을 없애고 위로 올리자"고 했습니다.

**숫자 칸은 오른쪽 맞춤입니다 (2026-09-13).**
왼쪽 맞춤이면 글자 폭이 제각각이라 오른쪽이 들쭉날쭉합니다 — 실측으로
`수익날 확률` 56px 인데 `80%` 는 41px, `중위 수익률` 56px 인데 `+13.56%` 는 97px
이라 **숫자가 제 머리글 밖으로 41px 나가 있었습니다.** 사용자가 "세로 줄이 잘 안맞아"라고
한 것이 이것입니다. 오른쪽으로 맞추면 자릿수가 같은 값끼리 소수점까지 줄이 섭니다.
첫 칸(지수 이름)만 왼쪽 맞춤입니다 — 이름은 숫자가 아닙니다.

**조건 문장은 현재값이 아니라 표에 붙습니다 (2026-09-13).**
`여기서 사서 1년 뒤` 는 원래 현재값 줄 뒤에 붙어 있었는데, 사실 **아래 표가
성립하는 조건**입니다. 구분선 아래로 내려 표 왼쪽에 두었습니다. 표는 오른쪽 끝으로
보내 **아래 그래프의 오른쪽 끝과 맞췄고**, 그러면서 비어 있던 왼쪽을 이 문장이 채웁니다.
`.cond` 의 `padding-top:21px` 은 **열 머리글 높이**입니다 — 표 첫 줄과 눈높이를 맞추는
값이라, 위로 붙이면 저도 머리글처럼 읽힙니다. 세로로 쌓이는 좁은 화면에서는 0 으로 돌립니다.

**영어 열 머리글은 좁은 화면에서 줄을 바꿔야 합니다.** `CHANCE OF PROFIT` 이
한글 `수익날 확률`보다 훨씬 길어서, `white-space:nowrap` 인 채로 두면 320px 에서
51px 이 넘칩니다. 지수 이름(`Nasdaq 100`)은 반대로 한 줄로 묶어 둬야 합니다.

**위·아래 기간 버튼은 연동됩니다 (2026-09-13).**
`30D 90D 180D 1Y 3Y 5Y` 가 위 차트(=얼마나 거슬러 볼까)와 아래 성적표
(=사서 얼마나 들고 있을까)에 따로 있었습니다. **뜻은 다릅니다.** 그래도 나란히 놓인
똑같은 모양의 버튼 두 줄이 따로 노는 편이 더 혼란스러워서, 어느 쪽을 눌러도 둘 다 옮깁니다.

값을 하나로 합치지는 않았습니다 — 아래 성적표는 **데이터에 있는 보유기간만**
가질 수 있습니다(`BT.horizons`). 그래서 라벨↔일수 표(`H_DAYS`)로 서로를 맞추고,
아래에 없는 칸이면 `syncHold()` 가 거짓을 돌려주어 **위만 움직입니다.**

옛 저장값은 둘이 어긋나 있을 수 있어서 `loadPrefs()` 가 **보유기간 쪽으로 맞춥니다.**
두 버튼 줄이 서로 다른 칸을 눌러 놓은 채 첫 화면이 그려지면 안 됩니다.

**통계 이벤트는 누른 쪽만 남깁니다.** 연동으로 따라 움직인 쪽은 이벤트를 쏘지 않습니다 —
한 번의 조작이 `range` 와 `horizon` 두 건으로 세어지면 숫자가 두 배로 부풀어
"3Y·5Y 를 쓰는가"를 판단할 수 없게 됩니다.

**성적표에는 숫자만 둡니다 (2026-09-09).** 자동 판정 문장과 설명 문단을 넣었다가
전부 뺐습니다. 남긴 것은 `표본 N일 / 전체 M일`, `표본 기간 1993–2026`,
그리고 `과거 성과일 뿐 미래를 보장하지 않습니다.` 한 줄입니다.

**SPY 를 유지합니다 (VOO 아님).** VOO 는 2010년 상장이라 2008년이 통째로 빠집니다.
SPY 는 1993년입니다. 수수료 차이(0.0945% vs 0.03%)보다 **표본 17년이 큽니다.**

**디자인 언어는 "계측기 / 측량 도면"입니다.**
전고점은 수위선, 낙폭은 빗금, 화면 밖 전고점은 파단선. 여백이 설계의 일부입니다.
새 기능도 이 언어를 유지하세요.

**흰 카드 위 글자 대비는 재서 정했습니다 (2026-09-09).**
예전 `--ink-3` 은 `#8B99A6` 으로 **대비 2.91:1** 이었습니다. WCAG 큰 글자 최소치
3:1 에도 못 미치는데 12.5px 문단 12곳을 포함해 23곳에 쓰이고 있었습니다.
사용자가 "글자 색깔도 눈에 띄지가 않아"라고 지적한 것이 이것입니다.

```
--ink   #0F1A24  17.6:1
--ink-2 #475765   7.5:1
--ink-3 #68767F   4.7:1   ← WCAG AA(4.5) 통과
```

**그런데 이 값은 흰 카드 위에서만 잰 것이었습니다 (2026-09-14 고침).**
종이 바탕(`--paper #E9ECF0`) 위에서 `--ink-3` 은 **3.95:1 로 AA 미달**이었고,
거기 놓인 글이 넷 있었습니다 — `.eyebrow`(브랜드 눈썹줄), `.stamp`(갱신 시각),
`.support`(후원 한 줄), `.foot`(각주). 전부 카드 바깥입니다.

```
--ink-3   #68767F → #5F6B74      카드 4.68 → 5.47 · 종이 3.95 → 4.61
```

**같은 실수를 두 번 했습니다.** 그 전 값 `#8B99A6` 도 카드에서 2.91 이라
지적을 받고 고친 것인데, 고칠 때 **또 카드만 봤습니다.**
**색 토큰을 손볼 때는 렌더된 화면에서 요소마다 실제 바탕을 찾아 재세요** —
CSS 만 읽으면 어떤 글이 어떤 바탕 위에 앉는지 안 보입니다.
**반투명 바탕은 아래 색과 합성해야 합니다**(`rgba(196,54,44,.05)` 를 원색으로
읽으면 멀쩡한 칸이 1.02 로 나옵니다 — 검수 중에 실제로 헛짚었습니다).
고친 뒤 크로미움으로 `--ink-3` 을 쓰는 요소 34건을 전부 다시 재서 미달 0건입니다.

---

## 5. 방문자 통계 (Umami)

```
계정        dysan1000@gmail.com
요금제      Hobby ($0) — 100K 이벤트/월, 사이트 1개, 6개월 보관
리전        United States
Website ID  41e07a57-a87c-403b-bdf8-e12d911457b6
```

`index.html` 맨 위, `<style>` 바로 앞에 한 블록으로 격리돼 있습니다.
**화면·데이터 코드와 완전히 분리돼 있어 통째로 지워도 대시보드는 돕니다.**

```
window.STATS = { id, src }   ← 설정은 이 두 줄이 전부
loadStats()                  ← id 없으면 아무 것도 안 함, localhost 도 제외
window.track(name, data)     ← 통계가 없거나 차단돼도 조용히 넘어감
```

제공사를 바꾸려면 `loadStats` 와 `track` 안쪽만 교체합니다. 호출부는 그대로 둡니다.

**바로 위에 `window.LIVE`·`window.SUPPORT` 블록이 같은 요령으로 들어 있습니다**
(9-2, 9-4-6). 셋 다 **주소 한 줄이 설정의 전부이고, 비우면 통째로 잠깁니다.**
설정은 이 세 블록에 모아 두세요.

### 남기는 이벤트

| 이름 | 언제 | 데이터 | 알고 싶은 것 |
|---|---|---|---|
| `line` | 위 차트에서 선 켜고 끄기 | `{axis, line}` | SPY 만 보는가, QQQ 도 보는가 |
| `axis` | 공포탐욕↔VIX 전환 | `{axis}` | VIX 잣대를 실제로 쓰는가 |
| `horizon` | 보유기간 버튼 | `{h, axis}` | 1Y 만 보는가, 3Y·5Y 도 보는가 |
| `range` | 차트 기간 버튼 | `{slot, range}` | 어느 구간을 더 보는가 |
| `lang` | 언어 전환 | `{lang}` | 어느 언어 수요가 있는가 |
| `support` | 후원 한 줄을 누름 | `{lang}` | 후원 0건이 "안 좋아해서"인지 "못 봐서"인지 |

`horizon` 이 특히 쓸모 있습니다 — "공포탐욕 3Y·5Y 를 뺄까"를 추측 대신 숫자로 판단할 수 있습니다.
`line` 은 새로 생긴 것입니다(합친 차트). **드래그(팬)는 아직 재지 않습니다.**

### API 는 Hobby 에서 막혀 있습니다

`API access requires a Pro plan`. 그래서 텔레그램 **주간 방문자 보고**를 못 만들었습니다.
**워크플로 실패 알림과는 다른 이야기입니다** — 그쪽은 Umami 와 무관하게 이미 돕니다(6-2).
사용자는 텔레그램 봇(BotFather)을 이미 갖고 있고, 주 1회 월요일 아침 보고를 원합니다.
가능해지는 조건은 셋 중 하나입니다.

1. **셀프호스팅** — MIT 라이선스. Vercel + Neon 무료 조합. API 완전 개방, 보관 무제한.
   사용자는 Vercel·Supabase 를 이미 씁니다. **Supabase 무료는 조직당 프로젝트 2개**라
   이미 다 쓰고 있으면 3번째는 유료($25/월) — 그래서 **Neon(무료 프로젝트 100개)** 을 권했습니다.
2. Umami Pro $20/월 — 알림 하나에는 아깝습니다.
3. 공유 링크(Share URL) 우회 — 비공식 경로라 권하지 않습니다.

옮길 때 `index.html` 에서 고칠 건 `src` 한 줄과 `id` 한 줄뿐입니다.

---

## 6. 다국어 (2026-09-08, 저장소 반영 완료)

**대상은 한국이 아니라 글로벌입니다.** 방문자의 브라우저(=시스템) 언어를 따라갑니다.

`index.html` 맨 위 `<!-- ══ 다국어 -->` 블록 하나에 다 들어 있습니다.
**언어를 추가하려면 사전 `D` 에 항목을 통째로 복사해 넣으면 됩니다.**
화면 코드는 `tx("키")` 만 부르므로 블록 밖은 건드릴 필요가 없습니다. 탭도 저절로 늘어납니다.

### 언어를 고르는 순서

```
1) 주소의 ?lang=en       링크 공유 시 그 언어로 열림. SEO 에도 이 구조를 씁니다
2) 지난번 선택            localStorage "dt.lang"
3) navigator.languages   브라우저(=시스템) 언어
4) 모르는 언어면 영어
```

### 색 규칙 — 언어를 따라 뒤집힙니다

| | 상승·탐욕 | 하락·공포 |
|---|---|---|
| ko / ja / zh | 빨강 `#C4362C` | 파랑 `#1F5FA9` |
| 그 외 | 초록 `#1B7F4B` | 빨강 `#C4362C` |

**이걸 고정하면 정반대로 읽힙니다.** 미국 사용자에게 파란 하락선은 뜻이 통하지 않고,
빨간 상승선은 폭락으로 보입니다. `REDUP` 집합과 `PAL` 에 정의돼 있고,
공포탐욕 색 띠와 VIX 띠(`tint`, `tone()`)도 함께 뒤집힙니다.

### 데이터 파일에는 한국어를 넣지 않습니다

```
backtest.json  { "key": "vix.verycalm", "label": "아주 잠잠" }
                 ↑ 사전이 이걸로 찾음      ↑ 사전에 없을 때만 쓰는 폴백
```

`backtest.py` 의 `AXES` 가 이 key 를 만듭니다. **파이썬은 언어를 몰라도 됩니다.**
지수 이름도 `market.json` 대신 사전(`tx("idx")[slot]`)에서 가져옵니다.

### 영어 검수 (2026-09-13) — ⓘ 툴팁·문단 8개 완료

사용자가 영어를 못 읽으므로 **제미나이와 ChatGPT 에 각각 돌렸습니다.**
프롬프트에 한국어 원문과 영어를 나란히 넣고, 사이트 성격·독자·금지사항을 적었습니다.

**두 모델이 독립적으로 같은 네 곳을 짚었습니다.** 전부 같은 병이었습니다 —
**영어가 한국어보다 "사라"는 쪽으로 기울어 있었습니다.**

```
Buy one share of SPY …        명령문. 투자 권유로 읽힘
put money behind …            "돈을 넣는 가장 확실한 방법"
went on sale                  공포에 사면 기회라고 우리가 주장하는 꼴
```

한국어에는 없던 행동 유도입니다. **영어로 옮길 때 이쪽으로 새기 쉬우니
새 문구를 쓸 때마다 확인하세요.**

그 밖에 잡힌 것: `banking` 은 한국어 '금융'보다 좁습니다(은행만) → `finance`.
'성장성 추구 ETF'와 '주식 초보자분들이' 두 곳이 영어에서 통째로 빠져 있었습니다.

**검수 결과를 그대로 받지는 않았습니다.** 제미나이는 고치면서 `The` 를 빠뜨리고
(`CNN Fear & Greed Index combines`) 확인 안 되는 `most popular growth ETF` 를
넣었습니다. `plain dial` → `intuitive dial` 도 거절했습니다 — 直觀的 을 사전에서
찾아 끼운 번역투라, ChatGPT 도 "그대로 두라"고 했습니다.
**두 모델이 엇갈리면 근거를 보고 판단하세요. 둘 다 틀릴 때가 있습니다.**

### 영어 검수 2차 (2026-09-13) — `.about` 7개 절 완료

같은 방식으로 두 모델에 돌렸습니다. **이번에도 둘이 독립적으로 같은 곳을 짚었습니다.**

```
[3] Buying at a reading of 5 was up a year later 98%…   둘 다 "영어로 어색"
[5] that stretch's peak                                 둘 다 "구어적·모호"
[7] What you do with it, and what follows, is yours.    둘 다 "면책이 너무 약함"
```

**엇갈린 곳에서는 사이트의 원칙을 따랐습니다.** [1]·[3] 에서 제미나이는 `if you had
bought` / `If you bought, you had a profit` 처럼 **2인칭 매수 표현을 넣자**고 했고,
ChatGPT 는 반대로 **행위자를 지우자**(`A reading of 5 was followed by a positive
return`)고 했습니다. 뒤쪽을 골랐습니다 — 6번에 적힌 이 프로젝트의 실패 이력이
정확히 "영어가 사라는 쪽으로 기운다"이고, 화면이 재는 것도 사실 매수 시뮬레이션이
아니라 **그 수준 이후의 수익률**입니다.

**제미나이 안을 그대로 받았으면 검사가 깨졌습니다.** `the median +11.44%` 를
`the median was +11.44%` 로, `3 January 2011` 을 `2011-01-03` 으로 바꾸자고 했는데
둘 다 `check_about.py` 의 정규식을 빗나갑니다. 날짜 형식은 읽기도 더 나쁩니다.

**두 모델이 볼 수 없던 것을 하나 찾았습니다.** 글은 VIX 20~30 을 `uneasy` 라고
했는데 **화면 이름표는 `Anxious`** 입니다. 모델에게는 글만 줬으니 알 수 없습니다.
**글을 검수시킬 때는 화면에 실제로 찍히는 말과 맞는지 따로 보세요.**

그 밖에: `coloured` 는 영국식 철자 하나만 외톨이로 있어서 `colored` 로,
'전혀 다른 이야기'의 '전혀'가 영어에서 빠져 있어 `entirely` 로 되살렸습니다.

**문장을 고치면 `check_about.py` 의 정규식도 같이 고쳐야 합니다.** 이번에 실제로
멈췄고, 그게 맞는 동작입니다 — 조용히 넘어가면 검사가 통과한 것처럼 보입니다.

### 함정

- **번역 함수는 `t` 가 아니라 `tx` 입니다.** 코드 곳곳에서 `t` 를 지역 변수로 쓰고 있어
  이름이 겹칩니다(`T` 도 차트 여백으로 이미 쓰입니다).
- **`Intl.DateTimeFormat` 에서 `dateStyle`/`timeStyle` 은 `timeZoneName` 과 같이 못 씁니다**
  (TypeError). 항목을 하나씩 줘야 합니다.
- 시각 표시는 **보는 사람의 시간대**를 따릅니다. 예전에는 KST 고정이었습니다.
- **HTML 에 영어를 하드코딩하면 한국어 화면에 그대로 남습니다.** `eyebrow` 가 그랬습니다.
  초기 표시용 글자라도 `paintStatic()` 이 덮어쓰는지 확인하세요.

---

## 6-2. 검색엔진과 공유 (2026-09-13)

### 정적 머리글은 영어입니다 — 일부러 그렇습니다

**크롤러 대부분은 JS 를 안 돌립니다.** 그래서 HTML 에 적힌 글자가 곧 검색 결과에
나오는 글자입니다. 이 사이트의 기본 언어가 영어이고 `<html lang="en">` 이므로
`<title>`·`description`·`og:*` 의 정적 값도 **영어**로 둡니다.

한국어 방문자에게는 `applyLang()` → **`paintHead()`** 가 제목·설명·`og:*`·`canonical`
을 한국어로 갈아끼웁니다. 사전의 `docTitle`·`metaDesc` 가 그 값입니다.
**언어를 추가하면 이 둘만 채우면 머리글도 따라옵니다.**

### 길이는 재서 맞췄습니다

```
제목  57자   (구글은 60자쯤에서 자름)
설명 151자   (구글은 155자쯤에서 자름)
```

**찾는 말을 앞에 둡니다** — `S&P 500 returns by Fear & Greed and VIX — Daniel's timing`.
브랜드를 앞에 두면 정작 검색어가 잘려 나갑니다.

### canonical 은 주소에 `?lang=` 이 있을 때만 자기 자신을 가리킵니다

```
itpaidoff.com/            → canonical  itpaidoff.com/          (x-default 와 같음)
itpaidoff.com/?lang=ko    → canonical  itpaidoff.com/?lang=ko
```

`hreflang` 은 en / ko / x-default 세 줄입니다. 이렇게 두지 않으면 두 언어가
**같은 내용의 중복 페이지**로 잡힙니다.

### 공유 카드는 언어별로 두 장

`og.png`(영어) · `og-ko.png`(한국어). 정적 기본값은 영어이고, `?lang=ko` 로 열면
`paintHead()` 가 한국어판으로 바꿉니다. 크롤러 대부분은 정적 값만 읽으므로
**한국에서 공유할 때는 `?lang=ko` 가 붙은 주소를 쓰세요.**

두 이미지와 애플 아이콘은 **크로미움으로 구워서 만들었습니다**(8번 참고).
원본 HTML 은 남겨 두지 않았으니 고치려면 다시 만들어야 합니다.

### 아이콘과 공유 카드는 무채색입니다

**상승·하락 색은 언어에 따라 뒤집히는데**(6번) 아이콘과 공유 카드는 한 장뿐입니다.
올라가는 선을 파랗게 칠하면 한국어 화면에서는 하락으로 읽히고, 빨갛게 칠하면
영어권에서 폭락으로 읽힙니다. **그래서 둘 다 먹빛 한 가지로 그렸습니다.**
뒤집고 싶으면 언어마다 한 장씩 더 구워야 합니다.

파비콘은 16px 로 줄면 점선이 뭉개져 체크표시처럼 보입니다. 24px 부터는 수위선이
살아납니다 — 점선을 더 가늘게 만들지 마세요.

### 검색어는 "방법"이 아니라 "찾는 사람의 질문"에 맞춥니다 (2026-09-13)

사용자가 이 사이트를 만든 이유를 나중에야 말해 줬습니다 — **"현금 투입 시점(투자
타이밍)을 재려고, 특히 주린이한테 도움이 될 것 같아서."** 그런데 화면의 모든 문구는
*계산 방법*("공포탐욕·VIX 수준별 수익률")을 말하고 있었습니다.
**주린이는 그렇게 검색하지 않습니다.** `투자 타이밍`, `지금 사도 되나` 쪽으로 칩니다.

그래서 제목·설명·`h1` 을 전부 그쪽으로 옮겼습니다. 계산 방법은 뒤에 남깁니다.

```
전  S&P 500 returns by Fear & Greed and VIX — Daniel's timing
후  Market timing by Fear & Greed and VIX — S&P 500 history
전  공포탐욕지수·VIX로 본 S&P 500 수익률 — Daniel's timing
후  투자 타이밍 — 공포탐욕지수·VIX로 본 S&P 500 과거 기록
```

**그래도 "사라"고 쓰지는 않습니다.** 6번의 실패(영어가 한국어보다 사라는 쪽으로
기울었던 것)와 같은 함정입니다. `투자 타이밍` 은 화면이 다루는 주제이고,
답은 언제나 **과거 기록**입니다 — 그래서 제목에 `과거 기록`·`history` 를 같이 답니다.
`When to buy?` 같은 제목은 우리가 답을 준다는 뜻이 되므로 쓰지 않았습니다.

**길이는 글자 수가 아니라 픽셀로 잘립니다.** 한글 한 자가 영문 두 자쯤이라
한국어 제목은 **30자 남짓**이 한계입니다(CLAUDE.md 의 `57자`는 영어 기준).
영문 환산으로 제목 60 · 설명 155 안쪽에 맞췄습니다.

### 제목 태그 계단 (2026-09-13)

구글이 제일 무겁게 보는 `h1` 에 **브랜드 이름만** 있었습니다. 크롤러가 받는 것을
세어 보니 이랬습니다.

```
전   <h1> Daniel's timing        ← 검색어 0개
     <h2> (비어 있음 — JS 가 채움)
     <h3> × 14                   ← h1 다음이 바로 h3

후   <h1> Market timing by Fear & Greed and VIX
     <h2> S&P 500 (SPY) and CNN Fear & Greed     ← 정적 기본값
     <h2> How these numbers are worked out       ← .about 여닫이 제목
     <h3> × 14
```

- **브랜드는 눈썹줄(`.eyebrow`)로 내렸고, HTML 에 그대로 박았습니다.** 사전에
  넣지 않습니다 — 번역하지 않는 이름이라 6번의 "하드코딩하면 한국어 화면에 남는다"가
  해당되지 않습니다. 대신 `h1` 이 `tagline` 키로 언어를 따라갑니다.
- **`#main-name` 에 정적 기본값을 적었습니다.** `renderMain()` 이 곧 덮어쓰므로
  화면은 그대로인데, JS 를 안 돌리는 크롤러에게는 이것이 유일한 `h2` 입니다.
- **`.about` 의 `<summary>` 안을 `<h2>` 로 감쌌습니다.** 글자 모양은 `summary` 가
  쥐고 있으므로 `h2` 는 기본 크기·여백만 지웁니다(`font:inherit;margin:0`).

**`<meta name="keywords">` 는 넣지 마세요.** 구글이 2009년에 무시하기로 했습니다.
"검색어 작업"이 그걸 뜻한다면 할 일이 없고, 실제로 할 일은 위의 제목 태그입니다.

**어느 말이 얼마나 검색되는지는 이 환경에서 못 잽니다.** 도구가 없고
`itpaidoff.com` 접속도 막혀 있습니다(프록시 403). 색인이 되고 나면
**Search Console 의 `실적 → 검색어`** 가 실제로 뭘 치고 들어왔는지 공짜로 보여 줍니다.
추측하지 말고 그걸 보세요.

### 사이트맵 `lastmod` 는 봇이 찍습니다 (2026-09-13)

날짜를 박아 두면 곧 거짓말이 되고, **구글은 못 믿을 `lastmod` 를 아예 무시합니다.**
그래서 `update-data.yml` 이 **데이터가 실제로 바뀐 날에만** 오늘 날짜로 옮깁니다.
안 바뀐 날에도 찍으면 "매일 바뀐다"는 거짓 신호가 되므로, `git diff --staged --quiet`
가 거짓일 때(= 바뀐 게 있을 때) 안쪽에서만 `sed` 를 돌립니다.

### 본문 — HTML 에 직접 박았습니다 (2026-09-13)

구글이 이 사이트를 **"크롤링됨 - 현재 색인이 생성되지 않음"** 으로 판정했습니다.
막힌 것도 없고 잘 받아갔는데 **검색 결과에 넣을 만하지 않다**고 본 것입니다.
크롤러가 받던 본문을 세어 보니 이유가 분명했습니다.

```
전   304자   — 그중 "Loading…" 두 개. 나머지는 각주뿐
후 4,418자
```

`.about` 블록(`index.html` 의 `<section class="panel about">`)이 그 글입니다.

**이 글을 사전 `D` 로 옮기지 마세요.** 다른 글자는 전부 사전에 있으니 옮기고 싶어지는데,
사전에 넣는 순간 JS 가 그리게 되고 **크롤러가 받는 페이지는 다시 빈 페이지가 됩니다.**
이 파일에서 유일하게 HTML 에 직접 박혀 있어야 하는 글입니다.

**두 언어를 다 넣어 두고 CSS 로 한쪽만 보입니다.**

```css
html[lang="ko"]      .about [data-lang="en"]{display:none}
html:not([lang="ko"]) .about [data-lang="ko"]{display:none}
```

`applyLang()` 이 `<html lang>` 을 바꾸면 글도 따라 바뀝니다. JS 를 안 돌리는
크롤러(네이버 등)에게도 **한국어가 그대로 보이게** 하려는 구조입니다.
확인: 한국어로 열면 보이는 영어가 0자, 영어로 열면 한국어가 0자입니다.

**글에 넣은 숫자는 그날 `backtest.json` 으로 다시 계산한 값입니다.**
이 문서에 적힌 수치를 그대로 베끼지 마세요 — 데이터가 매주 갱신됩니다.
쓴 숫자: 평균 +26.78% vs 중앙값 +11.44%(공포탐욕 0 근처, 1년, 표본 44일),
승률 98%(지수 5) vs 80%(지수 24), 공포탐욕 3,854일 / VIX+SPY 8,462일.

### 실패는 텔레그램으로 알립니다 (2026-09-13)

**메일을 안 쓴 이유가 있습니다.** GitHub 의 실패 메일은 계정 알림 설정에 걸려 있고,
예약 워크플로는 **cron 줄을 마지막으로 고친 사람**에게만 갑니다. 설정을 건드리거나
누가 실행 시간을 바꾸면 조용히 끊깁니다. 사용자가 텔레그램을 원한 것도 있지만
**끊길 구석이 적다는 것이 더 큰 이유**입니다.

```
저장소 Settings → Secrets and variables → Actions
  TELEGRAM_TOKEN   BotFather 토큰
  TELEGRAM_CHAT    받을 chat id
```

**봇은 아무거나 됩니다.** 코드에 봇이 박혀 있지 않고, `TELEGRAM_TOKEN` 에 넣은
토큰의 봇이 말을 겁니다. 쓰던 봇과 섞기 싫으면 BotFather 에서 하나 더 만들어
그 토큰만 넣으면 됩니다.

**chat id 는 봇이 달라도 같은 번호입니다.** 1:1 대화의 chat id 는 곧 내 텔레그램
사용자 번호라서, 봇을 바꿔도 그대로 씁니다. 다만 **새 봇에게는 말을 한 번 걸어
둬야** 합니다 — 텔레그램은 봇이 먼저 말을 거는 것을 막습니다(안 하면 `chat not found`).

**알림 제목 앞에 저장소 이름을 붙입니다**(`[timing] …`). 휴대폰 알림 미리보기는
앞부분만 보여서, 봇 하나가 여러 곳에서 말하면 어느 것인지 알 수 없습니다.

**비밀값이 없으면 `notify.py` 는 조용히 넘어갑니다(종료코드 0).** 알림을 못 보내는
것이 워크플로를 실패시킬 이유는 아닙니다 — 이미 실패해서 불려 온 참입니다.
텔레그램이 죽어 있어도 마찬가지입니다.

**예약 워크플로 셋 다 붙였습니다.** 11번에 적힌 대로 이 프로젝트에서 진짜 위험한
것은 코드가 터지는 것이 아니라 **데이터가 조용히 끊기는 것**입니다(Stooq 차단,
CNN 418 — 둘 다 겪었습니다). 그 셋이 말없이 실패하면 화면만 며칠 낡습니다.

**`tee` 앞의 `set -o pipefail` 은 장식이 아닙니다.** 없으면 파이프 마지막 명령인
`tee` 의 종료코드가 잡혀서 **실패가 성공으로 보이고** `if: failure()` 가 아예
안 돕니다(실측으로 확인).

**시험 발송은 Actions → Check → Run workflow 를 누르면 저절로 같이 갑니다.**
처음에는 켜고 끄는 체크박스를 뒀는데, **안 켠 채로 돌려서 초록불인데 아무것도
안 오는 일이 실제로 있었습니다**(단계가 `skipped` 로 남습니다). 손으로 돌렸다는 건
알림 경로까지 보고 싶다는 뜻이므로 조건을 없앴습니다. PR 에서는 안 보냅니다 —
화면에 빨간 X 로 바로 보이고, 리뷰마다 울릴 이유가 없습니다.

**`--test` 는 못 보내면 실패로 끝냅니다(종료코드 1).** 실패 알림과 반대입니다.
시험 발송은 "되는지 확인해 달라"는 뜻이라, 조용히 넘어가면 **초록색인데 아무것도
안 온** 상태가 되어 설정이 된 건지 아닌지 알 수가 없습니다.

**표준입력은 `--body-stdin` 을 줄 때만 읽습니다.** 예전에는 `--body-file` 이 없으면
알아서 읽었는데, 표준입력이 **열린 파이프면 EOF 를 기다리며 영영 멈춥니다.**
러너에서는 보통 `/dev/null` 이라 지나갔지만, `update-data.yml`·`update-long.yml` 이
본문 없이 부르고 있어서 알림 한 줄 때문에 실행이 6시간 걸려 죽을 수 있는 자리였습니다
(시험 중에 실제로 멈춰서 찾았습니다).

**본문은 `<pre>` 로 보냅니다.** 대조표가 휴대폰에서 안 깨집니다. `<`·`&` 는
escape 하고, 4096자 한도에 맞춰 자릅니다. **토큰은 어디에도 찍지 않습니다.**

### 글의 숫자는 봇이 대조합니다 (2026-09-13)

글 속 숫자는 손으로 적혀 있는데 `backtest.json` 은 주 1회 다시 계산됩니다.
**아무도 확인하지 않으면 글의 숫자만 조용히 낡습니다.**
`scripts/check_about.py` 가 그것을 막습니다.

```
주 1회  update-backtest.yml  ← 새 숫자를 저장한 뒤에 대조. 어긋나면 실패 + 메일
PR 때   check.yml            ← index.html / backtest.json 을 건드린 PR 에서 미리
```

**데이터를 먼저 커밋하고 그 뒤에 봅니다.** 글이 낡았다고 데이터까지 안 올라가면
곤란합니다 — 글은 사람이 고쳐야 하고, 데이터는 그대로 맞습니다.

**두 층으로 봅니다.**

1. **숫자** — 적힌 값과 다시 계산한 값의 차이
2. **논지** — "평균이 중앙값보다 훨씬 크다", "같은 칸 안에서도 승률이 갈린다",
   "VIX 표본이 두 배 넘게 길다". 이 관계가 깨지면 **문단을 통째로 다시 써야 합니다.**

**매주 실패하면 아무도 안 봅니다.** 표본은 주마다 늘어나므로 자릿수는 늘 움직입니다.
그래서 `경고`(자릿수만 다름, 통과)와 `오류`(오해를 부를 만큼 벌어짐, 실패)를 나눴습니다.
일수는 1~2%, 평균·중앙값은 1%p, 승률은 3%p 까지 봐 줍니다.

**한국어와 영어가 서로 다른 숫자를 말하면 그것도 오류입니다** — 한쪽만 고치는 실수가
제일 잦습니다.

**글을 고쳐 쓰면 정규식도 같이 고치세요.** 문장이 바뀌어 숫자를 못 찾으면
조용히 넘어가지 않고 **그 자리에서 멈춥니다** — 건너뛰면 검사가 통과한 것처럼 보입니다.

**면책 문구는 본문과 같은 크기·글꼴로 둡니다.** 처음에 고정폭 작은 글씨로 뒀더니
한글 자간이 벌어지고 색도 흐려서 **제일 중요한 문장이 제일 안 읽혔습니다.**

---

## 7. 코드에서 조심할 곳

- **`.seg.mini` 클래스가 네 군데에서 쓰입니다** — 차트 기간, 보유기간, 잣대 전환, 언어.
  전역 클릭 핸들러가 서로의 버튼을 잡아 오류가 난 적이 있습니다.
  `if(!slot||!(slot in state.range))return;` 가드가 그 방지책입니다. 지우지 마세요.
- **선 그리기 애니메이션은 `getTotalLength()` 로 실제 길이를 재야 합니다.**
  `calibrateDraw()` 가 그 일을 합니다. **이 함정을 2026-09-09 에 실제로 밟았습니다** —
  차트를 좌우로 끌 때 매 프레임 다시 그리면서 길이를 다시 재지 않아
  **공포탐욕·S&P500·나스닥 선이 중간에 끊겨 보였습니다**(사용자가 버그로 신고).
  지금은 팬의 `requestAnimationFrame` 안에서 `renderMain()` 다음에 `calibrateDraw()` 를
  같이 부르고, `settled` 이후에는 `draw` 클래스를 아예 붙이지 않습니다.
  **새 차트를 추가하면 반드시 `calibrateDraw()` 를 부르세요.**
- **`.svg-axis{fill:...}` 같은 CSS 가 SVG 의 `fill=` 속성을 이깁니다.**
  낙폭 `%` 를 상승/하락 색으로 칠했는데 회색으로 나왔던 원인입니다.
  SVG 안에서 색을 개별로 주려면 **속성이 아니라 `style="fill:…"`** 로 주세요.
- **버튼은 한 번만 만들고 이후엔 `aria-pressed` 만 옮깁니다.** 매번 innerHTML 로
  다시 그리면 방금 누른 버튼이 사라져 키보드 포커스를 잃습니다.
  (예외: 언어를 바꾸면 글자가 바뀌므로 비우고 다시 그립니다.)
- **ⓘ 툴팁의 `focusin` 과 `click` 이 터치에서 경합합니다.** 탭하면 포커스로 열렸다가
  곧바로 클릭 핸들러가 닫아 버립니다. 지금은 **hover 는 마우스일 때만**(`pointerType`),
  클릭은 `pointerdown` 시점에 잡아 둔 상태로 판단, 키보드는 `e.detail===0` 으로 구분합니다.
- **SVG 뷰박스는 창 폭이 아니라 패널 폭에 맞춥니다**(`geo()`).
  좌우 여백은 **하드코딩하지 말고 `getComputedStyle` 로 실제 padding 을 재세요.**
  카드 padding 을 바꿨을 때 차트만 어긋납니다.
- **위 차트 툴팁의 가격에는 티커를 병기합니다.** 거기 찍히는 `764.29` 는
  **S&P 500 지수(7,656)가 아니라 SPY 값**입니다. `S&P500` 이라고만 쓰면 지수 값으로
  읽힙니다. 그래서 가격선(`side==="L"`)은 `mainFull()`(`S&P500(SPY)`), 심리선은
  `mainName()`(`공포탐욕`)을 씁니다 — 심리선은 숫자가 곧 그 지수라 티커가 필요 없습니다.
  사용자가 발견한 지점입니다.
- **십자선 점은 여러 개일 수 있습니다.** `attachHover` 에 `meta.pts` 를 주면
  SVG 안의 `.cdot` 개수만큼 함께 움직입니다.
- **구간 이름표는 띠가 좁으면 자동으로 생략됩니다.** 문턱은 고정 px 가 아니라
  `MINH = max(16, 차트높이 * 0.075)` 입니다. 차트를 1.25배 키웠더니 고정 14px 문턱을
  VIX 의 '아주 잠잠' 띠가 넘어서 이름이 나타나고 다른 이름들의 간격이 망가졌습니다.
  **차트 높이를 바꾸면 이 값도 같이 움직여야 합니다.**
  이름의 세로 위치는 띠 안으로 **위 10px / 아래 6px** 를 남기고 자릅니다
  (글자 상자가 기준선보다 위아래로 더 큽니다).
- **지운 패널의 CSS 가 남아 화면을 밀어낸 적이 있습니다.** `.now` 는 원래 낙폭 패널의
  숫자 표시(값 + 등락률)를 겸해서 `display:flex; white-space:nowrap` 이었습니다.
  3단계에서 그 패널을 지웠는데 규칙만 남았고, 성적표 첫 줄이 **문장인데도 한 줄로
  붙들려** 320px 화면에서 58px(영어는 75px)을 밀어냈습니다.
  **블록을 지울 때는 그 블록만 쓰던 CSS 규칙도 같이 보세요.** 8번의 '선언 식별자 비교'는
  JS 만 잡아 주고 CSS 는 안 잡아 줍니다.
  같은 자리에서 지수 이름 칸(`.idx`)의 고정폭도 고쳤습니다 — 영어 `Nasdaq 100` 이
  좁은 화면에서 두 줄로 쪼개졌습니다.
- **선 점의 값이 숫자인지 `cut()` 에서 거릅니다.** 마지막 점 하나만 `null` 이어도
  선 끝의 현재값(`fmt`)에서 `toLocaleString` 이 터져 **차트가 통째로 사라집니다.**
  중간의 `null` 은 무해한데 끝만 치명적이라 눈에 안 띕니다. 지금은 파이썬이 `null`
  을 빼고 쓰고 `mergeDaily`·`applyLive` 도 `typeof==="number"` 로 막으므로 들어올
  길이 없지만, 막는 곳이 네 군데라 하나라도 새면 화면이 빕니다.
- **`.info` 의 초점 표시는 hover 와 달라야 합니다.** 예전에는
  `.info:hover,.info:focus-visible{…outline:none}` 한 줄로 묶여 있어서
  **키보드 초점이 마우스 hover 와 똑같아 보였습니다.** 다른 버튼은 전부
  `outline:2px solid var(--ink)` 를 두르는데 여기만 1px 색 변화뿐이었습니다.
  (대비 자체는 7.45:1 로 기준을 넘었지만, 구별이 안 되는 것이 문제였습니다.)
- **`h2.name` 과 `.name h2` 는 다릅니다.** 선택자를 잘못 써서 제목이 브라우저 기본
  32px 로 나와 휴대폰에서 넘쳤던 적이 있습니다.
- 애니메이션은 최초 1회만. 1.6초 뒤 `body.settled` 가 붙습니다.
- **`localStorage` 를 써도 됩니다.** 언어(`dt.lang`)와 차트 설정(`dt.chart`)에 씁니다.
  다만 실패해도 화면이 도는 형태로만 쓰세요(try/catch).
  `loadPrefs()` 는 **옛 형식도 받아들입니다** — 예전에 저장된 `{spx:true,ndx:true}` 를
  새 형식 `{price:"spx"}` 로 옮겨 줍니다. 이 변환을 지우면 재방문자 화면이 빕니다.

---

## 8. 검증 방법

이 프로젝트에서 실제로 버그를 잡아낸 방법들입니다. **화면을 고쳤으면 반드시 돌리세요.**

- **jsdom 으로 렌더링 경로를 통과시킵니다.** 정상 경로뿐 아니라 **실패 경로**
  (데이터 없음 / 옛 형식 / 한쪽 지수만 있음 / 현재값 없음 / `market-long.json` 만 실패)도
  함께 돌리세요.

  요령:
  - `runScripts:'dangerously'` + **`beforeParse` 에서 `fetch` 를 갈아끼웁니다**
    (파싱 시점에 이미 호출되므로 나중에 넣으면 늦습니다).
  - `SVGElement.prototype.getTotalLength` 를 스텁으로 채웁니다.
  - `navigator.languages` 는 `Object.defineProperty` 로 덮습니다.
  - 검사할 때 **`<script>`·`<style>` 을 제거한 뒤** `textContent` 를 봅니다.
  - 언어 검사에서 **언어 전환 버튼은 제외**하세요.
- **Playwright 로 실제 픽셀을 잽니다.** 겹침·색·대비처럼 눈으로 봐야 하는 것은 이쪽입니다.
  이 환경에는 크로미움이 이미 깔려 있습니다. **`playwright install` 을 돌리지 마세요** —
  버전이 어긋나므로 실행 파일을 직접 지정합니다.

  ```js
  chromium.launch({ executablePath:
    '/opt/pw-browsers/chromium-1194/chrome-linux/chrome' })
  ```

  **십자선·호버를 테스트할 때는 먼저 `scrollIntoViewIfNeeded()`** 를 부르세요.
  차트가 화면 아래에 있으면 `mouse.move` 가 빗나가서 **멀쩡한 코드가 실패로 나옵니다.**

  **그림을 구울 때도 같은 크로미움을 씁니다.** 공유 카드(1200×630)와 애플 아이콘은
  HTML 을 만들어 `screenshot()` 으로 떴습니다. 이 환경에 PIL 이 없어서 다른 방법이
  없기도 합니다. 로컬 SVG 를 `<img src="file://…">` 로 불러오면 **깨진 그림으로 나오니**
  SVG 마크업을 페이지에 직접 넣으세요.
- **파이썬 집계는 인공 데이터로 검증합니다.** 매일 오르는 가격 → 전 구간 승률 100%,
  매일 내리는 가격 → 0%, 창 크기별 표본 수가 기대와 일치하는지.
- **구간을 바꿨으면 연속성을 확인합니다.** 앞 구간 hi == 뒤 구간 lo, 빈틈·중복 없음,
  경계값이 정확히 한 구간에만 속하는지(`lo <= v < hi`).
- **`backtest.py` 를 고쳤으면 직렬화만 떼어내 돌려 보세요.**
- **큰 블록을 지울 때 문자열 검색으로 자르지 마세요.** `s.index("주석")` 이 의도한
  블록보다 앞의 같은 주석을 찾아서, 잘라 붙인 결과에 같은 구역이 두 번 들어간 적이 있습니다
  (`Identifier 'grid' has already been declared`). **줄 번호로 자르세요.**
  그리고 지운 뒤에는 **옛 파일과 새 파일의 선언 식별자를 비교**해서
  같이 딸려 나간 것이 없는지 보세요(`axName`, `STAMP_ISO` 가 그렇게 사라졌습니다).
- **숫자를 주장하기 전에 실제로 계산해 보세요.** 개발 환경에서 야후가 막히면,
  사용자 브라우저에서 야후 API 페이지를 열고 거기서 JS 로 CSV 를 `fetch` 하면
  (raw.githubusercontent 는 CORS 허용) 파이프라인 없이도 같은 계산을 재현할 수 있습니다.
- **가로 넘침은 `main` 과 나란히 재세요.** 그리고 **원인을 끝까지 좁히세요** —
  "넘치는 요소"를 세면 스크롤 컨테이너(`.ctl-row`) 안의 버튼들까지 딸려 나와
  범인이 여럿인 것처럼 보입니다. 조상 사슬의 `overflow-x` 를 보면
  그것들은 이미 갇혀 있고, 페이지를 실제로 밀어낸 것은 하나뿐이었습니다.

  ```js
  document.documentElement.scrollWidth - document.documentElement.clientWidth
  ```

  이 값이 0 이 아니면 넘치는 것입니다. 320 / 360 / 390px, 한국어·영어 둘 다 재세요
  (영어가 더 깁니다 — 같은 버그가 320px 에서 한국어 58px, 영어 75px 이었습니다).
- **CSS 를 파이썬으로 써 넣을 때 `\25B8` 은 삼각형이 아닙니다 (2026-09-22 사고).**
  파이썬의 보통 문자열에서 `\25` 는 **8진수 표기**라 `\25B8` 이
  `제어문자(0x15) + 글자 B8` 이 됩니다. 화면에 ▸ 대신 **`B8`** 이 찍혔고
  (펼치면 `BE`), 사용자가 발견했습니다. 원래 있던 `더 보기` 의 같은 줄은
  멀쩡했으므로 **제가 새로 넣은 자리만 망가진 것**이었습니다.

  ```
  python3 -c "print(repr('\25B8'))"   →  '\x15B8'      ← 이게 원인
  ```

  고치는 길은 **원시 문자열**(`r"""…"""`)이나 `\\25B8` 입니다. 그리고 쓴 뒤에는
  **제어문자가 들어갔는지 파일을 훑으세요** — 눈으로는 안 보입니다.

  ```
  grep -nP '[\x00-\x08\x0b-\x1f]' 파일 | cat -v
  ```

  검사도 한 건 넣었는데, **그 검사가 진짜로 잡는지 일부러 되돌려 확인했습니다**
  (버그를 되살리면 26건 중 1건 실패, 고치면 26건 통과). 8번의 *"`hidden` 속성은
  붙어 있어서 검사가 통과했다"* 와 같은 자리입니다.
- 사용자에게 넘기기 전 **저장소를 실제로 읽어** 반영됐는지 봅니다.

---

## 9. 다음 후보

### 9-1. 야간 모드 — 안 하기로 했습니다 (2026-09-14)

**사용자가 "패스"라고 했습니다. 다시 제안하지 마세요.**

`prefers-color-scheme` 이 `index.html` 에 **한 번도 안 나옵니다.** 밤에 열면 흰 바탕
그대로입니다. 나중에 마음이 바뀌면 어두운 팔레트 한 벌 + **차트 색 띠(`tint`)·빗금·
투명도를 어두운 바탕에서 다시 확인**하는 일입니다. 지금 색 띠는 흰 바탕 기준으로 옅게
깔아 둔 것이라 어두운 바탕에서는 안 보이거나 탁해집니다.

### 9-2. 실시간 중계소 — 켰습니다 (2026-09-13)

```
주소   https://yellow-salad-ec67.dysan1000.workers.dev/
코드   workers/live.js  (클라우드플레어 대시보드에 붙여넣은 것과 같은 내용)
설정   index.html 의 window.LIVE.url 한 줄
```

**CNN 이 클라우드플레어는 막지 않았습니다.** 이게 미지수였습니다 — GitHub 러너에서는
418(봇 차단)이었는데, 중계소로는 넷이 전부 돌아왔습니다(`failed` 없음).

```
spx 764.29  ndx 714.88  vix 15.84  fng 33      (2026-09-13 02:34, 장 닫힘)
```

**아직 확인 못 한 것 하나.** 위 응답은 일요일에 받은 것이라 공포탐욕 시각이
`2026-09-11T23:59:53Z` — 금요일 마감 스냅샷입니다. **CNN 이 응답한다는 것은 확인됐지만,
장중에 값이 실제로 움직이는지는 미국장이 열렸을 때 다시 봐야 압니다.**
(SPY·QQQ·VIX 는 예전 워크플로에서 장중 값이 나오던 것을 140회 확인했으므로 확실합니다.)

**장이 닫혀 있으면 '실시간'이라고 쓰지 않습니다.** 금요일 종가를 일요일에 실시간이라
부르면 거짓말이므로, `LIVE.open` 이 참일 때만 시각 표시가 `실시간` 으로 바뀝니다.

중계소가 죽거나 느려도 화면은 그대로 돕니다 — 종가만으로 이미 맞기 때문에
오류를 띄우지 않고 조용히 넘어갑니다. 폴링은 장중 1분, 닫혀 있으면 10분 간격이고
탭을 보고 있을 때만 돕니다(무료 한도 하루 10만 요청).

### 9-3. 대가들의 선택 — 사용자가 생각하는 진짜 메인 제품

**이 대시보드는 곁가지고, 메인은 이쪽입니다.** 대가들이 무엇을 샀고,
**그 선택이 지금 어떤 결과를 낳았는지**를 보여 주는 것.
한국어로는 이미 서비스하는 앱이 있습니다(사용자 것이 아님).
**차별점은 "지금 뭘 들고 있나"가 아니라 "그래서 어떻게 됐나"입니다.**

데이터는 **13F 공시(SEC)** 입니다. 미국 정부 저작물이라 **퍼블릭 도메인** —
11번의 라이선스 문제가 여기서는 통째로 빠집니다. 이 프로젝트에서 가장 큰 이점입니다.
단, 45일 지연 공시이고 미국 주식 롱 포지션만 보인다는 한계는 화면에 밝혀야 합니다.

**작업은 `docs/masters-13f.md` 에서 이어집니다.** 다른 창에서 이어받을 수 있게
정리해 둔 문서입니다 — 후보를 거르는 시험 네 개, 넣을 여덟 명과 뺄 사람(달리오·
르네상스·시타델·ARK)과 그 이유, 버크셔부터 하는 이유, 막힌 곳, 데이터 크기.
**어시스턴트용이라 영어로 촘촘하게 적혀 있습니다**(사용자가 읽을 필요 없음).

**지금까지 된 것 (2026-09-14).** 첫 번째는 **버크셔 한 명**으로 정했습니다 —
유명해서가 아니라 **연차보고서에 보유 종목이 나와 우리 계산을 대조할 수 있는
유일한 곳**이기 때문입니다. 그리고 `scripts/probe_sec.py` +
`.github/workflows/probe-sec.yml` 을 만들었습니다. **아직 한 번도 안 돌렸습니다.**

**SEC 는 개발 환경에서 막혀 있습니다**(오늘 재 봤고 `www.sec.gov`·`data.sec.gov`
둘 다 프록시 403 — 야후와 같습니다). 러너에서는 됩니다. 그래서 **자료가 어떻게
생겼는지 짐작해서 파서를 짜지 않고**, 러너에서 한 번 받아 원본을 아티팩트로
올려 두는 정찰을 먼저 만들었습니다. 해석은 하지 않습니다 — 상태 코드·크기·
태그 개수를 세고 원본을 남길 뿐입니다.

**막혀 있는 것은 하나뿐입니다: `SEC_CONTACT` 비밀값.** SEC 는 이름 없는 요청을
거절하고 User-Agent 에 연락처 메일 주소를 요구합니다. **이 저장소는 공개**라
코드에 박으면 긁힙니다. 그래서 텔레그램 토큰과 같은 요령으로 비밀값입니다.
**어느 주소를 쓸지는 물어서 정했습니다 (2026-09-14): 쓰시던 메일 그대로.**
그 주소가 SEC 라는 바깥 서비스로 실제로 나가므로 임의로 정할 일이 아니었습니다.
**다만 저장소에는 어디에도 적지 않았습니다** — 문서에 적는 것도 코드에 박는 것과
똑같이 긁힙니다. 비밀값 칸에만 들어갑니다.

비밀값이 없으면 정찰은 **종료코드 1** 로 끝냅니다. `notify.py --test` 와 같은
이유입니다(6-2) — 손으로 돌린 것은 "되는지 확인해 달라"는 뜻이라, 조용히
넘어가면 초록불인데 아무것도 안 받아온 상태가 됩니다.

**화면 첫 장을 만들었습니다 (2026-09-14).** `titans/index.html` — **지금 들고 있는 것**
한 장입니다. 사용자가 고른 모양입니다: 종목을 나열하되 **비중을 늘리고 줄인 흔적**이
보이고, **신규 진입 / 전량 매도 / 몇 년째 보유**가 드러나고, 숫자에 **해석 한 줄**이 붙습니다.

정한 것 넷입니다.

1. **판정은 비중이 아니라 주식수로 합니다.** 비중에는 가격이 섞여 있습니다 —
   애플은 이번 분기에 한 주도 안 건드렸는데 비중이 +0.1%p 움직였고, 셰브런은
   가만히 있는데 -2.0%p 빠졌습니다. "비중을 줄였다"고 쓰면 거짓말이 되는 자리입니다.
   막대는 비중으로 그리되 `늘림/줄임/그대로` 는 주식수 차이로만 판정합니다.
2. **같은 발행사의 여러 종류 주식을 한 줄로 묶습니다.** CUSIP 아홉 자리 중 앞
   여섯 자리가 발행사입니다. 알파벳 A(02079K305)와 C(02079K107)를 따로 두면
   **한 번의 결정이 두 줄로 쪼개져 오히려 안 보입니다** — 합치면 4위가 아니라 3위(12.6%)입니다.
   묶은 줄에는 `A+C` 꼬리표를 답니다(합친 사실을 밝히지 않으면 공시와 대조할 수 없습니다).
   **우선주는 묶지 않습니다** — 표만 없는 보통주(알파벳 C)와 달리 배당·청산 순위가
   다른 별개의 증권입니다. 28년치에 딱 하나 있습니다(Sealed Air `PFD CVA`, 2002~2003).
3. **상위 10개만 펴고 나머지는 접습니다.** 사용자가 정했습니다. 접히는 16줄은 다 합쳐
   9.0%입니다. **다만 사건은 접히지 않습니다** — 이번 분기 신규 진입이 하필 26위
   (D R Horton, $58만)라 크기로 접으면 사라집니다. 신규·재진입·전량매도는 크기가
   아니라 사건이므로 요약 칸 바로 밑에 따로 둡니다.
4. **해석 문장은 행동만 적습니다.** "알파벳을 83% 늘렸습니다"는 사실이고,
   "알파벳을 좋게 본다"는 추측입니다. **13F 에는 "왜"가 없습니다** — 무엇을,
   얼마나만 있습니다. 13번의 "자동 판정 문장 금지"는 *판단*("평균보다 낫다")을
   막는 것이고, 셈을 문장으로 옮기는 것은 거기 해당하지 않습니다.

**연속 보유 기간은 끊긴 것을 건너뛰지 않습니다.** D R Horton 은 2022~2025 에
3.2년 들고 있다가 팔았고 이번에 다시 샀습니다. 그걸 이어 붙여 "4년 보유"라고
쓰면 중간에 판 사실이 사라집니다. 지금은 **다시 들어온 시점부터** 세고 `재진입`
꼬리표를 답니다.

**추정 매수 평균가를 넣었습니다 (2026-09-14, 두 번째 PR).** 사용자 요청입니다 —
"추정 매수 단가 평균 → 현재가 (±%)". 13F 에 체결가는 없지만 **분기말 금액 ÷ 주식수**가
그 분기말 가격이므로, 분기마다 늘어난 주식수를 *직전 분기말과 이번 분기말의 중간값*으로
사들였다고 보고 이동평균으로 잇습니다. **외부 데이터가 한 톨도 필요 없습니다.**

**액면분할을 안 잡으면 이 화면은 거짓말을 합니다.** 실측으로 확인했습니다.

```
애플 · 분할 무시   추정 평균 $213.33  →  +35.6%    ← 틀림
애플 · 분할 반영   추정 평균  $39.59  →  +630.9%   ← 맞음
```

2020년 8월 4:1 분할 때 주식수가 245M → 944M 으로 뜁니다. 그걸 매수로 세면
평균단가가 통째로 망가집니다. 분할은 **주식수가 f 배로 뛰면서 가격이 1/f 로
내려간 분기**로 찾는데, **정수 배수만 인정합니다.** 1.5 배를 후보에 넣었더니
'55% 더 산 분기'(애플 2016-06)와 '크게 늘린 분기'(레너드 2026-03)가 분할로
잡혔습니다. 정수만 두면 현재 26묶음에서 다섯 건이 정확히 잡히고 오탐이 0입니다 —
애플 ×4(2020-09) · 아멕스 ×3(2000-06) · 코카콜라 ×2(2012-09) · 무디스 ×2(2005-06) ·
다비타 ×2(2013-09). **가격 쪽 허용 오차를 넉넉히(±30%) 둔 이유**는 분기 중에
시세가 움직이기 때문입니다 — 애플은 분할과 같은 분기에 주가가 27% 올랐습니다.

**못 쓰는 자리가 둘 있고, 둘 다 말로 밝힙니다.** 첫 공시(1998-12-31)에 이미 들어
있던 종목은 사들이는 장면이 안 보여서 `1998년 이전부터 보유` 로 적습니다 —
코카콜라와 아메리칸익스프레스가 여기 해당하고 **둘이 합쳐 28%** 입니다. 이번 분기에
처음 들어온 종목은 매수가와 기준가가 같은 값이라 `이번 분기 진입` 으로 적습니다.

**아직 대조하지 않았습니다.** 버크셔를 첫 번째로 고른 이유가 **연차보고서에
취득원가가 나와 우리 계산을 검증할 수 있다**는 것이었는데(`docs/masters-13f.md`),
그 대조는 아직입니다. 개발 환경에서 SEC 가 막혀 있어 러너에서 받아와야 합니다.
**그전까지 이 숫자는 화면에도 문서에도 '추정'입니다.**

**이름은 가장 긴 것이 아니라 가장 최근 분기의 것을 씁니다.** 2013년 이전 텍스트
공시를 변환한 이름에 깨진 것이 섞여 있어서, 가장 긴 이름을 고르면
`BRIDGE & IRON CO N V COCA` 같은 찌꺼기가 코카콜라를 이깁니다.

**보유 연차 자는 뺐습니다 (사용자 판단).** "굳이 의미 없는 거 같아."
전량매도 줄의 `보유 1.5년` 만 남겼습니다 — 거기서는 그게 이야기의 전부입니다.

**색은 수익률에만 씁니다.** 늘림/줄임은 이득이 아니라 **행동**이라 중립색이고
`늘림 +83.2%` 처럼 말로 방향을 밝힙니다. 한 줄에 색을 두 종류 쓰면 어느 쪽이
돈 이야기인지 흐려집니다(재료표 13번의 "색을 늘리지 마세요").

**회사 마크를 넣었습니다 (2026-09-14, 사용자 판단).** 제가 처음에 "디자인이 깨진다"며
말렸는데 사용자가 "색색깔 로고 얹어도 괜찮지 않을까"라고 했고, **그건 취향이고 이 사이트는
사용자 것이므로 따랐습니다.**

**디자인이 무너진 원인은 색이 아니라 크기와 상자였습니다.** 17px 로 만들었더니
사용자가 "디자인이 너무 무너지는데"라고 했고, 벤치마킹하는 사이트(stockcircle.com)의
화면을 보니 차이가 분명했습니다.

```
  17px · 상자 없음   심벌(사과·G)만 읽히고 글자 로고는 뭉갬 → 몇 줄만 그림이 있는 목록
  30px · 테두리 상자  글자 로고도 읽힘 · 로고와 글자 타일이 같은 상자라 줄이 고름
```

**핵심은 로고와 글자 타일이 똑같은 상자를 쓰는 것입니다.** 상자가 제각각이면
"덜 만든 화면"처럼 보입니다. 둥근 모서리는 저쪽이 쓰지만 우리는 안 씁니다
(재료표 5번 — 이 화면의 동그라미는 ⓘ 와 표시등뿐).

**마크 주소는 설정 블록 한 줄입니다.** `window.MARKS.url` 이고, `LIVE`·`STATS`·
`SUPPORT` 와 같은 요령이라 **비우면 통째로 잠깁니다**(5번).

```
https://api.elbstream.com/logos/{idType}/{id}?format=png&size=64
```

**Elbstream Stock Logos API** 입니다. 사용자가 벤치마킹하는 stockcircle 이 쓰는
곳이고, OpenAPI 원문을 받아서 읽었습니다.

```
GET /logos/{idType}/{id}     security: []            ← 인증 없음이 명시적으로 선언됨
POST /news · /companies      security: [BearerAuth]
idType = isin | symbol | wkn | cik | lei | provider | financial_institution
       | crypto | country | currency | commodity          ← cusip 은 없습니다
```

**API 키는 필요 없습니다.** 전역이 `BearerAuth` 인데 로고 경로만 `security: []` 로
**명시적으로 끕니다.** 사용자가 브라우저에서 `US0378331005` 를 열어 실제로도 확인했습니다.
추측이 아닙니다.

**티커 매핑 작업이 대부분 사라졌습니다: ISIN 을 CUSIP 에서 직접 만듭니다.**
미국 종목의 ISIN 은 "US" + CUSIP 아홉 자리 + 체크숫자(글자를 A=10…Z=35 로 편 뒤
Luhn)입니다. 문서 예시로 대조했습니다 — `594918104 → US5949181045`,
`166764100 → US1667641005`(문서의 뉴스 응답에 `Chevron Corp.` 으로 실려 있습니다).

**나머지 5.8% 는 기계가 채웁니다.** 첫 글자가 숫자가 아닌 CUSIP(CINS — 버뮤다·
케이맨·아일랜드·스위스 법인)은 ISIN 을 지어낼 수 없습니다. 28년치 223개 묶음 중
**13개**이고 지금 보유 중인 것은 처브 하나입니다.

**손으로 적지 않습니다.** 처음에 처브 한 줄을 박아 뒀는데 사용자가 정확히 짚었습니다 —
*"처브 같은 거 안 나오면 일일이 내가 찾아서 넣어 줄 수는 없잖아."* 매니저를 더
넣으면 계속 늘어나고, **사람이 붙어야 하는 표는 결국 낡습니다.**

```
scripts/fetch_tickers.py   OpenFIGI 에서 CUSIP → 티커 (키 없음, 분당 25요청)
data/titans/tickers.json   봇이 채우는 표. 화면은 이것만 읽습니다.
```

**OpenFIGI 는 러너에서만 부릅니다.** 방문자 브라우저는 저장소의 표만 읽으므로
방문자 IP 가 OpenFIGI 로 나가지 않고, 화면이 그쪽에 묶이지도 않습니다.
(개발 환경에서는 `000` 으로 막혀 있습니다 — SEC·야후와 같습니다.)

**못 찾은 것도 기록합니다.** 옛 종목의 사라진 주식 종류(리버티글로벌 4종 등)는
OpenFIGI 에도 없어서 영영 못 찾는데, 안 적어 두면 **매주 다시 묻고 매주 실패합니다** —
6-2 의 "매주 실패하면 아무도 안 봅니다"가 그대로 생깁니다. 빈 문자열로 적어 두고
90일 뒤에 한 번 더 물어봅니다. **실패로 끝내는 경우는 하나뿐입니다**: 한 번도 안
물어본 종목이 있는데 그중 하나도 못 받았을 때(= OpenFIGI 가 막힘).

**티커 단계는 공시를 저장한 뒤에 돕니다.** 티커를 못 받는다고 13F 데이터까지
안 올라가면 곤란합니다(6-2 의 "데이터를 먼저 커밋하고 그 뒤에 본다"와 같은 순서).

**두 번 돌려서 알아낸 것 (2026-09-14).** 첫 실행은 17건 전부 '못 찾음' 이었는데
**로그에 우리가 해석한 결과만 찍혀서 왜인지 알 수가 없었습니다.** 개발 환경에서
OpenFIGI 가 막혀 있으니(`000`) 응답 모양을 짐작해서 파서를 짠 셈이고, 그건
`probe_sec.py` 를 만들면서 하지 않기로 한 일입니다(9-3). 원본을 찍게 고치고
다시 돌리니 답이 나왔습니다.

```
첫 응답 원본: HTTP 200 · [{"warning":"No identifier found."} ×3,
                          {"error":"Invalid idValue format."},   ← 딱 한 건
                          {"warning":"No identifier found."} ×6]
```

**OpenFIGI 는 막힌 적이 없습니다.** 멀쩡히 HTTP 200 으로 답하면서 그런 건 모른다고
한 것입니다. 여기서 두 가지가 나왔습니다.

**1. OpenFIGI 는 CUSIP 체크숫자를 검사합니다.** `Invalid idValue format` 이 온
자리를 세어 보니 **정확히 체크숫자가 틀린 CUSIP** 이었습니다. 28년치에서 두 건이고,
**둘 다 올바른 짝이 자료 안에 따로 있습니다.**

```
G47766101  Ingersoll-Rand 2006~2007   바른 값 G4776G101
           2013년 이전 텍스트 공시를 변환할 때 글자 G 를 숫자 6 으로 읽음 — 우리 잘못
G6693N103  NU Holdings 2021~2022      바른 값 G6683N103
           XML 시대라 SEC 원문 자체의 오타 (나중 분기에 고쳐짐)
```

`cusip_ok()` 가 이제 보내기 전에 거릅니다. **체크숫자 검사는 공짜로 얻은 검증기**라
앞으로 다른 매니저를 넣을 때도 자료 오류를 바로 잡아 줍니다.

**2. OpenFIGI 는 CINS 를 `ID_CUSIP` 으로 색인하지 않습니다.** 형식이 멀쩡한 열다섯
건이 전부 `No identifier found` 였습니다. 문서에 있는 여덟 자리 방식
(`ID_CUSIP_8_CHR`)도 같이 물어보게 해 뒀습니다 — **짐작으로 결론 내지 않고
한 번 더 실제로 물어보는 것**입니다.

**실패로 끝내는 기준을 고쳤습니다.** 처음에는 "하나도 못 찾으면 실패"였는데,
**OpenFIGI 는 답을 하면서 모른다고 하는 것이므로 그건 고장이 아닙니다.** 그대로
두면 새 해외 종목이 들어올 때마다 빨간 X 가 떠서 6-2 의 "매주 실패하면 아무도 안
봅니다"가 그대로 생깁니다. 지금은 **응답이 하나도 오지 않았을 때만** 실패입니다.

**곁가지가 거짓 경보를 보냈던 것도 고쳤습니다.** 13F 는 이미 성공해서 커밋된
뒤였는데 텔레그램에는 `13F 받기가 실패했습니다` 가 갔습니다. 티커 두 단계가
`continue-on-error` 라 이제 알림은 진짜 13F 실패일 때만 울립니다.

**`python -u` 로 돌립니다.** 버퍼링 때문에 오류 줄이 결과 목록보다 먼저 찍혀서
로그를 거꾸로 읽게 됐습니다.

**세 번째 실행에서 결론: OpenFIGI 로는 CINS 를 못 찾습니다.** `ID_CUSIP_8_CHR`
도 전부 `No identifier found` 였습니다(`새로 찾은 것 0개`). 두 방식 다 재 봤으니
여기서 닫습니다.

**그래서 SEC 로 갑니다 — 이름으로 찾습니다.** SEC 가 회사 이름·티커·CIK 표를
공개하고(`company_tickers.json`), **이 저장소는 이미 `SEC_CONTACT` 를 갖고
있습니다.** 새 열쇠도, 새 바깥 서비스도 필요 없습니다.

**이름으로 찾아도 되는 이유는 로고이기 때문입니다.** 리버티글로벌 네 종류는
전부 같은 회사이고 로고도 하나입니다. **값을 다루는 일이었다면 이 방식을 쓰지
않았을 겁니다** — 주식 종류를 못 가리니까요.

**한 회사로 좁혀질 때만 받습니다.** 전체 이름으로 먼저 보고, 안 되면 앞 두 낱말로
봅니다. 그 열쇠에 **CIK 가 둘 이상 걸리면 버립니다** — 엉뚱한 회사 로고를 다는 것보다
글자 타일이 낫습니다. 흉내 자료로 확인했습니다: `NU HLDGS LTD` 는 `NU Skin
Enterprises` 와 안 헷갈리고, 같은 이름의 회사가 둘이면 버립니다.

**`SOURCES` 표식이 세대입니다.** 못 찾은 것은 빈 값으로 굳어 있어서 그냥 두면
90일 뒤에나 다시 묻습니다. **출처를 새로 붙이면 이 문자열을 바꾸세요** — 파일의
표식과 달라지면 못 찾은 것을 전부 한 번 다시 물어봅니다.

**SEC_CONTACT 가 없으면 이 길만 잠기고 OpenFIGI 쪽은 그대로 돕니다.**

**뒤로 물러나는 순서가 셋입니다: 바깥 그림 → 저장소의 simple-icons → 글자 타일.**
그래서 **elbstream 이 죽어도 화면은 이 기능을 켜기 전 모습 그대로**가 됩니다.
개발 환경에서 elbstream 이 차단돼 있어 그 상태를 크로미움으로 실제로 재 봤습니다 —
**깨진 그림 0개**, 상위 5개는 simple-icons, 나머지는 글자 타일로 떨어졌습니다.
`tickers.json` 이 아직 없을 때도 같습니다(처브만 글자 타일).

**바깥 주소를 쓰면 방문자 IP 가 그쪽으로 갑니다.** 이용약관도 쓰기 전에 확인할
일입니다. **어시스턴트가 대신 판단하지 않습니다** — 설정 한 줄을 비우면 꺼집니다.

**로고 출처를 재 본 결과 (2026-09-14).****로고 출처를 재 본 결과 (2026-09-14).**

```
simple-icons (CC0-1.0)   상위 10 중 5개   흰 바탕에서 전부 보임   저장소에 박아 둠
nvstly/icons (티커별)    상위 10 중 10개  3개가 안 보임          LICENSE 파일 없음
elbstream.com/logos      ?               ?                      개발 환경에서 막힘
```

`nvstly/icons` 는 다크 테마용이라 흰 바탕에서 `AAPL` 대비 **1.00**, `CB` **1.00**,
`MCO` **1.74** 였습니다(픽셀을 실제로 쟀습니다). 게다가 `LICENSE` 가 없어 기본값이
"모든 권리 보유"입니다. **공개 저장소에 넣는 판단은 사용자 몫이라 안 넣었습니다.**

`elbstream.com` 은 stockcircle 이 쓰는 곳이라고 사용자가 알려 줬는데,
**개발 환경에서 연결이 차단돼(`000`) 주소 규칙을 확인하지 못했습니다.**
위키데이터·위키미디어 커먼즈·financialmodelingprep·구글 파비콘도 전부 같습니다.
**로고를 구할 길은 `raw.githubusercontent` 하나뿐입니다.**

**바깥 주소를 쓰면 방문자 IP 가 그쪽으로 갑니다.** 그리고 그 서비스의 이용약관은
쓰기 전에 확인할 일입니다 — 어시스턴트가 대신 판단하지 않습니다.

**우리 추정 매수가가 맞는지 처음으로 대조됐습니다 (2026-09-14).**
사용자가 보내 준 stockcircle 화면과 같은 공시(2026-06-30)를 비교했습니다.

```
애플 평균 매수가   stockcircle $39.63   우리 $39.59   차이 0.1%
BoA 이번 분기      -5.9%                -5.9%
알파벳 이번 분기   +45.2%               +45.2%
```

**그런데 1998년 이전부터 들고 있는 종목은 저쪽 숫자가 틀렸습니다.**
코카콜라 평균 매수가를 `$32.90` 이라고 적어 뒀는데, 우리 자료의 **1998-12-31
관측가 $67.00 을 2012년 2:1 분할로 나눈 $33.50** 과 거의 같습니다. 즉 저쪽은
**자료에 처음 보이는 분기의 가격을 매수가로 쓰고 있습니다.** 버크셔가 코카콜라를
실제로 산 것은 1988~1994년입니다. **우리가 `1998년 이전부터 보유` 라고 비워 두는
쪽이 맞습니다** — 벤치마크가 그렇게 한다고 따라가지 마세요.

**상위 10종목을 펴고 나머지는 접습니다 — 지우는 것이 아닙니다.**
**다만 제목의 `26종목 · $299B` 는 그대로 둡니다** — 10개가 전부인 것처럼 보이면
화면이 거짓말을 합니다. 밑에 `상위 10종목만 · 전체의 91.0%` 를 적고,
`작은 16종목 더 보기 · 전체의 9.0% · 늘림 4 · 줄임 5` 로 접습니다.

**여기 "접힘을 없앴습니다"라고 적혀 있었는데 그것이 잘못이었습니다 (2026-09-15).**
2026-09-14 에 "상위 10개만"이라는 말을 **접기가 아니라 삭제로** 옮겼고, 이 문서에도
그렇게 적어 두는 바람에 다음 세션까지 그 상태로 굳었습니다. 사용자가 화면을 보고
바로잡았습니다 — *"상위10개만 우선 보여주고 나머지는 더보기에 숨겨두기로 했는데
지금 보면 완전 삭제했어."* **접어 두는 것과 없애는 것은 다릅니다.**
지금은 `<details class="fold">` 로 돌아왔고, CSS 에 지우지 말라는 주석을 달았습니다.

**펼치면 위 캡션이 바뀝니다.** `상위 10종목만 · 전체의 91.0%` 를 펼친 채로 두면
화면이 자기 말을 어기므로, 열리는 순간 `26종목 전부 펼침` 으로 바꿉니다.

**접힌 줄의 막대도 1위 기준으로 그립니다.** 접힌 쪽만 따로 최대값을 잡으면
펼쳤을 때 위아래가 다른 자로 읽힙니다.

**접힌 줄의 마크는 늦게 뜹니다.** 접혀 있는 동안은 브라우저가 그림을 안 받으므로,
펼친 직후 0.5초에는 깨진 그림이 3개 보입니다. 1.5초 뒤에는 0개이고 전부 글자
타일로 떨어집니다(개발 환경에서 elbstream 이 막힌 상태로 실측). **펼친 직후를
찍어서 버그로 착각하지 마세요.**

**개발 환경에서 로고를 구할 길은 `raw.githubusercontent` 하나뿐입니다.**
위키데이터·위키미디어 커먼즈·financialmodelingprep·구글 파비콘 전부 `000`
(연결 차단, 404 아님)입니다. 나머지 다섯 개를 진짜 로고로 바꾸려면 **사용자가
받아서 저장소에 넣어야** 합니다.

**사이트맵 줄마다 표식을 달았습니다.** `<!-- root -->` / `<!-- titans -->` 입니다.
표식 없이 `sed` 를 돌리면 매일 도는 `update-data.yml` 이 **분기마다 바뀌는 titans
줄까지 매일 찍어서** 6-2 에 적힌 "거짓 lastmod" 를 스스로 만듭니다.

**SEC 이름 대조가 첫 실행에서 통째로 실패했습니다 — gzip 이었습니다 (2026-09-14).**
PR #42 를 머지하고 돌린 Update 13F #6 은 초록불이었는데 티커는 하나도 안 채워졌습니다.
로그에 답이 있었습니다.

```
SEC 표 207,552 bytes · 앞부분  \x1f\x8b\x08…      ← gzip 바이트
실패 JSONDecodeError: Expecting value: line 1 column 1
```

`Accept-Encoding: gzip, deflate` 를 **보내 놓고 풀지 않았습니다.** 파이썬 `urllib` 은
자동으로 풀지 않습니다. 이름이 안 맞아서가 아니라 **표를 읽지도 못했습니다.**
지금은 헤더와 **매직 바이트를 둘 다** 보고 풉니다(헤더 없이 압축해 보내는 서버가 있습니다).

**원본을 찍게 해 둔 것이 이것을 잡았습니다.** 안 찍었으면 "이름 대조가 안 되네"로
또 잘못 진단했을 자리입니다 — 같은 회차에서 이미 한 번 겪은 일입니다.

**못 찾은 것은 빈 값으로 굳어 있으므로 `SOURCES` 세대를 올려야 다시 묻습니다**
(`openfigi+sec` → `openfigi+sec2`). 안 올리면 고쳐 놓고도 90일을 기다립니다.

**이름 열쇠를 넓혔습니다.** 공시의 이름은 줄여 쓰여 있어서 통째 비교로는 안 맞습니다.

```
BANK AMER            vs  BANK OF AMERICA        → OF·AND 를 버림
OCCIDENTAL PETE      vs  OCCIDENTAL PETROLEUM   → 앞 4자·3자로도 비교
SIRIUSXM             vs  Sirius XM              → 띄어쓰기를 없앤 열쇠
MOODYS               vs  Moody's Corp /DE/      → 아포스트로피 삭제 · /DE/ 제거
```

넓은 열쇠일수록 엉뚱한 회사가 걸릴 수 있는데, **한 회사로 좁혀질 때만 받는 규칙**이
그것을 막습니다. 흉내 표로 재 보니 이번 분기 26개 중 표에 있는 16개가 전부 맞았고,
**처브도 여기서 잡힙니다.**

**섹터(업종)를 이름 옆에 적습니다 (2026-09-14, 사용자 요청).**
*"각각 종목 옆에 섹터를 적어줬으면 좋겠어. 예를 들면 쉐브론은 석유·화학이잖아?"*

**새 출처가 필요 없습니다.** SEC 가 회사마다 업종 코드(SIC)를 매겨 두고, 방금 만든
이름 대조가 CIK 를 찾아 줍니다.

```
CHEVRON CORP NEW → CIK 93410 → data.sec.gov/submissions/CIK0000093410.json
                 → "sic":"2911","sicDescription":"Petroleum Refining"  → 석유·정유
```

**앞부분만 읽고 끊습니다.** submissions 파일에는 최근 공시 목록이 통째로 들어 있어
회사에 따라 몇 MB 입니다. `sic` 은 맨 앞에 있으므로 16KB 만 받습니다 — 실측으로
정상 경로에서 **16,384 바이트**만 읽습니다. 못 찾으면 512KB 까지 더 읽고 포기합니다.
**압축은 요청하지 않습니다**(앞부분만 읽기와 gzip 은 같이 못 갑니다). 그래도 압축해
보내면 매직 바이트를 보고 통째로 다시 받습니다.

**이름표는 회사가 아니라 코드로 답니다.** `SIC4`(네 자리 46개) → `SIC2`(대분류 구간
29줄) → SEC 가 보낸 영문 설명 순으로 떨어집니다. 회사마다 적으면 매니저를 더 넣을
때마다 표가 늘어나고, **사람이 붙어야 하는 표는 결국 낡습니다** — 티커에서 사용자가
정확히 짚은 그 문제입니다.

**모르면 비웁니다.** 틀린 섹터를 적는 것보다 안 적는 것이 낫습니다. 파일이 없거나
깨졌거나 배열이거나 값이 문자열이어도 화면은 그대로 돕니다(넷 다 크로미움으로 재서
JS 오류 0건 · 줄 10개 그대로).

**테두리를 두르지 않습니다.** 테두리 상자는 주식 종류(`A+C`)의 것이라, 한 줄에
똑같이 생긴 상자가 둘이면 무엇이 무엇인지 흐려집니다. 섹터는 이름에 딸린 설명이라
10.5px · `--ink-3` 글자만 둡니다.

**390px 에서 긴 이름은 섹터가 둘째 줄로 넘어갑니다**(아메리칸익스프레스·알파벳 두 줄,
합계 712 → 745px). 가로 넘침은 320/360/390/900px × 한국어·영어 **전부 0px** 입니다.

**첫 실행 결과와 남은 구멍 (2026-09-15).** 섹터가 221개 중 **160개** 채워졌고,
이번 분기 26개 중 24개가 붙었습니다. **못 붙은 것은 애플과 D R 호턴 둘**이었는데,
둘 다 SIC 가 없어서가 아니라 **이름으로 한 회사를 못 좁힌 것**이었습니다
(CIK 을 못 찾은 것 61개 · CIK 은 찾았는데 SIC 가 없던 것 0개).

**애플이 떨어진 이유는 넓은 열쇠가 좁은 열쇠를 이겼기 때문입니다.**

```
APPLE INC                    통째 열쇠  APPLE
Apple Hospitality REIT, Inc. 첫낱말 열쇠 APPLE      ← 같은 칸에 들어감
                             → 두 회사로 갈려서 버려짐
```

**그래서 열쇠를 층으로 나눠 같은 층끼리만 맞춥니다**(`LEVELS = 6`).
통째 열쇠 칸에는 애플 하나뿐이라 그대로 잡힙니다.

**D R 호턴은 낱말 순서가 달랐습니다** — SEC 는 `HORTON D R INC /DE/` 로 적습니다
(성을 앞에 적는 EDGAR 표기). **순서를 무시한 열쇠**(낱말을 정렬해서 잇기)를
1층에 넣어 해결했습니다.

```
0 통째     APPLE          2 붙여쓰기  SIRIUSXM = Sirius XM
1 순서무시  D HORTON R     3 앞 4자    BANK AMER = BANK OF AMERICA
                          4 앞 3자    OCC PET = OCCIDENTAL PETROLEUM
                          5 첫낱말    CHEVRON
```

**낱말이 하나여도 1·2층을 비우면 안 됩니다.** 층을 나누면서 한 번 비웠더니
공시의 `SIRIUSXM` 이 SEC 의 `Sirius XM`(붙여쓰기 열쇠)과 만날 자리가 없어져
떨어졌습니다. 낱말이 하나면 순서무시도 붙여쓰기도 그 낱말 자신입니다.

흉내 표(미끼 6개 포함 — Chevron Phillips · Liberty Broadband · Kilroy Realty 등)로
재 보니 **이번 분기 26개 전부 맞고 잘못 짝지은 것 0개**입니다.

**고친 뒤에는 세대를 올려야 다시 묻습니다** — `sec-sic` → `sec-sic2`,
`openfigi+sec2` → `openfigi+sec3`. 못 찾은 것은 빈 값으로 굳어 있습니다.

**그래도 이름 대조는 최선을 다하는 방식이지 보장이 아닙니다.** 못 좁히면 섹터를
비우고 마크는 글자 타일로 갑니다 — **틀린 것을 적지는 않습니다.** 새 종목이
들어와도 사람이 손댈 일은 없고, 그 종목만 조용히 비어 있을 수 있습니다.

**종목 줄을 다시 짰습니다 (2026-09-15, 사용자 요청).**

**두 번 만에 맞췄습니다.** 처음에는 사용자가 적어 준 조각들을 *제가 생각한 자리*에
배치했는데 "내가 원하는 디자인이 아닌데"가 돌아왔습니다. 그래서 사용자가 구조를
직접 그려 줬고, 그대로 만들었습니다. **조각 목록과 배치는 다른 이야기입니다 —
자리를 안 정해 줬다면 물어보는 편이 한 번 덜 돕니다.**

```
{왼쪽 정렬}  [로고] [보유 금액 순위] [종목이름] [종목 섹터]
{가운데 정렬} [보유주식수] [±주식수 or 그대로] [추정 수익률]
{오른쪽 정렬} [보유 금액] 줄바꿔 작은 글씨로 [비중(%)]
```

```
전   1  Apple Inc 컴퓨터                              $66.0B
        39.59→289.36  +631%          그대로        22.0%

후  [로고] 1 Apple Inc 컴퓨터   2.28억주  그대로  +631%      $66.0B
                                                            22.0%
```

**한 줄로 합쳐졌습니다.** 예전에는 이름 아래 둘째 줄에 숫자가 붙었는데, 지금은
세 덩어리가 한 줄에 섭니다.

**매수가 화살표를 뺀 이유.** `39.59→289.36 +631%` 에서 사람이 읽는 것은 맨 뒤
하나였고, 앞의 두 숫자는 **그 값을 어떻게 얻었는지의 설명**입니다. 그래서 각주로
내렸습니다 — `수익률 = 추정 매수 평균가 → 2026.06.30 가격 · 배당 제외`.

**증감을 비율이 아니라 주식수로 적습니다** — `늘림 +83.2%` → `+4,814만주`.
판정 자체는 예전처럼 주식수 차이로 하므로 말과 셈이 어긋나지 않습니다.
그 값을 쓰려고 `build()` 에 `dn`(주식수 차이)을 더했습니다 — `dsh`(비율)는
판정에, `dn` 은 표기에 씁니다.

**`비중유지` 가 아니라 `유지` 입니다.** 사용자가 처음에 `비중유지` 로 적어 줬는데
**이 화면에서는 거짓말이 됩니다** — 애플은 이번 분기에 한 주도 안 건드렸는데
비중은 +0.1%p 움직였습니다(9-3 맨 위의 "판정은 비중이 아니라 주식수로"와 같은
자리). 이유를 말했더니 사용자가 **"비중이라 하면 좀 어폐가 있구나. 그냥 유지라고
쓰는 건 어떤가"** 로 정리해 줬습니다. `그대로` 보다 짧고 뜻이 같습니다.

**`1998년 이전` 은 `집계 전` 으로 바꿨습니다 (사용자 지적).**
*"1998년전부터 라는건 의미를 잘 모르겠어."* 맞습니다 — **방문자는 1998년이 우리
자료의 시작이라는 걸 알 길이 없습니다.** 연도는 줄에서 빼고 각주에서 한 번만
밝힙니다(`‘집계 전’ 은 우리가 가진 첫 공시(1998년)에 이미 들어 있던 종목입니다`).

**`BUY`/`SELL` 이름표를 주식수 위에 답니다 (사용자 요청).**

```
   BUY          SELL
+4,814만주    −3,023만주        유지
```

**색은 이름표에만 씁니다.** 숫자까지 칠하면 한 줄에 색깔 숫자가 둘이 되어(증감과
수익률) 어느 쪽이 돈 이야기인지 흐려집니다 — 재료표 13번이 막으려던 바로 그것입니다.
`--up`/`--down` 을 쓰므로 **언어를 따라 함께 뒤집힙니다**: 한국어는 빨강 매수·파랑
매도(사용자가 지정한 그대로), 영어는 초록 매수·빨강 매도(6번의 색 규칙).

**안 움직인 줄에도 이름표 자리를 비워 둡니다**(`.act i` 가 늘 있습니다). 안 그러면
사고판 줄만 두 줄이 되어 목록이 들쭉날쭉합니다. **그리고 나머지 두 칸에도 같은
14px 빈 자리를 둡니다**(`.hold::before` 등) — 안 그러면 그 칸 숫자가 한 줄 내려앉아
가로줄이 어긋납니다. **줄 높이(`line-height`)도 세 칸에 똑같이 못박아야 합니다** —
`.act` 만 다른 값을 쓰는 동안 숫자가 3px 어긋나 있었습니다(실측으로 잡았습니다).

**변하는 것은 줄에, 안 변하는 것은 머리에 (2026-09-15).**
사용자가 물었습니다 — *"추정 수익률이라고 명시해줬으면 좋겠는데, 표처럼 하는 게
좋을까 매 종목마다 쓰는 게 좋을까?"* **표 머리글 한 줄**로 갔습니다.

```
                       보유 주식 수   이번 분기   추정 수익률   금액 · 비중
```

근거는 이 문서에 이미 있습니다 — 4번 성적표에서 *"라벨이 숫자보다 자리를 더
차지했습니다 … 열 머리에 한 번만 씁니다"* 라고 같은 판단을 했습니다. 26줄이면
같은 말이 78번 나옵니다. **`BUY`/`SELL` 만 줄 안에 두는 이유는 그것이 줄마다
바뀌는 값, 즉 라벨이 아니라 데이터이기 때문입니다.**

**좁은 화면(640px 이하)에서는 머리글을 감춥니다.** 거기서는 세 칸이 한 줄에 안 서서
머리글이 가리킬 자리가 없습니다 — **자리를 못 가리키는 이름표는 없느니만 못합니다.**

**변수 이름을 `tag` 로 쓰지 마세요.** `rowHTML` 안에서 `tag` 는 이미 주식 종류
꼬리표(`A+C`)입니다. BUY/SELL 을 `tag` 로 받았다가 `Identifier 'tag' has already
been declared` 로 **화면이 통째로 비었습니다.** 지금 이름은 `flag` 입니다.

**죽은 사전 키를 지웠습니다** — `sinceBefore`·`justIn`(짧은 말로 대체됨),
`colShare`·`colValue`(아무도 안 쓰던 옛 키). 앞 커밋에서 제가 "`sinceBefore` 는
'이번 분기' 칸에서 아직 쓴다"고 적었는데 **틀렸습니다** — 그 칸은 `evNew`·`evBack`
을 씁니다. 지우기 전에 실제로 `grep` 해서 확인하세요.

**칸마다 최소 폭을 줍니다.** 줄(`<li>`)마다 격자가 따로라서, 폭을 안 주면 칸이
줄마다 제 내용만큼만 넓어져 **세로줄이 안 맞습니다.** 계측기·측량 도면이라는
이 화면의 언어가 거기서 무너집니다. 지금은 `.hold` 74px(영어 118) ·
`.act` 86px(영어 124) · `.ret`/`.note` 70px 이고 전부 가운데 맞춤입니다.

**가운데 칸이 좁아서 안내 문구를 줄였습니다.** `집계 전`·`이번 분기`
(사전의 `noRetOld`·`noRetNew`). 긴 설명은 **아래 각주**가 합니다 — 카드 위 캡션에
넣었더니 영어 대문자 캡션이 두 줄이 되어 읽기 나빠졌습니다.

**640px 이하에서는 왼쪽만 첫 줄에 두고 가운데·오른쪽을 아래로 내립니다.**
세 덩어리가 한 줄에 안 들어갑니다. 로고와 이름이 먼저 읽히는 순서는 지키면서
숫자 칸이 제 너비를 갖습니다. 최소 폭도 거기서는 풉니다.

**같이 지운 것**: `.sub2`·`.cost`·`.cost em`·`.w` CSS 와 `price()` 함수,
그리고 `.mk` 의 `grid-row:1/span 2`(둘째 줄이 없어져 무의미). 남겨 두면 7번의
"지운 블록의 CSS 가 남아 화면을 밀어낸다"가 됩니다.
(`shortShares()` 는 반대로 **정의만 있고 아무도 안 쓰던 함수**였는데 살아났습니다.)

**영어 초록이 막대 위에서 4.49 로 AA 에 0.01 모자랍니다.** 흰 카드 위에서는 5.02
인데, 비중 막대(`rgba(15,26,36,.055)`)가 깔린 자리에서 그렇습니다. **제가 만든 것이
아니라 원래 그랬고**(색·크기·바탕 그대로, 자리만 옮김), 고치려면 `--up #1B7F4B` 를
`#1A7A47` 쯤으로 내려야 하는데 **그 토큰은 대시보드(`index.html`)와 재료표가 같이
쓰는 값**이라 두 화면을 함께 다시 재야 합니다. 사용자에게 알리고 남겨 뒀습니다.

**주소를 `/titans/` 에서 `/titans/berkshire/` 로 옮겼습니다 (2026-09-16, 사용자 제안).**
*"지금 주소가 /titans 인데, 사실 /titans/berkshire가 되어야 할거 같은데, 어때?"* 맞습니다 —
투자자를 여덟 명까지 넣을 자리인데 한 명이 `/titans/` 를 차지하고 있었습니다.

**지금이 제일 쌌습니다.** GitHub Pages 는 301 을 못 합니다(2번). 그래서 주소를 옮기는
비용은 **그 주소가 얼마나 알려졌느냐**에 비례하는데, 지금은 가리키는 것이 사이트맵
한 줄뿐이고 색인도 아직 안 됐습니다. 투자자가 둘이 된 뒤에 옮기면 그때는 비쌉니다.

**`warren-buffett` 이 아니라 `berkshire` 입니다.** 이 문서의 "살아 있는 투자자의
이름을 간판에 걸지 마세요"(퍼블리시티권·상표)가 주소에도 걸립니다. 13F 를 내는 주체도
개인이 아니라 Berkshire Hathaway Inc. 입니다. stockcircle 은 `/portfolio/warren-buffett`
을 쓰지만 따라가지 않습니다.

**옛 주소에는 튕기는 자리를 뒀습니다.** `canonical` 로 진짜 주소를 가리키고, 0초
새로고침과 `location.replace` 로 넘기고, **눈에 보이는 링크**를 같이 둡니다.
`?lang=ko` 도 들고 갑니다(실측 확인). **링크를 숨기지 않습니다** — `opacity:0` 으로
감춘 링크는 구글이 스팸으로 보는 항목이고, 이 사이트는 아직 색인 판정을 기다립니다.

**`/titans/` 를 지금 목록 화면으로 만들지 마세요.** 링크 한두 개짜리 얇은 페이지가
되어 색인 판정에 오히려 나쁩니다 — 루트를 허브로 아직 안 만드는 것과 같은 이유입니다.
**투자자가 둘이 되면** 그때 목록으로 바꾸세요.

**경로가 한 단 깊어졌습니다** — 화면이 읽는 자료가 `../data/` 에서 `../../data/` 로
바뀌었습니다(세 곳). 여기를 안 고치면 화면이 통째로 빕니다.

**벤치마크(stockcircle)와 숫자가 다른 이유를 확인했습니다 (2026-09-16).**
사용자가 저쪽 화면에 **마루베니**가 있는데 우리에겐 없다고 짚었습니다. 재 봤습니다.

```
일본 상사 5곳(마루베니·미쓰비시·미쓰이·이토추·스미토모)   28년치 111분기에 0건
```

**우리 파서 문제가 아닙니다.** 도쿄 거래소에서 산 주식이라 13F 에 안 실립니다.
**결정적 단서는 날짜**입니다 — 저쪽 마루베니 줄에 `May 07, 2026` 이라고 적혀 있는데,
13F 는 분기말 스냅샷뿐이라 그런 날짜가 나올 수 없습니다. 즉 저쪽은 **13F 말고 다른
공시를 섞고** 있습니다. 해외 보유분은 **추후로 미뤘습니다**(사용자 판단, 2026-09-16) —
넣으면 "퍼블릭 도메인 한 출처로 자동으로 돈다"는 이 프로젝트의 이점이 깨집니다.

**거꾸로 우리 계산이 저쪽과 정확히 맞는 것도 확인됐습니다.** 저쪽 `Q4 2025 · Sold 4.3%`
와 우리 자료의 `2025-12-31 · 238,212,764 → 227,917,808 (−4.3%)` 가 분기도 수치도 같습니다.

**수익률이 달랐던 것(+631% vs +736%)은 기준 가격 차이였습니다.**

```
평균단가 $39.63 기준   분기말가 $289.36 → +630%   ← 우리
                      현재가   $331.34 → +736%   ← 저쪽
```

**현재가 기준으로 바꾸지 않기로 했습니다**(사용자 판단, 2026-09-16). 바꾸려면 26종목의
오늘 주가를 새로 받아와야 하고, 야후 의존과 11번의 라이선스 문제가 따라붙습니다.

**종목마다 일생 차트를 달았습니다 (2026-09-16, 사용자 요청).**
*"차트를 우리 디자인에 맞춰서 만들어두면 좋을거 같아 … 15년까지 보여주고 그 이상은
차트를 횡스크롤해서 볼 수 있도록."* 줄을 누르면 그 아래로 펼쳐집니다.

```
위 칸   분기말 가격선          ← 중립색 (--ink-2 · 1.6px)
아래 칸  그 분기에 사고판 주식수 ← 0선 위 매수 / 아래 매도, 색은 여기만
가로축   둘이 함께 씁니다        ← 성적표가 이미 쓰는 어휘
```

**새 데이터가 한 톨도 필요 없습니다.** 가격은 `금액 ÷ 주식수`(그 분기말 가격)이고,
추정 매수가를 만들 때 이미 쓰던 셈입니다. 그래서 **주 1회 13F 워크플로가 도는 것만으로
차트도 같이 갱신됩니다** — 사용자가 걱정한 "추후 업데이트"가 저절로 해결됩니다.

**분기당 폭이 고정입니다**(`PXQ=12`). 900px 카드에서 **60분기(15년)** 가 들어가고
그 이전은 옆으로 끕니다. 폭을 컨테이너에 맞춰 늘리면 코카콜라(111분기)와
메이시스(2분기)가 같은 폭이 되어 자가 사라집니다.

**여는 순간 오른쪽 끝으로 보냅니다.** 접혀 있는 동안은 폭이 0 이라 미리 못 옮깁니다.
그리고 **"끌 수 있다"는 말은 실제로 넘칠 때만** 합니다 — 분기 수로 짐작하면 좁은
화면에서는 짧은 차트도 넘치고 넓은 화면에서는 긴 차트도 안 넘칩니다. 그려진 뒤에
`scrollWidth > clientWidth` 를 재서 켭니다.

**액면분할을 매수로 그리고 있었습니다 (검수에서 잡음).** `lifeOf()` 가 지난 분기들만
분할 배수로 고치고 **직전 주식수(`held`)는 그대로 둬서**, 애플 2020-09 가
`944M − 245M = +699M` 으로 잡혀 **사상 최대의 매수 막대**가 서 있었습니다.
`held` 도 같이 늘리면 `944M − 980M = −36M` 으로, 그 분기에 실제로 판 만큼만 남습니다.
9-3 맨 위의 "분할을 안 잡으면 이 화면은 거짓말을 합니다"가 **막대에서 다시 나온 것**입니다.

실측으로 대조했습니다(전부 분할 반영값).

```
2018-03-31  +296.9M   = 74.2M ×4        버크셔가 애플을 크게 사들인 분기
2020-06-30  $91.20    = $364.80 ÷4      실제 종가와 일치
2020-09-30  −36.3M                      분할 분기의 진짜 매도
2024-06-30  −389.4M   보유 400.0M       지분을 반으로 줄인 그 분기
```

**한 번도 안 사고판 종목은 아래 칸을 아예 안 그립니다.** 코카콜라는 111분기 내내
막대가 0 인데(1994년 이후 안 건드렸습니다), 빈 칸에 0선만 그으면 **고장난 화면으로
읽힙니다.** 칸을 지우고 `이 기간에 사고판 적이 없습니다` 를 아래 한 줄에 적습니다.
캡션도 `분기말 가격` 하나로 줄입니다 — 없는 칸을 가리키면 안 됩니다.

**호버를 안 만들었습니다.** 손가락에는 호버가 없습니다. 대신 **가장 큰 매수 하나와
가장 큰 매도 하나**에만 이름표를 달고(스물몇 개를 다 적으면 12px 간격에서 겹칩니다),
나머지는 아래 한 줄(`42분기 · 최저 $23.90 → 최고 $289.36`)이 말합니다.

**`text-anchor` 를 CSS 에 두면 안 됩니다.** 맨 왼쪽 연도가 그림 밖으로 반쯤 잘려서
속성으로 `start` 를 줬는데 안 먹혔습니다 — 7번의 *"`.svg-axis{fill:}` 이 SVG 의
`fill=` 속성을 이긴다"* 와 **같은 자리**입니다. `.axL` 에서 빼고 속성으로만 줍니다.

**줄 여백은 `.row` 가 아니라 `.rowline`(=`<summary>`)에 있어야 합니다.** `.row` 에
두면 펼친 차트 아래까지 여백이 붙고, 비중 막대의 기준 상자가 차트 높이만큼 늘어나
**줄이 아니라 기둥**이 됩니다. 막대와 직전분기 표식을 `<summary>` 안에 둔 이유도
같습니다(실측: 막대 26개 전부 `summary` 안 · 차트가 막대 위에 얹힌 것 0건).

**`PADB` 를 18 → 30 으로 늘렸습니다.** 매도 이름표(`−3.89억주`)와 연도(`2024`)가
겹쳐 있었습니다. `RIGHT` 도 64 → 76 인데, NVR 처럼 네 자리 주가(`$7,553.99`)가
오른쪽 여백을 넘기 때문입니다.

**재 본 것** (크로미움, 26줄 전부 펼친 상태):
JS 오류 0 · 차트 26/26 · 가로 넘침 320/360/390/900px × 한국어·영어 **전부 0px** ·
이름표가 그림 밖으로 나가거나 연도와 겹친 것 0건 · 차트 글자 대비는 흰 카드 위라
전부 AA(한국어 매수 5.37 매도 6.43, 영어 매수 5.02 매도 5.37, 연도 5.47, 가격 7.45).
**막대 위 4.49 문제는 여기서 안 생깁니다** — 비중 막대는 줄 안에만 있고
차트는 흰 바탕에 그려집니다.

**"자동 갱신"은 차트가 아니라 새 분기 공시를 뜻했습니다 (2026-09-16 확인).**
사용자가 바로잡아 줬습니다 — *"내가 말한 자동 갱신이라는 거는 분기가 바뀌어서 13f에서
새로운 공시가 발표될 때를 말하는 거야."* 그래서 **되는지 실제로 재 봤습니다.**

`berkshire.json` 에 가짜 2026-09-30 분기를 넣고(애플 10% 매도 · 알파벳 30% 매수 ·
메이시스 전량매도 · 비자 진입) 화면을 띄워 앞뒤를 나란히 놓았습니다.

```
전  2026.06.30 기준 · 26종목 $299B · 1 Apple 유지 +631%  · 사건: D R Horton 재진입
후  2026.09.30 기준 · 26종목 $318B · 1 Apple SELL −2,279만주 +689% · 사건: Visa 재진입
                                                                    Macys 전량매도
```

**손댈 곳이 없습니다.** 분기 목록을 코드에 박아 두지 않았습니다 —
`fetch_13f.py` 의 `list_filings()` 가 SEC 의 공시 목록(`filings.recent` + 쪼개진
옛 파일)에서 `13F-HR` 을 전부 긁어 오고, 이미 받은 분기는 건너뜁니다. 워크플로는
**주 1회 돌면서 새 것이 없으면 커밋도 안 합니다.** 화면 쪽도 `qs[qs.length-1]` 로
마지막 분기를 잡으므로 날짜·종목수·금액·갱신 표시가 저절로 따라옵니다.

**단 두 곳이 손으로 적혀 있었습니다. 그것도 고쳤습니다.**

```
전  "… 애플 4:1(2020), 아메리칸익스프레스 3:1(2000), 코카콜라 2:1(2012) …"
    "… 코카콜라와 아메리칸익스프레스가 여기 해당합니다."
후  둘 다 자료에서 뽑아 씁니다 (B.splits · B.preNames)
```

시뮬레이션이 바로 값을 했습니다 — 비자가 들어오자 각주에 **`Visa Inc 4:1(2015)`
가 저절로 실렸습니다.** 손으로 적은 목록이었다면 그 줄이 조용히 빠져 있었을 자리입니다.
9-3 의 *"사람이 붙어야 하는 표는 결국 낡습니다"* 가 각주에서도 똑같이 걸립니다.

**각주는 이제 배열이 아니라 함수입니다** — `foot:(firstY,splits,pre)=>[…]`.
`lifeOf()` 가 찾은 분할을 `SPLITS` 에 모으고, `build()` 가 그것을 지금 보유 종목과
맞춰 `B.splits` 로 돌려줍니다. **`paintFoot()` 를 두 번 부릅니다** — 자료가 없어도
각주는 나와야 하므로 한 번은 예시 없이, `build()` 뒤에 한 번 더.

**각주에 쓰는 이름은 한글 이름이 아니라 화면에 찍히는 그 문자열입니다**
(`코카콜라` 아님, `Coca Cola Co`). 6번의 *"글을 검수시킬 때는 화면에 실제로 찍히는
말과 맞는지 따로 보세요"* 가 여기 걸립니다 — 줄에는 `Coca Cola Co` 라고 적혀 있는데
각주만 `코카콜라` 면 같은 것을 가리키는지 알 수 없습니다.

**중복 각주 하나를 지웠습니다.** `집계 전` 설명이 두 번(5번째와 9번째 줄) 들어 있었고,
한국어·영어 양쪽 다 그랬습니다. 각주 13줄 → 12줄입니다.

**남은 사람 몫은 하나입니다: 정정 공시(13F-HR/A).** `fetch_13f.py` 가 **일부러
안 받습니다** — 통째로 다시 쓰는 정정과 빠진 것만 덧붙이는 정정이 있는데, 둘을
반대로 처리하면 종목이 두 배가 되거나 통째로 사라집니다. 실물을 한 번도 못 봤으므로
짐작해서 짜지 않고 **있는지 없는지만 로그에 알립니다.** 나오면 한 건을 열어 보고 넣습니다
(`docs/masters-13f.md` 11번의 14번 항목).

**차트 버그 셋을 고쳤습니다 (2026-09-16, 사용자가 PDF 로 지적).**
사용자가 화면을 PDF 로 떠서 보내 줬습니다. **셋 다 실제 버그였습니다** — 0번의
*"화면 지적이 들어오면 반박하기 전에 먼저 재 보세요"* 가 또 맞았습니다.

**1. 가로축이 시간이 아니라 배열 첨자였습니다 — 7년이 사라져 있었습니다.**

```
전  뱅크오브아메리카  … 2010  2018  2020 …   ← 2010-09 와 2017-09 가 바로 옆
후  뱅크오브아메리카  … 2008 2010 [빈 자리 7년] 2018 2020 …  선도 거기서 끊김
```

`lifeOf()` 는 **들고 있던 분기만** 담습니다. 그 배열의 첨자로 가로 위치를 잡으니
전량 매도했던 7년이 통째로 접혔고, 선은 2010년 값과 2017년 값을 **곧장 이어**
그렸습니다. 코드에는 *"전량 매도하면 끊습니다"* 라는 주석이 이미 붙어 있었는데,
**끊는 것은 자료뿐이고 그림은 안 끊고 있었습니다.**

지금은 `lifeOf` 가 `gi`(전체 분기 목록에서의 자리)를 같이 담고 `x` 가 그것으로
잡습니다. 안 들고 있던 분기도 자리를 차지하고, `o.first` 에서 `M` 으로 선을
끊습니다. 실측: 끊긴 자리 4곳(BoA · 옥시덴탈 · 델타 · D R 호턴).

**2. 짧은 이력이 우표만 하게 그려졌습니다.** 알파벳(4분기)이 818px 칸에 **122px**
로 앉아 있었습니다. 분기당 12px 고정이라 그렇습니다. 지금은 **이력이 짧으면
늘립니다** — `PX = min(PXQ*4, max(PXQ, PXQ*60/span))`. 60분기(15년)어치 폭을
넘지 않으므로 긴 차트는 그대로이고, 짧은 것만 그 폭까지 채웁니다(알파벳 230px,
애플 806px). **몇 분기인지는 아래 한 줄이 늘 적으므로** 배율이 달라져도 속지 않습니다.

**3. 연도 이름표가 겹쳐 있었습니다** — `2007 2008`, `2015 2016`, `2019 2020`,
`2025 2026`. 원인이 둘이었습니다.

- `%2` 로 박아 둔 규칙. 지금은 **픽셀을 재서** 정합니다(`step = ceil(60/(4*PX))`) —
  늘어난 차트에서는 매년, 기본 폭에서는 2년마다가 저절로 나옵니다.
- **가운뎃점 사이 거리로 겹침을 재고 있었습니다.** 맨 왼쪽 이름표는 `start` 맞춤이라
  오른쪽으로만 뻗는데 그것을 반만 쳐서 `2007`(10~36)과 `2008`(33~59)이 3px 겹쳤습니다.
  지금은 **글자 상자의 가장자리**로 봅니다(9.5px 고정폭 네 글자 ≈ 24px).

**겹치면 첫 눈금을 버립니다.** 첫 눈금은 1월이 아니라 *처음 산 분기*에 서 있어서,
그것을 남기려고 다음 연도를 버리면 축이 `2020 2022 2023…` 처럼 들쭉날쭉해집니다.
선이 어디서 시작하는지는 그림이 이미 말합니다. 실측: 26줄 전부 겹침 0건.

**마우스를 올리면 그 분기 값을 읽어 줍니다 (2026-09-16, 사용자 요청).**

```
위 칸 — 분기말 가격 · 아래 칸 — …        2024.06.30 · $210.62 · SELL −3.89억주
```

**그림 위에 말풍선을 띄우지 않았습니다.** 말풍선은 선과 막대를 가리고 좁은 화면에서
그림 밖으로 나갑니다. 대신 **캡션 줄 오른쪽에 고정된 자리**를 두고 거기를 채웁니다 —
자리가 늘 비어 있으므로 글자가 나타나도 줄이 안 밀립니다(`min-height` 로 못박음).
그림 안에는 십자선 한 줄과 점 하나만 둡니다(재료표의 눈금 어휘).

**여기서는 `pointer` 이벤트를 그대로 씁니다.** 7번의 *"hover 는 마우스일 때만"* 은
ⓘ 툴팁처럼 **여닫이가 있는 것**의 이야기입니다. 이건 읽기만 하므로 경합할 상대가
없고, 손가락으로 끌어도 값이 따라 움직입니다(`pointerup` 에서 지웁니다).

**좌표는 `LIFES` 배열에 담고 `<figure data-life>` 가 첨자만 듭니다.** SVG 속성에
넣으면 111분기짜리 스물몇 개가 HTML 에 통째로 실립니다. `render()` 가 다시 돌 때마다
(언어 전환 포함) 비웁니다 — 안 비우면 계속 불어납니다.

**정정 공시가 뜨면 텔레그램으로 알립니다 (2026-09-16, 사용자 요청).**
*"정정공시는 한번 경험해보자, 그게 중요한 거 같아."*

`fetch_13f.py` 가 **지난번 파일에 없던 정정만** 골라 `amend-alert.txt` 를 남기고,
워크플로가 그 파일이 있을 때만 텔레그램을 보냅니다(`hashFiles`). 쪽지에는 분기·
공시일·접수번호와 **EDGAR 주소**가 들어가므로 바로 열어 볼 수 있습니다.
`.gitignore` 에 넣었습니다 — 저장소에 올릴 것이 아닙니다.

**여전히 자동으로 합치지는 않습니다.** 통째로 다시 쓰는 정정과 빠진 것만 덧붙이는
정정이 있는데 둘을 반대로 처리하면 종목이 두 배가 되거나 통째로 사라집니다.
실물을 한 번도 못 봤으므로 **사람이 한 건을 열어 보게 알리는 것**이 지금의 최선입니다.

**시험을 만들다가 진짜 버그를 하나 잡았습니다.** `amended_by` 표시를 **새로 내려받는
분기에만** 붙이고 있었습니다. 공시는 한 번 받으면 다시 안 받으므로, **나중에 뜬
정정은 파일에 영영 안 적혔습니다.** 그러면 같은 정정이 **매주 '새 것'으로** 울립니다 —
6-2 의 *"매주 실패하면 아무도 안 봅니다"* 가 그대로 생기는 자리입니다.
지금은 `quarters.sort()` 뒤에 **모든 분기에 다시 붙입니다**(없어진 정정은 지웁니다).

SEC 없이 네 경우를 다 돌려 확인했습니다(`list_filings` 만 흉내로 교체).

```
① 새 정정 없음      쪽지 없음   ← 매주 도는 평소
② 정정이 새로 뜸    쪽지 있음
③ 그 다음 주        쪽지 없음   ← 여기가 위 버그를 잡은 자리
④ 첫 실행(옛 파일 없음)  쪽지 없음   ← 28년치가 통째로 '새 정정'이 되면 안 됨
```

**실패 알림을 `13F 받기` 단계에 콕 집었습니다**(`steps.fetch.outcome`). 정정 알림
단계는 일부러 `continue-on-error` 를 안 붙였는데 — 알림이 조용히 실패하면 그 정정은
파일에 이미 '아는 것'으로 적혀 다시는 안 울리기 때문입니다 — 그냥 `failure()` 로
두면 **"13F 받기가 실패했습니다"** 라는 거짓 경보가 갑니다(9-3 에 적힌 그 사고).

**화면은 아직 정정을 표시하지 않습니다.** `amended_by` 는 자료에만 있고 `index.html`
쪽에서 한 번도 안 읽습니다(실측: 0건). 정정을 실제로 한 건 보고 나서 정할 일입니다.

**차트에 기간 버튼을 달았습니다 (2026-09-16, 사용자 요청).**
*"그래프를 1년/3년/5년/10년/15년 이렇게 좀 나눠서 볼 수 있으면 더 좋겠는데?"*
`1년 3년 5년 10년 15년 전체` 여섯입니다. **`전체` 를 같이 둡니다** — 그게 지금까지의
모습이고 이 차트의 이름(일생)이기도 해서, 빼면 있던 것이 없어집니다. 기본값도 `전체`.

**26줄이 함께 움직입니다.** 기간은 그 종목의 성질이 아니라 *보는 방법*이라, 줄마다
따로 놀면 위아래를 견줄 수 없습니다. 4번 대시보드에서 위·아래 버튼 줄을 묶어 둔 것과
같은 판단입니다. 고른 값은 `localStorage` 의 `bk.range` 에 남습니다.

**버튼은 차트 안에 둡니다 (머리글이 아니라).** 4번의 *"변하는 것은 줄에, 안 변하는
것은 머리에"* 를 그대로 따르면 머리글로 가야 하는데, **차트가 안 펼쳐졌을 때
가리킬 것이 없는 죽은 버튼**이 목록 맨 위에 남습니다. 그래서 값은 전역이되 버튼은
펼친 차트 안에만 나옵니다.

**이력보다 긴 기간은 못 누르게 합니다.** 애플은 10.5년이라 `15년` 이 흐려져 있습니다 —
누르면 `전체` 와 똑같은 그림이 나오면서 이름표만 거짓말을 합니다. **감추지 않고
흐리게** 둡니다(줄마다 버튼 개수가 달라지면 목록이 들쭉날쭉해집니다). 저장된 값이
그 줄에서 못 쓰는 것이면 **그 줄만** `전체` 로 떨어집니다 — 값을 고쳐 놓지 않으므로
코카콜라(28년)는 `15년` 그대로입니다(실측으로 확인).

**자르는 쪽은 늘 과거입니다.** 가장 최근 분기는 어느 기간에서도 보여야 합니다.
잘라서 점이 하나만 남으면 선이 안 그려지므로 둘이 될 때까지 되물립니다 — 이번 분기에
들어온 종목에서 실제로 일어납니다.

**`preData` 가드는 전체 이력의 첫 분기에만 겁니다.** 잘라 낸 창의 첫 점에 걸면
창을 옮길 때마다 엉뚱한 분기의 막대가 사라집니다(`o===ALL[0]` 으로 봅니다).

**줄을 통째로 다시 그리지 않습니다.** `chartHTML` 을 `chartBody`(그림) 와 껍데기로
나눠서, 버튼을 누르면 **그림과 아래 한 줄만** 갈아 끼웁니다. 다시 그리면 펼쳐 둔 줄이
접히고 방금 누른 버튼이 사라져 키보드 초점을 잃습니다(7번의 "버튼은 한 번만 만들고
이후엔 `aria-pressed` 만 옮깁니다"와 같은 자리).

**짧은 이력을 펴는 한도를 4배 → 8배로 올렸습니다.** `1년`(4분기)이 230px 짜리
우표가 되기 때문입니다. 지금은 374px 이고, 3년 이상은 전부 806px(카드 폭)입니다.

**`li` 를 함수 안에서 다시 선언해서 화면이 통째로 비었습니다.** `chartBody(r, li)` 의
매개변수인데 옛 코드의 `const li=LIFES.length;` 가 남아 있었습니다 —
`Identifier 'li' has already been declared`. **`tag`/`flag` 때와 똑같은 사고이고
이 파일에서 세 번째입니다.** 함수를 쪼갤 때는 옮긴 쪽의 지역 변수가 새 매개변수와
겹치는지 보세요.

**SVG 안에서는 `hidden` 속성이 안 먹습니다 (같은 회차에 찾음).**
십자선 `<g class="cross" hidden>` 이 **실제로는 그려지고 있었습니다.** 자리를 아직
안 정했으니 `x=0`, `cy=0` — **모든 차트 왼쪽 위에 세로줄 하나와 점 반쪽**이
붙어 있었습니다. 지금은 CSS 클래스(`.cross{display:none}` / `.cross.on`)로 켜고 끕니다.

**못 잡은 이유가 더 중요합니다.** 시험이 `g.hasAttribute("hidden")` 를 보고 있었습니다 —
**속성은 제대로 붙어 있었으므로 통과했습니다.** 화면을 찍어 보고서야 나왔습니다.
**"숨겼다"를 잴 때는 속성이 아니라 `getComputedStyle().display` 나 실제 사각형을
보세요**(고친 뒤 26개 전부 `display:none` · 그려진 넓이 0 으로 다시 쟀습니다).

**재 본 것**: JS 오류 0 · 버튼 여섯 × 두 언어 전부 동작 · 다른 줄이 같이 따라감 ·
저장·복원됨 · 못 쓰는 기간이 저장돼 있으면 그 줄만 전체로 떨어짐 · 이상한 값이
들어 있어도 전체로 돎 · 가로 넘침 320/360/390/900px × 한국어·영어 전부 0px.

**차트를 다섯 군데 더 고쳤습니다 (2026-09-16, 사용자가 화면 두 장을 나란히 보내 줌).**
자기 화면과 벤치마크(stockcircle)를 같이 캡처해서 보내 줬습니다. **다섯 다 맞는 지적입니다.**

**1. 눈금이 자가 아니었습니다.** 옥시덴탈 축이 `2019 2020 2021 …` 인데
**2019↔2020 만 42px 이고 나머지는 107px** 이었습니다. 맨 왼쪽 눈금을
*처음 산 분기* 자리에 박고 있었기 때문입니다(그 종목은 2019년 3분기에 들어왔습니다).
지금은 **달력 경계에만** 찍습니다 — 4번의 *"x축 눈금은 달력 경계에 놓습니다"* 와
같은 규칙이고, `2025` 가 두 번 나왔던 그때와 같은 병입니다.

**간격도 픽셀로 정합니다.** 한 분기가 `PX` 이므로 60px 을 넘는 가장 작은 분기
배수를 고르고, 1년 이상이면 1월 자리에만 연도를 적습니다. 그보다 잦으면
`25 Q4` 처럼 **분기**를 적습니다 — `1년` 창에서 연도만 적으면 눈금이 한두 개뿐입니다.

**2. `1년` 만 차트가 짧았습니다.** 분기 수로만 폭을 정해서 4분기면 374px 토막이
됐습니다. 지금은 **칸 폭을 실제로 재서**(`LIFEW`) 어느 기간이든 꽉 채웁니다.
12px/분기 아래로는 안 내려가고, 그보다 좁아질 상황이면 옆으로 끕니다.

**재는 자리가 중요합니다.** `render()` 앞에서 쟀더니 그때는 `#book` 이 아직
`hidden` 이라 `clientWidth` 가 **0** 이었고, 390px 화면도 900px 기본값으로
그렸습니다. 지금은 **카드를 펼친 뒤**(`el("book").hidden=false` 다음) `fitLife()`
가 재고, 달라졌으면 한 번 다시 그립니다. 창 크기가 바뀔 때도 같은 함수를 부릅니다.

**3. 막대 이름표에서 단위를 빼고 숫자를 다 적습니다** (사용자 요청).
`+120.9M shares` → `+120,900,000`. 줄여 쓰면 정확한 주식수를 알 수 없고,
`shares` 는 캡션이 이미 `사고판 주식수` 라고 말하고 있습니다.

**4. 분기말에 다 거래한 것처럼 보였습니다.** *"분명 분기별 공시인데, 마치 분기별
마지막 날에 모든 거래가 다 이루어진 것처럼 표현되어 있어서 좀 별로야."* 맞습니다 —
13F 는 분기말 스냅샷뿐이라 **그 안에서 언제 사고팔았는지는 아무도 모릅니다.**
막대를 분기말 자리에 세우면 화면이 없는 정보를 있는 척합니다.

지금은 **그 분기 한가운데**에 세웁니다. 폭은 분기 하나를 덮되 **44px 에서
끊습니다** — 안 끊으면 `1년` 처럼 넓게 편 화면에서 한 분기가 240px 이 되어
막대가 아니라 **굵은 줄 하나**로 보입니다(처음 만들었을 때 실제로 그랬습니다).
각주에도 한 줄 적었습니다.

**5. 값을 짚은 자리에 띄웁니다.** *"차트 구석에 표시되니까 도저히 못알아보겠어."*
캡션 줄 오른쪽에 두었던 것을 **점 옆 말풍선**으로 옮겼습니다. 그림 안(SVG)이 아니라
**위에 얹는 HTML** 이라 바탕을 깔 수 있고 잘리지 않습니다. 기준 상자가
`.lifescroll` 이라 옆으로 끌면 같이 따라갑니다.

**뒤집는 기준은 그림 전체가 아니라 보이는 창입니다** — 전체 폭으로 재면 옆으로
끌어 놓은 28년치 차트에서 말풍선이 그림 안에는 있는데 화면 밖으로 나갑니다
(코카콜라로 900·390px 각각 여섯 자리를 재서 창 밖 0건).

**손가락으로 탭하면 값이 남아 있어야 합니다.** `pointerup` 에서 지우고 있었는데
탭은 누르자마자 떼는 것이라 **떴다가 바로 사라져 휴대폰에서는 아무것도 못
읽었습니다.** 지금은 마우스가 빠져나갈 때만 지우고, 손가락은 **다른 데를 누를 때**
지웁니다(문서 캡처 단계에서 한 번만 답니다).

**그 시험도 처음엔 거짓으로 통과했습니다.** 탭 자리를 `boundingBox()` 로 잡았는데
차트가 화면 아래에 있어 **이벤트가 하나도 안 들어왔고**, 그래서 "안 된다"가
아니라 "아무 일도 없다"로 나왔습니다. 8번에 이미 적혀 있는
*"십자선·호버를 테스트할 때는 먼저 `scrollIntoViewIfNeeded()`"* 를 제가 안 지켰습니다.

**맨 첫 분기 막대는 왼쪽으로 뻗을 자리가 없습니다** — 안 자르면 그림 밖으로
반쯤 나가 잘립니다(실측에서 잡음). `mid()` 가 `LEFT` 아래로 안 가게 막습니다.

**재 본 것**: JS 오류 0 · 차트 26/26 · 연도 겹침 0 · 이름표가 그림 밖으로 0건 ·
말풍선이 보이는 창 밖으로 0건(900·390px) · 손가락 탭으로 값 읽힘 ·
가로 넘침 320/360/390/900px × 한국어·영어 전부 0px · 글자 대비 전부 AA ·
새 분기 시뮬레이션 그대로 통과.

**화면을 틀로 바꿨습니다 (2026-09-17, 사용자 요청).**
*"이 화면은 현재 버크셔 해서웨이의 것이지만 다른 투자자를 추가할 때마다 같은
서식으로 해야 하거든. 앞으로 모든 수정은 이 점을 감안해야 해."*

파일 맨 위에 **`window.TITAN` 블록 여섯 줄**을 뒀습니다 —
`slug` · `cik` · `data` · `name{en,ko}` · `since`. 제목·설명·`h1`·`canonical`·
각주·자료 경로가 전부 여기서 나옵니다. 그래서 사전 항목이 문자열이 아니라
**함수**(`docTitle:(n,y)=>…`)입니다. `LIVE`·`STATS`·`SUPPORT`(5번)와 같은
요령이고, **설정은 한 블록에 모으고 나머지는 그것만 읽습니다.**

**틀이 진짜 도는지 재 봤습니다.** 폴더를 복사해 **블록 여섯 줄만** 바꾸고
띄웠더니 제목·`h1`·`canonical`·각주가 전부 따라왔습니다(JS 오류 0 · 차트 26개).
*이 확인을 안 하면 "틀로 만들었습니다"는 주장일 뿐입니다.*

한 명 더 넣는 절차는 `docs/design-system.md` 12-2 에 다섯 줄로 적어 뒀습니다.
**앞으로 이 화면을 고칠 때는 "버크셔에서 맞나"가 아니라 "여덟 명 누구에게나
맞나"로 보세요.**

**공통 화면을 실제 단일 파일로 분리했습니다 (2026-09-19 · Codex).** 처음의
`window.TITAN` 방식은 설정은 공통이었지만, 새 투자자를 만들 때 117KB짜리 HTML을
통째로 복사해야 했습니다. 그러면 이후 디자인을 투자자 수만큼 반복 수정하게 됩니다.
이제 공통 CSS는 `titans/shared/investor.css`, 공통 화면 구조·한국어/영어 문구·차트·
계산은 `titans/shared/investor.js`에 한 벌만 있습니다. 투자자별 `index.html`은 고유
`window.TITAN` 설정과 canonical/hreflang/OG, 검색엔진용 정적 영문 `h1`·소개만 둡니다.
JavaScript가 시작되면 공통 화면을 붙이고 설정의 이름·자료·CIK·첫 연도를 채웁니다.
그래서 이후 공통 파일의 디자인 변경은 모든 투자자에게 동시에 적용됩니다.

기존 CSS 내용과 화면 동작 코드는 분리 전 main과 바이트 내용으로 대조했습니다
(투자자 고정 기본값·설명 주석 제외). 버크셔의 20개 Python·19개 Node 검사, 가짜
`Example Capital/샘플 투자사` 설정의 공통 계산, 실제 브라우저 한국어/영어 전환·
26개 차트·차트 펼침·가로 넘침 0·콘솔 오류 0을 확인했습니다. 공통 파일에는
버크셔 이름이나 `berkshire.json` 경로가 없습니다. 320/360/390px의 이전 디자인
검증 결과는 CSS가 동일하므로 유지되지만, 이번 분리 후 실제 모바일 브라우저 폭을
다시 찍었다고 보고하지는 않습니다. 여러 투자자의 13F 수집 자동화는 다음 별도 작업입니다.

**글자 크기를 토큰 여섯 개로 다시 짰습니다 (2026-09-17, 사용자 지적).**
*"어떤 글자 크기는 너무 작아. 제목은 적당한 거 같은데 말이야."* 재 보니
**각주 13문단이 11px 고정폭**이었고, 9.5 / 10 / 10.5 / 11 / 12 / 13 / 15 / 20 이
뒤섞여 있었습니다 — 0.5px 차이는 눈에 안 보이면서 고칠 곳만 늘립니다.

**바닥을 11px 로 잡았습니다.** 애플 HIG 가 *어떤 글자도 11pt 아래로 두지 말라*
고 하고, 머티리얼의 제일 작은 칸도 11sp 입니다. WCAG 는 크기를 안 정하므로
(1.4.4 는 200% 확대만 요구) 이 둘이 실질 기준입니다. 읽는 글은 13.5px 로
올렸고 **제목은 안 건드렸습니다**(사용자가 괜찮다고 했습니다).

```
--t-micro 11   --t-label 11.5   --t-small 12.5
--t-text  13.5 --t-num   14     --t-name  16
```

**크기를 올리면 칸 폭을 다시 재야 합니다.** 12 → 14px 로 올리자 영어 목록의
세로줄이 **18~23px 씩 흔들렸습니다.** 원인이 둘이었습니다.

- 최소 폭이 옛 크기 기준 → 제일 긴 내용을 실제로 재서 다시 잡음
- **`.ret` 와 `.note` 의 폭이 달랐습니다.** 가운데 칸이 `justify-content:center`
  라 셋째 칸만 넓은 줄이 섞이면 **덩어리 전체가 밀립니다.** 같은 자리에 번갈아
  오는 칸은 폭을 같게 두세요.

**자리가 모자라면 되풀이되는 단위를 먼저 버립니다.** 영어 섹터가 둘째 줄로
밀려났는데 `227.9M shares` 의 `shares` 를 떼니 해결됐습니다 — 열 머리글이 이미
`SHARES HELD` 입니다. 한국어 `주` 는 못 뗍니다(`2.28억` 만으로는 뜻이 안 섭니다).

**문구를 손봤습니다 (2026-09-17, 사용자 지정).**

```
늘림 → 확대 · 줄임 → 축소 · 그대로 → 유지
"가장 많이 산 것은 Alphabet Inc 입니다 — 분기말 가격으로 약 $17.2B."
  → 최대 매수   Alphabet Inc            $17.2B 추정
"26종목 중 14종목은 한 주도 움직이지 않았습니다."  → 삭제
"작은 16종목 더 보기 · 전체의 9.0% · 늘림 4 · 줄임 5"  → "더 보기"
```

**줄글을 세 칸으로 바꿨습니다** — 바로 아래 사건 줄(`재진입`/`전량매도`)과 같은
짜임(이름표 · 이름 · 값)이라 둘이 한 표로 읽힙니다. `untouched` 줄을 뺀 것은
**바로 위 계기판의 `유지` 가 이미 같은 숫자를 적고 있어서**입니다.
사건 두 줄 사이도 띄웠습니다(`.ev+.ev`).

**차트 기간을 `1년 5년 10년 15년` 네 개로 줄였습니다** (`3년`·`전체` 삭제, 사용자 판단).
기본값은 15년입니다.

**창이 그 종목의 이력이 아니라 달력이 됐습니다 (사용자 요청).**
*"15년을 클릭할 시에 이 종목의 15년 차트를 보여줘서, 이 투자자가 어느 시점에
이 종목에 들어갔고 어느 시점에 탈출했는지를 보여주고 싶어."*

전에는 들고 있던 구간만 잘라 보여 줘서, 2년 들고 있는 처브를 `15년` 으로 봐도
2년치만 나왔습니다. 지금은 **가장 최근 분기에서 N분기 뒤로** 창을 잡고 그 안에서
들고 있던 구간만 선이 그려집니다 — 처브는 15년 축 오른쪽 끝에 2년치 선만 있고,
그 시작점이 곧 **언제 들어왔는지**입니다.

**26줄이 같은 창을 씁니다.** 그래서 줄끼리 가로축이 맞고 "누가 먼저 샀나"도
견줘집니다. **버튼을 흐리게 만들 이유도 없어졌습니다** — 축이 달력이라 어느
기간이든 뜻이 있습니다(목록의 26줄은 전부 지금 보유 중이라 값이 늘 있습니다).

**"차트가 너무 듬성듬성해"** — 가격 눈금선을 넣었습니다. 선 하나만 떠 있으면
높이를 읽을 자가 없어 빈 칸처럼 보입니다. 값은 1·2·2.5·5 × 10ⁿ 중에서 고르되
**줄 수가 네 개에 가장 가까운 것**을 고릅니다 — "raw 보다 큰 첫 값"으로 고르면
애플처럼 값이 12배 벌어진 종목에서 줄이 두 개밖에 안 남습니다(실측으로 잡음).
**이름표는 왼쪽 여백으로 빼지 않고 선 위에 얹습니다** — 여백을 늘리면 28년치가
더 좁아집니다. 가격 칸도 96 → 124px 로 키웠습니다.

**재 본 것**: JS 오류 0 · 차트 26/26 · 눈금선 3~5개(2개 미만 0건) ·
세로줄 흔들림 0(한국어·영어) · 연도 겹침 0 · 말풍선이 창 밖으로 0 ·
손가락 탭으로 값 읽힘 · 가로 넘침 320/360/390/900px × 두 언어 전부 0px ·
대비 전부 AA · 9.5~10.5px 글자 0개(바닥 11px) · 가짜 투자자로 틀 확인 통과.

**차트를 아홉 군데 고쳤습니다 (2026-09-17, 사용자가 번호를 매겨 지적).**
*"듬성듬성의 뜻을 니가 잘못 이해했네."* — 앞 회차에서 제가 **눈금이 없다**는
뜻으로 읽고 가격 눈금선을 넣었는데, 사용자 말은 **점이 성기다**는 뜻이었습니다.
넣었던 눈금선을 도로 뺐습니다.

**1. 가격 눈금선을 뺐습니다.** 높이를 읽을 자가 필요하면 선 끝의 현재값과
아래 한 줄의 최저·최고가 말하고, 짚으면 말풍선이 그 자리에 적습니다.

**2. 가로축을 `2026.03.31` 로.** `26 Q1` 이었습니다 — **`Q1` 은 주린이에게
통하는 말이 아니고**, 연도만 적으면 그 점이 그 해의 어느 분기인지 알 수
없습니다. 13F 는 실제로 그날의 스냅샷이라 그날을 그대로 적는 것이 가장
정직합니다. 이름표가 열 글자(66px)로 길어졌으므로 **눈금 간격도 같이
늘렸습니다**(`LW+16` 을 넘는 가장 작은 분기 배수). 두 언어가 같은 꼴을 씁니다.

**3. 막대 이름표를 `+136.37M` 으로 줄였습니다.** 앞 회차에 사용자가
*"숫자는 다 보여줬으면"* 이라고 해서 `+136,373,000` 으로 적었는데, 이번에는
*"너무 디테일하게 쓰는데 대충 줄여줘"* 였습니다 — **소수 둘째 자리까지**가
조건입니다(`compShares`). 말풍선도 같이 바꿨습니다.

**4. 캡션 줄을 지웠습니다.** `위 칸 — 분기말 가격 · 아래 칸 — 그 분기에 사고판
주식수` 에 대해 *"무슨 뜻인지도 모르겠어"* 였습니다. 두 칸이 무엇인지는 값
(달러 · 주식수)과 말풍선이 이미 말합니다.

**5. 창 밖의 거래를 창 안에 그리고 있었습니다 (진짜 버그).**

```
1년 · 애플   축 왼쪽 끝 2025.09.30   그 자리에 −41,787,236 막대가 반쯤 잘린 채
```

그 분기에 적힌 증감은 **직전 분기말과 그 분기말 사이**의 일이라, 축이
`2025.09.30` 에서 시작하면 그 거래는 **그 날짜 이전**입니다. 세울 자리가 없어
왼쪽 끝에 붙어 잘렸고 눈금과도 안 맞았습니다(사용자가 정확히 짚었습니다).
지금은 **창의 첫 분기에는 막대를 안 세웁니다.**

**6. 대신 창 이전 거래를 반투명 카드로 요약합니다** (사용자 요청) —
`이전에 있었던 거래 · BUY 14건 · SELL 11건`. 안 그리면 "이 기간에만 움직였다"로
읽히고, 그리면 창 밖의 일이 창 안에 들어옵니다. 세어서 말로 적는 것이 둘 다
피하는 길입니다.

**카드는 SVG 가 아니라 위에 얹는 HTML 입니다**(말풍선과 같은 요령). SVG 로
그리면 바탕 상자 폭을 글자 수로 어림해야 하는데 **한글은 고정폭에서도 영문의
두 배**라 어림이 양쪽으로 다 빗나갑니다 — 실측으로 한국어는 28px 모자라고
영어는 15px 남았습니다. HTML 은 제 글자만큼 넓어집니다.
**반투명은 바탕만 줍니다** — 요소째로 `opacity` 를 내리면 글자까지 옅어집니다.

**7. 축의 왼쪽 끝을 창의 왼쪽 끝에 못박았습니다.** 창 안에 점이 하나뿐일 때
바로 앞 점을 끌어오고 `gA=Math.min(g0, L[0].gi)` 로 축을 넓히고 있었습니다 —
`1년` 을 골라도 축이 1년보다 길어집니다. 지금은 **창이 곧 축**이라 이력이 짧은
종목도 창을 꽉 채우고, 점이 하나면 선 대신 점만 찍습니다(D R 호턴).

**8. 옥시덴탈의 `10년`·`15년` 이 깨져 보이던 이유 — 자료가 아니라 화면이었습니다.**
2019년에 들어와 **2020년 1분기에 전량 매도**하고 2022년에 다시 샀는데, 선이
그냥 허공에서 끊겼다가 허공에서 다시 시작했습니다. 게다가 그 토막의 끝이
2020-03-31 의 **$11.58**(코로나 바닥, 실제 값입니다)이라 곤두박질친 조각이
떠 있는 꼴이 됐습니다. 자료는 맞는데 화면이 **"여기서 팔았다"를 말하지
않은 것**입니다.

지금은 **들어온 자리와 나간 자리에 표식**을 찍습니다(사용자 요청 — "매수한
시점과 매도한 시점을 보여줘야 하고"). 들어온 자리는 실선 동그라미, 나간 자리는
**점선 동그라미**입니다. 속은 안 채웁니다 — 채우면 점이 선보다 먼저 읽히고,
매매 막대가 이미 색을 쓰고 있어 여기까지 칠하면 한 그림에 색이 셋이 됩니다.

**나간 자리는 마지막으로 들고 있던 분기에 찍습니다.** 그 다음 분기 공시에는
이름이 없으니 실제로 판 것은 그 사이인데, 다음 분기 자리에 찍으면 **자료에
없는 날**을 가리킵니다.

**9. 목록 줄의 `BUY`/`SELL` 스티커를 뺐습니다** (사용자 — *"글자가 커지니까
별로 뵈기가 싫으네"*). 대신 **숫자 자체를** 매수 `--up` · 매도 `--down` 으로
칠합니다. 원래 스티커를 둔 이유는 *"한 줄에 색깔 숫자가 둘이 되면 어느 쪽이
돈 이야기인지 흐려진다"* 였는데, **둘은 부호가 다릅니다** — 증감은 `+`/`−`,
수익률은 `%`. 열 머리글도 `이번 분기`·`추정 수익률` 로 갈라 적고 있습니다.

**스티커를 지우면서 세 칸의 빈 줄 자리(`::before` 14px)도 같이 지웠습니다.**
그 14px 은 이 칸이 두 줄이던 시절에 이웃 칸 숫자를 같은 높이에 맞추려던
것입니다 — 한 칸만 남기면 이번엔 반대로 어긋납니다(7번의 "지운 블록의 CSS 가
남아 화면을 밀어낸다"와 같은 자리). 실측으로 네 칸 세로 흔들림 0px 입니다.

**못 한 것이 하나 있습니다: 주별 차트.** 사용자가 *"기간 차트 1년이랑 5년은
분기별이 아닌 주별로 차트를 찍어줘"* 라고 했는데, **13F 에는 분기말 스냅샷밖에
없습니다.** 주별 가격선을 그리려면 종목마다 야후에서 주봉을 따로 받아야 하고,
그러면 (가) 9-3 의 이점인 *"퍼블릭 도메인 한 출처로 자동으로 돈다"* 가 깨지고
11번의 라이선스 문제가 따라붙으며, (나) 가격선이 추정 매수가를 계산하는
그 계열이 아니게 되어 **화면의 두 숫자가 서로 다른 자료가 됩니다.**
사용자가 앞서 *"수익률을 현재가 기준으로는 하지 말자"* 로 같은 이유를 이미
물리친 적이 있어(2026-09-16) **임의로 넣지 않고 알렸습니다.**
매매 막대는 주별로 바꿔도 분기 그대로입니다 — 분기 안에서 언제 사고팔았는지는
13F 가 말하지 않습니다(사용자가 지난 회차에 정확히 지적한 그 자리).

**영어 `--up` 이 비중 막대 위에서 4.49 인 문제가 한 칸 늘었습니다.** 증감
숫자에도 색이 생겼기 때문입니다. **값도 자리도 제가 만든 것이 아니고**
(`--up #1B7F4B` 는 대시보드·재료표와 같이 쓰는 토큰), 고치려면 두 화면을 함께
다시 재야 해서 그대로 뒀습니다 — 2026-09-15 에 내린 판단과 같습니다.
한국어는 미달 0건입니다.

**재 본 것** (26줄 전부 펼친 상태 · 기간 네 개 × 두 언어):
JS 오류 0 · 차트 26/26 · 축 이름표 겹침 0 · 그림 밖으로 나간 이름표 0 ·
왼쪽 끝에 잘린 막대 0 · 0선 방향 어긋남 0 · 반투명 카드 삐져나감 0 ·
말풍선 창 밖 0 · 손가락 탭으로 값 읽힘 · 칸 세로 흔들림 0px(두 언어) ·
가로 넘침 320/360/390/900px × 두 언어 전부 0px · 새 요소 대비 전부 AA.

**머리글에 이름표를 달고 투자자 이름을 떼어 냈습니다 (2026-09-20, 사용자 요청).**
*"버크셔 해서웨이 글자를 강조해주고, 색이든 뭐든 구분해줬으면 좋겠어. 그리고 그 앞에 로고를 추가해줘."*

```
전  버크셔 해서웨이 · 최근 공시 기준 보유 종목          ← 한 덩어리, 전부 같은 색
후  [BH] 버크셔 해서웨이 · 최근 공시 기준 보유 종목      ← 이름만 --ink, 설명은 --ink-3
```

**색을 새로 만들지 않았습니다.** 이미 쓰는 잉크 세 단계를 그대로 씁니다 —
이름 `--ink`(종이 위 14.85), 설명 `--ink-3`(4.61, AA 통과). 재료표의
*"색을 늘리지 마세요"* 가 여기서도 걸립니다.

**사용자가 보내 준 로고는 다른 회사였습니다.** 버크셔 해서웨이 홈서비스(BHHS)의
도장 마크인데 **부동산 중개 가맹 브랜드**이고, 13F 를 내는 Berkshire Hathaway Inc.
와 다른 법인입니다. 달았으면 화면이 엉뚱한 회사를 가리켰을 자리입니다. 게다가
등록상표라 이 문서의 *"살아 있는 투자자의 이름을 간판에 걸지 마세요"* 와 같은 종류의
문제가 붙습니다. **어시스턴트가 대신 판단하지 않고 알리고 대안을 만들었습니다.**

**그래서 기본은 머리글자 타일(`BH`)입니다.** 버크셔 해서웨이 본사는 원래 로고라고
할 만한 것이 없고, 앞으로 넣을 헤지펀드들도 대부분 아이콘 세트에 없습니다.
**사람이 로고를 찾아 붙여야 하는 화면은 결국 낡습니다** — 티커에서 사용자가
정확히 짚은 그 문제입니다. 진짜 그림이 필요하면 `window.TITAN.mark` 에 저장소 안
경로를 한 줄 적으면 되고, **비우면 타일로 돌아갑니다**(`LIVE`·`STATS`·`SUPPORT` 와
같은 요령). 그림을 못 받아도 `onerror` 가 같은 타일로 떨어집니다.

**머리글자는 늘 영어 이름에서 뽑습니다.** `monogram()` 이 A-Z 만 보므로 한글 이름을
주면 `?` 가 나오고, 언어를 바꿀 때마다 표식이 달라지면 안 됩니다 — 같은 투자자의
같은 표식입니다. 검사로 못박아 뒀습니다.

**소개 문단을 뺐습니다 (사용자 요청).** `분기말 기록·45일·현금/공매도 미포함·추정·
배당 제외` 를 적던 `.intro` 한 문단입니다. **한계 설명은 하나도 안 사라집니다** —
각주 12줄이 같은 내용을 이미 전부 따로 적고 있습니다(2·3·4·8·10번). 지우기 전에
각주를 읽어서 확인한 것이고, 안 겹치는 것이 하나라도 있었으면 각주로 옮겼을 자리입니다.
사전의 `intro` 키와 `.intro` CSS 도 같이 지웠습니다 — 7번의 *"지운 블록의 CSS 가
남아 화면을 밀어낸다"*.

**다만 크롤러가 받는 본문이 줄었습니다.** 6-2 의 색인 판정("내용이 얇다")이 아직
안 끝난 상태라, `/titans/berkshire/` 가 색인되지 않으면 **정적 HTML 에 본문을 더
넣어야 합니다**(소개 문단을 되살리는 것이 아니라, 이 투자자가 무엇을 했는지를
쓰는 쪽). 지금은 사용자 판단이 먼저입니다.

**각주 12번이 지금 사실과 다릅니다 (이번에 발견, 안 고침).** *"회사 마크는
simple-icons 에서 가져와 이 저장소에 넣어 둔 것입니다. 바깥에서 불러오지
않습니다"* 라고 적혀 있는데, `window.MARKS.url` 이 `api.elbstream.com` 을 가리키고
있어 **종목 줄의 마크는 실제로 바깥에서 받아옵니다**(방문자 IP 가 그쪽으로 갑니다).
머리글 이름표는 바깥에 안 묻지만 각주는 종목 줄 이야기입니다. 고치는 길은 둘 —
각주를 사실대로 고치거나, `MARKS.url` 을 비워 저장소 안 그림만 쓰거나.
**어느 쪽이든 사용자 판단이라 알리고 남겨 뒀습니다.**

**재 본 것**: 가로 넘침 320/360/390/900/1280px × 한국어·영어 **전부 0px** ·
JS 오류 0 · 목록 26줄 그대로 · 이름표가 제목 첫 줄과 나란함(1px) ·
대비 이름 14.85 / 설명 4.61 / 이름표 글자 17.59 전부 AA ·
정적 HTML 의 제목이 JS 가 그리는 것과 **글자 하나까지 같음**(검사로 고정) ·
파이썬 45건 · 화면 20건 통과.

**각주를 절반으로 줄이고 사실관계를 고쳤습니다 (2026-09-21, 사용자 요청).**
*"너무 장황한데, 최대한 간략하게 고치고, 사실과 다른 부분은 바꿔줘. 중요한
순서대로 적어주고, 글자 크기도 너무 커. 이걸 누가 읽겠어."*

```
        문단   크기      글자수(ko/en)   높이(ko/en)
전      14    13.5px    1,453 / 2,682   815 / 1,015px
후       9    12.5px    1,044 / 1,863   513 /   558px
```

**중요한 순서로 다시 세웠습니다.** 맨 위 넷이 *이 화면을 오해하게 만드는 것들*
입니다 — 지금 보유가 아님 · 가진 것 전부가 아님 · 매수가와 수익률은 추정 ·
막대는 '이 사이 어딘가'. 나머지는 확인용이라 아래로 내렸습니다.
전에는 출처가 맨 위였는데, **출처는 의심이 들 때 찾는 것**이지 먼저 읽을 것이
아닙니다.

**각주 12번이 거짓말을 하고 있었습니다 — 이번에 고쳤습니다.**
*"회사 마크는 … 저장소에 넣어 둔 것입니다. 바깥에서 불러오지 않습니다"* 라고
손으로 적혀 있었는데, `window.MARKS.url` 은 줄곧 `api.elbstream.com` 을
가리키고 있었습니다. **방문자 IP 가 그쪽으로 나가는데 화면은 안 나간다고
적고 있었습니다.** 지금은 `markNote()` 가 **설정을 읽어서** 문장을 만듭니다 —
주소가 있으면 어디로 나가는지 밝히고, 비우면 "저장소 것이고 바깥에서 불러오지
않습니다" 로 저절로 바뀝니다. 9-3 의 *"사람이 붙어야 하는 표는 결국 낡습니다"*
가 각주에서 실제로 일어난 자리입니다.

**그 밖에 고친 사실 셋.**

```
전  "13F 는 분기가 끝나고 45일 뒤에 냅니다 … 최소 6주 전의 상태입니다"
후  "분기의 마지막 날 하루를 적은 것이고, 공시는 그로부터 45일 안에 냅니다"
    ← 기한은 '45일 뒤'가 아니라 '45일 안'입니다. 따라서 '최소 6주'도 안 따라옵니다.
전  "배당만큼 실제 결과는 이 숫자보다 낫습니다"
후  "배당도 들어 있지 않습니다"          ← 앞엣것은 사실이 아니라 주장입니다.
전  "미국 상장 주식의 매수 포지션만"
후  "미국 상장 주식만"                   ← 옵션 칸(putCall)은 28년치 전부 비어 있습니다.
```

**주가선 안내는 각주 안으로 들어갔습니다.** 전에는 `paintFoot()` 이 맨 뒤에
덧붙였는데, 그러면 **순서가 두 곳에서 정해집니다.** 지금은 `foot()` 이 날짜를
받아 제자리(다섯째)에 넣습니다 — 순서를 바꾸려면 사전 한 곳만 보면 됩니다.

**크기는 12.5px 입니다.** 2026-09-17 에 11 → 13.5px 로 올렸던 것을 한 칸
내렸습니다. **되돌린 것이 아닙니다** — 그때 올린 이유(11px 는 읽는 글로 작다)는
그대로 두되, 각주는 *읽는 글이 아니라 확인하는 글*이고 문단이 14개나 되어
벽처럼 보였습니다. **11px 바닥은 안 건드립니다.**

**`집계 전` 을 `집계 전부터 보유` 로 (2026-09-21, 사용자 요청).**
*"'집계 전'은 직관적이지 않으니 … 디자인을 해칠까봐 걱정이네."*
걱정이 맞았습니다 — 그 칸은 `min-width` 로 세로줄을 맞춰 두는 자리라,
길어지면 가운데 덩어리가 통째로 밀립니다.

```
집계 전        50px        held before records   160px
집계 전부터 보유 115px  →  칸을 118 / 164px 로 넓힘
```

**넓히는 것 자체는 공짜였습니다**(세로줄 흔들림 0 · 가로 넘침 0). 대신
**좁은 쪽에서 이름이 두 줄로 밀렸습니다.** 그래서 **줄이 한 줄로 서는 문턱을
640 → 810px 로 올렸습니다.** 그 아래는 원래도 쓰던 접힌 배치(가운데·오른쪽이
아래로 내려가는 것)로 가므로 넓힌 폭이 아무 데도 안 걸립니다.

**문턱은 짐작이 아니라 재서 잡았습니다** — 영어로 20px 씩 올려 보며 이름이
안 밀리는 첫 폭이 810px 이었습니다(768px 3줄 · 790px 1줄 · 810px 0줄).
**늘 영어가 문턱을 정합니다**(`held before records` 160px vs `집계 전부터 보유` 115px).

**새 미디어 블록을 640px 블록과 따로 뒀습니다.** 거기에는 여백·제목 크기처럼
*정말 좁은 화면에만* 맞는 것들이 들어 있어서, 태블릿까지 같이 줄일 이유가
없습니다. 줄 배치와 머리글 감추기만 810px 쪽으로 옮겼습니다 —
**옮긴 규칙은 640 블록에서 지웠습니다**(7번의 "지운 블록의 CSS 가 남아 화면을
밀어낸다").

**재 본 것**: 가로 넘침 320/360/390/641/700/767/790/809/810/860/900/1280px ×
한국어·영어 **전부 0px** · 세로줄 흔들림 전부 0 · 이름이 두 줄로 밀린 것 0 ·
JS 오류 0 · 머리글이 809px 이하에서만 사라짐 · 파이썬 45건 · 화면 20건 통과.

**각주를 네 문단으로 줄였습니다 (2026-09-21, 사용자가 문장을 직접 써 줌).**
*"이정도면 충분할 듯."* 앞 회차에서 9문단으로 줄인 것을 사용자가 다시 줄여
**출처 → 자료의 성격 → 추정 → 면책** 네 줄로 정리해 줬습니다.
제목도 `출처와 한계` → **`Disclaimer`** 입니다(두 언어 같은 말).

```
        문단   높이(ko/en)
처음    14    815 / 1,015px
1차       9    513 /   558px
지금      4    208 /   275px      ← 처음의 4분의 1
```

**빠진 것이 화면에서 아주 사라지지는 않습니다.** 지우기 전에 확인한 것입니다 —
`배당 제외` 는 목록 캡션(`costArrow`)이 두 언어 모두 그대로 적고 있고,
기간·날짜는 차트 축이 적습니다. **진짜로 사라진 것은 둘**입니다:
매매 막대가 *'이 사이 어딘가'* 라는 설명과, **회사 마크를 바깥에서 받아온다는
고지**입니다. 마크는 **켠 채로 두기로 사용자가 정했고**(2026-09-21),
각주가 거짓말을 하지는 않게 됐지만 **이제 아무 말도 하지 않습니다.**

**그래서 죽은 코드를 여섯 군데 같이 지웠습니다.** 네 문단은 자료를 한 톨도
안 읽으므로 — `markNote()` · `build()` 의 `splits`·`preNames` · 그 재료인
`SPLITS` 배열(모으기·비우기·`push`) · `FIRSTY` · `paintFoot` 의 인자 ·
**두 번 그리던 것 중 한 번**. 7번의 *"지운 블록의 CSS 가 남아 화면을 밀어낸다"*
와 같은 자리입니다. `SPLITS.push` 만 뺐고 **분할 보정 계산 자체는 그대로**
입니다 — 그 배열은 각주에만 쓰이던 것이었습니다.

**`본 사이트은` 을 `본 사이트는` 으로 고쳤습니다**(사용자가 적어 준 문장의 조사 오타).

**재 본 것**: 가로 넘침 320/360/390/641/809/810/900/1280px × 한국어·영어
**전부 0px** · JS 오류 0 · 목록 26줄 그대로 · 파이썬 45건 · 화면 20건 통과.

**여섯 군데를 손봤습니다 (2026-09-21, 사용자 지정).**

```
전                              후
Disclaimer(한국어)              유의사항                ← 영어는 Disclaimer 그대로
이번 분기                       이번 분기 (2026년 2분기)
상위 10종목만 · 전체의 91.0%     상위 10종목 · 전체 투자 금액의 91.0%
집계 전부터 보유 (한 줄)          집계 전부터 / 보유 (두 줄)
더 보기                         16개 종목 더 보기
비중 막대 위의 직전 분기 눈금      삭제
```

**분기 이름표는 자료에서 셉니다.** `qLabel()` 이 공시 날짜(`2026-06-30`)에서
`2026년 2분기`/`Q2 2026` 을 만듭니다 — 손으로 적으면 새 분기가 들어오는 날
조용히 거짓말이 됩니다(9-3 의 *"사람이 붙어야 하는 표는 결국 낡습니다"*).
`더 보기` 의 개수도 접힌 줄 수에서 옵니다.

**두 줄로 끊는 자리는 `\n` 이 정합니다.** 사전 값이 `집계 전부터\n보유` 이고
CSS 가 `white-space:pre-line` 으로 살립니다. **폭에 맡기면 언어·화면마다 다른
데서 끊깁니다.**

**선택자를 `.mid>.note` 로 써야 합니다.** `.note` 만 쓰면 바로 위
`.mid>span{white-space:nowrap;font-size:…}` 이 이겨서 **두 줄이 안 되고
크기도 안 바뀝니다** — 실측으로 `white-space` 가 `nowrap`, 14px 그대로였습니다.
클래스 하나(0,1,0)로는 클래스+요소(0,1,1)를 못 이깁니다.

**크기를 11.5px(`--t-label`)로 한 칸 내렸습니다.** 14px 두 줄이면 그 줄만
17px 커집니다. `.mid` 가 `flex-start` 라 **첫 줄은 옆 숫자와 나란하고 둘째
줄만 아래로 늘어집니다** — 세로줄은 그대로 섭니다(실측 흔들림 0px).

**칸이 118/164 → 72/96px 로 도로 좁아졌습니다.** 그래서 **문턱(810px)을
640px 로 되돌려 봤는데, 되돌리면 안 됩니다** — 641px 에서 영어 종목 이름이
**10줄** 밀립니다(700px 3줄). 그것은 칸을 넓히기 전부터 있던 것이고 810px
문턱이 지금 그걸 없애 주고 있습니다. **실측해서 그대로 뒀습니다.**

**직전 분기 눈금은 통째로 지웠습니다** — 표식(`.tick`)·설명줄(`.legend`
`prevTick`)·계산(`wPrev`)까지. 눈금만 지우고 설명줄을 남기면 **가리킬 것이
없는 이름표**가 됩니다.

**재 본 것**: 가로 넘침 320/360/390/641/660/700/740/768/790/810/900/1280px ×
한국어·영어 **전부 0px** · 세로줄 흔들림 0 · 이름 두 줄 0 · JS 오류 0 ·
목록 26줄 그대로 · 파이썬 45건 · 화면 20건 통과.

**세 군데를 더 손봤습니다 (2026-09-22, 사용자 지정).**

```
전                                  후
이번 분기 (2026년 2분기)             2026년 2분기 (2026. 04. 01 ~ 2026. 06. 30)
16개 종목 더 보기 (펼친 뒤에도 그대로)  펼치면 16개 종목 숨기기
집계 전부터 / 보유  줄간 1.15          한글만 1.4  (영어는 그대로)
```

**분기 제목이 기간을 그대로 적습니다.** `이번 분기` 라는 말 자체는 뺐습니다 —
`2026년 2분기 (2026. 04. 01 ~ 2026. 06. 30)` 이 이미 *어느 분기인지*와
*언제부터 언제까지인지*를 다 말하므로, 앞에 `이번 분기` 를 또 붙이면 같은 말이
두 번입니다.

**끝날은 계산하지 않습니다.** `qSpan()` 은 **첫날만** 분기에서 세고(4~6월이면
`04-01`), 끝날은 **공시가 적은 그 날짜를 그대로** 씁니다 — 13F 는 분기 마지막
날의 스냅샷이라 그 날짜가 곧 자료의 사실입니다. 둘 다 계산해 버리면 자료와
화면이 따로 놀 수 있는 자리가 하나 생깁니다.

**`fdate`·`Intl` 을 안 씁니다.** 그쪽은 전역 `LANG`·`LOCALE` 을 보는데 `qSpan`
은 사전이 건네는 `lang` 을 받습니다 — 검사가 `D.en` 을 직접 부르면 둘이 어긋납니다.
그래서 영어 달 이름표(`MON`)를 따로 두고 **한 분기는 해를 넘지 않으므로**
연도를 한 번만 적습니다(`Apr 1 – Jun 30, 2026`).

**접기 이름표가 여닫이를 따라갑니다.** 펼쳐 놓고도 `더 보기` 라고 적혀 있으면
그 버튼이 무슨 일을 할지 **거꾸로 읽힙니다.** 바로 위 캡션(`상위 10종목만` ↔
`26종목 전부 펼침`)은 이미 뒤집히고 있었는데 버튼만 안 뒤집혔습니다.
이제 `ontoggle` 한 곳에서 **둘을 같이** 바꿉니다 — 두 군데에서 정하면 갈라집니다.

**한글 줄간은 잰 값이 같아도 다르게 보입니다.** 사용자가 *"영어는 괜찮은데,
한글 기준으로 줄간이 너무 붙어 있어"* 라고 했고, 재 보니 **잉크 간격이 두 언어
모두 정확히 3.21px** 이었습니다(`actualBoundingBoxAscent/Descent`, 8번의 방법).

```
집계 전부터 / 보유   글자 잉크 11px · 사이 3.21px   ← 네모를 꽉 채우는 글자
held before / records  글자 잉크 10px · 사이 3.21px   ← 글자 속이 비어 있음
```

**숫자가 같은데 사용자 지적이 맞습니다.** 한글은 글자가 네모 칸을 꽉 채워서
두 줄이 맞붙은 덩어리로 읽히고, 영문 소문자는 속이 비어 있어 같은 간격이
넓어 보입니다. **잉크 간격만 재면 이 차이가 안 보입니다** — 4번의 *"여백은 글자
상자가 아니라 잉크로 재세요"* 가 여기서는 한 걸음 더 갑니다. CJK 본문 줄간을
1.5~1.8 로 두는 관행이 이 때문이고, 여기는 두 줄짜리 이름표라 **1.4** 로 잡았습니다
(간격 3.21 → 6.1px).

**영어는 안 건드립니다**(`html[lang="ko"]` 로만 걸었습니다). 그리고 **칸만
아래로 5.8px 자라고 줄 높이는 60.5px 그대로**입니다 — `.mid` 가 `flex-start`
라 첫 줄은 옆 숫자와 나란히 있습니다(실측).

**재 본 것**: 가로 넘침 320/360/390/641/700/768/790/810/900/1280px ×
한국어·영어 **전부 0px** · 줄 안 세 칸 첫 줄 어긋남 0(810px 이상 세로줄 1열) ·
이름 두 줄 0 · JS 오류 0 · 목록 26줄 그대로 · 파이썬 45건 · 화면 22건 통과.
320px 한국어에서 가운데 칸이 두 줄로 접히는 6줄은 **main 에서도 똑같이 6줄**
이라 이번 변경과 무관합니다(원본으로 되돌려 재서 확인).

**분기별 보유 변화 표를 달았습니다 (2026-09-22, 사용자 요청).**
*"각 종목별로 전체 트레이드 레코드를 보게하고 싶은데, 별도 페이지가 나을지
아니면 한 화면에 보여줄지 그게 고민이야."*

**먼저 몇 줄인지 세어 보고 정했습니다.** 이 결정은 그 숫자가 전부입니다.

```
26줄  애플            ← 제일 많은 것
19줄  뱅크오브아메리카 · 셰브런      1줄  크래프트하인즈 · 제프리스 · NVR
합계 197줄 · 중앙값 6줄 · 26묶음 중 18개가 10줄 이하
```

**페이지 하나를 채울 분량이 아닙니다.** 그래서 **별도 페이지를 안 만들고** 이미
펼쳐지는 줄 안, 차트 바로 밑에 접어 두었습니다. 이유 넷입니다.

1. **차트가 이미 그 표입니다** — 그림으로 그린 것뿐입니다. 떼어 놓으면 한쪽을
   머리에 담고 다른 쪽을 보러 가야 합니다. 나란히 두면 차트가 전체 모양,
   표가 정확한 숫자로 역할이 갈립니다.
2. **새 자료가 한 톨도 필요 없습니다.** `lifeOf()` 가 차트를 그리려고 이미 만들어
   둔 시계열 그대로이고 **액면분할 보정도 거기서 끝나 있습니다.**
3. **별도 페이지는 색인 판정을 나쁘게 만듭니다.** 6-2 의 "내용이 얇다"가 아직 안
   풀렸는데 **숫자 표만 든 얇은 페이지 26장**을 새로 얹는 꼴입니다.
4. **틀이 깨집니다.** 투자자 8명 × 26종목이면 200장이 넘고 분기마다 종목이
   바뀝니다 — *"사람이 붙어야 하는 표는 결국 낡습니다"* 가 페이지 목록에서 걸립니다.

**`거래 기록` 이라고 쓰지 않습니다.** 13F 는 분기 마지막 날의 스냅샷뿐이라
한 줄은 체결이 아니라 **그 분기 동안의 순변화**입니다. 사용자가 전에 정확히
짚었던 *"분기 마지막 날에 모든 거래가 다 이루어진 것처럼 표현되어 있다"* 와
같은 자리라, 이름은 `분기별 보유 변화`/`Quarter-by-quarter record` 입니다.
검사로 못박아 뒀습니다(제목에 `거래`·`trade` 가 들어가면 실패).

**움직인 분기만 싣습니다.** 코카콜라는 111분기를 들고 있는데 **분할 보정을 하면
주식수가 한 번도 안 바뀝니다** — 다 적으면 같은 숫자가 110줄 되풀이됩니다.
(이 회차 초반에 제가 분할 보정 없이 세어 "코카콜라 2건"이라고 적었는데
**틀렸습니다.** 1건입니다 — 2012년 2:1 분할을 거래로 세고 있었습니다.)

**전량 매도 줄은 가격을 지어내지 않습니다.** 다음 공시는 수량이 사라졌다는 것만
알려 주므로 보유·가격·금액 세 칸을 비웁니다(`—`). 9-3-1 의 *"매도가를 만들어
넣지 않음"* 이 표에서도 그대로입니다. 실측(옥시덴탈):

```
2022.03.31  재진입      1.36억주  $56.74  $7.7B
2020.06.30  전량매도    —         —       —
2019.12.31  +1,147만주  1,893만주 $41.21  $780M
2019.09.30  진입        747만주   $44.47  $332M
```

**첫 공시에 이미 있던 종목은 `집계 전부터 보유` 입니다.** 거기 `진입` 이라고
적으면 그 분기에 샀다는 거짓말이 됩니다 — 목록 칸과 같은 판단입니다.

**기간 버튼을 안 탑니다.** 차트는 고른 창을 보여 주고 이 표는 전부입니다.
`drawLife()` 가 `.lifescroll` 안만 갈아 끼우므로 그 **형제**인 표는 그대로 남습니다 —
기간을 바꿔도 다시 그릴 것이 없습니다(검사로 고정).

**좁은 화면에서 다섯 칸이 안 섭니다 — 재서 고쳤습니다.**

```
320px 에서 쓸 수 있는 폭 262px   표가 필요로 한 폭  한국어 408 · 영어 533
```

셋을 손봤습니다. ① `평가 금액` 칸을 감춥니다 — **보유 주식수 × 분기말 가격**이라
바로 옆 두 칸에서 나오는 값이고 다섯 중 제일 덜 말합니다. ② 머리글과 **말로 된
이름표만** 줄을 바꿉니다(`Held before records` 가 한 줄이면 155px). ③ 그래도
넘치면 옆으로 끄는데 **날짜 칸을 왼쪽에 붙여 둡니다** — 안 붙이면 끌었을 때
지금 보는 줄이 어느 분기인지 알 수 없습니다.

```
고친 뒤   360px 부터 두 언어 모두 딱 맞음(옆으로 끌 것 0)
          320px 만 한국어 272 · 영어 288 로 조금 넘침 → 날짜 칸 고정한 채 옆으로 끌기
```

**세로 높이는 넓은 화면에서만 제한합니다**(340px · 머리글 고정). 좁은 화면에서는
풉니다 — **손가락이 상자 안에 닿으면 페이지가 안 넘어갑니다.** 지금 26개 중 7개만
세로로 흐릅니다(11줄 넘는 것).

**색은 변화 칸에만 씁니다.** 가격·금액까지 칠하면 한 줄에 색깔 숫자가 셋이 되어
어느 것이 행동이고 어느 것이 돈인지 흐려집니다(재료표 13). 표는 흰 카드 위에
그려지므로 **비중 막대 위 4.49 문제가 여기서는 안 생깁니다** — 영어 `--up` 이
5.02 입니다(실측).

**재 본 것**: 가로 넘침 320/360/390/641/700/810/900/1280px × 한국어·영어
**전부 0px** · 표 안 세로줄 흔들림 0 · JS 오류 0 · 표 26개 · 줄 합계 197 ·
새 글자 대비 전부 AA(5.02~17.59) · 11px 미만 글자 0개 ·
파이썬 45건 · 화면 25건(검사 3건 추가) 통과.

**그 표를 단추로 바꿨습니다 (2026-09-22, 사용자 지적).**
*"클릭하면 뭐가 나올지 알 수가 없게끔 만들어져 있네 … 빨간색이 좋을려나?"*

```
전   ▸ 분기별 보유 변화 26건        ← 11.5px 대문자 회색 — 캡션처럼 생김
후   [▸ 분기별 보유 변화 보기]  26개 분기   ← 테두리 단추 + 단추 밖 건수
     [▾ 분기별 보유 변화 숨기기]              ← 펼치면 이 글자로
```

**빨강은 안 썼습니다 — 사용자가 물어서 이유를 대고 정했습니다.** 이 화면에서
빨강·초록·파랑은 이미 상승·하락의 뜻이고 **언어를 따라 뒤집힙니다**(6번).
바로 아래 표의 `변화` 칸이 그 색을 쓰므로, 단추까지 칠하면 **한국어에서는
'매수', 영어에서는 '매도'로 읽히는 단추**가 됩니다. 조작 장치인데 값처럼 보이는
자리이고, 재료표 13 의 *"색을 늘리지 마세요"* 와 9-4-6 의 *"색 버튼을 놓지
마세요"* 에도 걸립니다.

**문제는 색이 아니라 모양이었습니다.** 11.5px 대문자 회색 글자는 이 화면에서
**캡션의 생김새**입니다. 그래서 **바로 위 기간 버튼(`.seg`)과 같은 옷**을
입혔습니다 — 테두리 1px · 카드 바탕 · `--cond` 600 · `--ink-2` · 대문자와
넓은 자간 삭제. 펼친 줄 안에 이미 단추가 있으니 **새 어휘를 하나도 안 만들고**
"이건 누르는 것"이 읽힙니다.

**건수는 단추 밖입니다.** 누르기 전에 분량을 알려 주는 값이지 단추의 이름이
아닙니다. 좁은 화면에서 자리가 모자라면 **건수만 아래로 내려가고 단추는 한 줄로
남습니다**(영어 320·360px 에서 실제로 그렇게 됩니다 — 단추가 두 줄로 쪼개진 것 0건).

**글자는 JS 로 안 갈아 끼웁니다.** 두 이름표를 다 넣어 두고 `details[open]` 이
CSS 로 하나만 보여 줍니다 — 건수가 안 바뀌므로 바뀌는 것이 말뿐이고, 다시 그릴
일이 없어 **키보드 초점도 안 잃습니다**(7번의 "버튼은 한 번만 만든다").

**아래 설명 줄을 지웠습니다** (사용자 — *"이미 disclaimer에서 이야기한 내용이야"*).
맞습니다 — `13F 는 분기 마지막 날 하루만 적습니다` 를 **유의사항 1번이 이미**
적고 있습니다. 뒷부분(`주식수가 그대로인 분기는 뺐습니다`)은 각주에 없는 말이지만
**제목 `분기별 보유 변화` 와 열 이름 `변화` 가 이미 말합니다.** 사전의 `recNote`
키와 `.recnote` CSS 도 같이 지웠습니다(7번).

**재 본 것**: 가로 넘침 320/360/390/641/700/810/900/1280px × 두 언어 **전부 0px** ·
표 안 세로줄 흔들림 0 · 단추가 두 줄로 쪼개진 것 0 · JS 오류 0 · 표 26개 ·
줄 합계 197 · 대비 단추글자 7.45 · 캐럿 5.47 · 건수 5.47 **전부 AA** ·
파이썬 45건 · 화면 26건(검사 1건 추가) 통과.

**사건 줄에 로고와 섹터를 달았습니다 (2026-09-22, 사용자 요청).**
*"재진입 / 전량매도 뒤에 바로 종목 이름이 가는데, 이거 종목 로고랑 카테고리도
적어주면 더 좋겠네."*

```
전   [재진입]   D R Horton Inc                        $581K
후   [재진입]   [HO] D R Horton Inc  주택건설           $581K
     [전량매도] [CB] Constellation Brands Inc  음료   보유 1.5년 · $95M
```

**목록 줄이 쓰는 조각을 그대로 씁니다** — `markHTML()`·`sectorOf()`.
같은 회사가 두 자리에서 다르게 보이면 안 되고, 새 함수를 만들면 나중에 한쪽만
고치게 됩니다.

**전량매도 줄이 걱정거리였습니다** — 그 종목은 이번 분기 목록에 **없습니다**.
그래서 짐작하지 않고 재 봤습니다: `B.out` 의 줄도 `cusip` 과 `key` 를 그대로
들고 있어서(직전 분기 자료를 편 것이므로) 마크는 ISIN 으로 만들어지고 섹터는
앞 여섯 자리로 찾힙니다. 실측으로 둘 다 나옵니다(Constellation Brands → 음료).
못 찾으면 목록과 똑같이 글자 타일로 떨어지고 섹터 칸은 빕니다 —
**틀린 것을 적지는 않습니다.**

**사건 블록을 한 격자로 바꿨습니다 — 이게 이번의 진짜 고침입니다.**
`flex-wrap` 인 채로 마크를 넣었더니 두 가지가 무너졌습니다.

```
1  좁은 화면에서 조각이 흩어짐   320px 에서 마크만 따로 한 줄에 떨어지고
                               이름표와 섹터가 붙어 버립니다(실측)
2  마크가 줄마다 다른 자리       이름표 폭이 다릅니다 — 재진입 48px vs
                               전량매도 60px → HO 와 CB 가 안 맞습니다
```

2번이 특히 이 화면에서는 큰 문제입니다 — **계측기·측량 도면**이라는 언어가
세로줄에서 무너집니다(사용자가 예전에 *"세로 줄이 잘 안맞아"* 라고 짚었던
그 자리입니다). 줄마다 격자를 따로 두면 `auto` 칸이 줄마다 제 내용만큼만
넓어지므로 **한 블록이 격자를 쥐어야** 합니다.

```css
.events{display:grid;grid-template-columns:auto auto minmax(0,1fr) auto}
.ev{display:contents}          ← 줄은 상자를 안 갖고 칸만 빌려 줍니다
```

**`display:contents` 면 그 요소는 상자가 없습니다.** 그래서 두 가지를 같이
옮겨야 합니다 — 줄 사이 여백은 `margin` 이 아니라 부모의 `row-gap` 으로,
글꼴·색은 자식이 직접 받도록(`.ev .sub` 에 `font-family` 를 다시 적었습니다).
**재는 것도 달라집니다**: `.ev` 의 `getBoundingClientRect()` 는 빈 사각형이라
검수 스크립트가 "카드 밖으로 나갔다"고 잘못 읽었습니다 — 자식을 재야 합니다.

**좁은 화면에서는 금액을 이름 칸 아래로 내립니다**(≤640px). 320px 에서
이름표·마크·금액이 먼저 자리를 가져가면 이름에 **44px** 밖에 안 남아 회사
이름이 세로로 쪼개집니다(실측). 금액을 셋째 칸 아래 줄로 내리면 왼쪽 두 칸은
그대로 있고 이름이 제 너비를 갖습니다. 거기서는 이름표·마크를 **첫 줄에
맞춥니다**(`align-items:start`) — 가운데 맞춤이면 석 줄짜리 줄에서 둘이
덩어리 한가운데까지 내려가 허공에 뜹니다.

**재 본 것**: 가로 넘침 320/360/390/641/700/810/900/1280px × 두 언어 **전부 0px** ·
이름표·마크가 두 줄에서 **같은 자리**(열 1개) · 마크 상자가 목록과 같음
(30px, 좁은 화면 26px) · 카드 밖으로 나간 조각 0 · 깨진 그림 0(개발 환경에서
elbstream 이 막혀 있어 둘 다 글자 타일로 떨어짐) · 섹터 2/2 · JS 오류 0 ·
대비 섹터 5.47 · 이름 17.59 · 타일 글자 7.45 **전부 AA** ·
파이썬 45건 · 화면 27건(검사 1건 추가) 통과.

**머리글 아래에 투자자 소개를 넣었습니다 (2026-09-22, 사용자 요청).**
*"왼쪽에 투자자 캐리컬쳐를 넣고 오른쪽에는 투자 철학을 설명하는 설명문과
유명한 명언 2줄 정도를 넣고 싶어."*

```
[ 그림 300×200 ]   워런 버핏은 1965년 … 아래 목록에서 한 줄을 펼치면 …
                   │ "남이 탐욕스러울 때 두려워하고 …"
                   │   2008년 10월 16일 뉴욕타임스 기고
                   │ "우리가 가장 좋아하는 보유 기간은 '영원히' 입니다."
                   │   1988년 주주 서한
```

**글은 공통 파일이 아니라 투자자의 `index.html` 에 글자로 박습니다.** 사전(`D`)에
넣으면 JS 가 그리게 되고 **크롤러가 받는 페이지에서 통째로 사라집니다** — 6-2 의
*"이 글을 사전 `D` 로 옮기지 마세요"* 와 같은 자리입니다. 두 언어를 다 넣어 두고
CSS 가 한쪽만 보입니다(`.about` 과 같은 규칙). 정적 HTML 에 영어 489자·한국어
265자가 늘었고, `/titans/berkshire/` 는 아직 색인 판정을 기다리는 중입니다.

**공통 JS 는 이 덩어리를 옮길 뿐 내용을 모릅니다.** 껍데기가 `body` 를 통째로
갈아끼우므로 **먼저 들어냈다가** 머리글 아래 제자리에 도로 넣습니다. 다음 투자자는
자기 파일에 자기 글과 자기 그림을 씁니다 — 절차는 `docs/design-system.md` 12-2 에
한 줄 더 적었습니다.

**여백은 `margin` 이 아니라 `gap` 입니다.** 두 언어를 다 넣어 두고 한쪽을
`display:none` 으로 감추므로, `*+*` 로 여백을 주면 **감춰진 형제 때문에 첫 줄에도
윗여백이 붙습니다.** `gap` 은 상자 없는 것을 안 셉니다(실측: 카드 위 경계에서
첫 줄까지 21px = 안쪽 여백 20 + 테두리 1, 군더더기 0).

**출처 줄은 고정폭이 아닙니다.** 처음에 `--mono` 로 뒀더니 `2008년  10월  16일` 처럼
띄어쓰기가 두 칸씩 보였습니다 — 6-2 의 *"면책 문구를 고정폭 작은 글씨로 뒀더니
한글 자간이 벌어졌다"* 와 같은 자리입니다. `--cond` 로 바꿨습니다. 영어는 고정폭에서도
멀쩡했으므로 **재 보지 않았으면 한국어만 나쁜 채로 지나갔을** 자리입니다.

**인용은 출처 없이 싣지 않습니다.** 두 줄 다 원문이 어디 있는지 화면에 적혀 있고,
검사가 `<cite>` 없는 인용을 막습니다. 되살려 확인했습니다 — 출처 한 줄을 지우면
28건 중 1건 실패, 도로 넣으면 28건 통과.

**그림이 들어왔습니다 (2026-09-22).** 사용자가 다른 AI 로 세 장을 만들어 와서
고른 것입니다 — 레트로 팝아트 캐리커처(`titans/berkshire/portrait.webp`, 104KB).
없으면 `onerror` 가 그림 칸을 통째로 빼고 글이 폭을 다 씁니다(`.nofigure`) —
`LIVE`·`MARKS` 와 같은 요령이라 **깨진 그림이 안 남습니다**(실측 0건).

**색이 맞는지는 재서 판단했습니다.** 처음에 온 것은 바탕이 크림색
(`#F6E6C4`)이었는데, `L*a*b*` 의 노랑↔파랑 축에서 **혼자 `b* +18.5`** 였습니다 —
카드는 0, 종이는 −2.3 으로 전부 중성~푸른 쪽입니다. 밝기는 거의 같아서
(91.8 vs 93.3) 어둡게 보이는 것이 아니라 **흰 종이 옆의 누런 종이**로
보입니다(카드 흰색과 색 거리 ΔE 20.2 — 5만 넘어도 눈에 띕니다).
**선과 어두운 부분은 잘 맞았습니다** — 그림의 남색 `#151927`(L 9.0 · b −10.2)이
`--ink #0F1A24`(L 8.7 · b −8.4)와 거의 같은 자리입니다.
사용자가 바탕이 순백인 것으로 다시 만들어 왔고, 재 보니 `#FFFFFF` 입니다.

**그림에 테두리를 두르지 않습니다.** 처음에는 `--rule-2` 로 1px 을 둘렀는데,
바탕이 카드와 같은 순백이라 **테두리만 네모로 떠서 없는 상자를 그려 놓은 꼴**이
됩니다. 빼니까 그림이 카드에 얹힌 한 장으로 읽힙니다.

**상자는 3:2 입니다(300×200).** 받은 그림이 1024×559 였는데 **흰 여백을 빼면
실제 그림은 3:2 에 가깝습니다**(잉크 범위 733×541). 여백째로 1:1 상자에 넣으면
`cover` 가 가로를 잘라 **엄지손가락이 통째로 날아갑니다**(실측). 그래서 잉크
범위를 기준으로 좌우 51·52px 씩만 남기고 3:2 로 구워 넣었습니다 —
**바탕이 카드와 같은 흰색이라 자른 자리가 안 보입니다.**
`object-fit:cover`·`object-position:50% 30%` 는 다음 투자자가 다른 비율을
가져와도 머리가 안 잘리게 하는 보험입니다.

**대체 글자(alt)도 언어를 따라갑니다.** 그림은 한 장이지만 화면 낭독기가 읽는 말은
두 벌입니다 — `data-alt-ko`/`data-alt-en` 을 `applyLang()` 이 갈아끼웁니다.

**사실관계 둘을 고쳐 적었습니다.** 사용자가 *"설립자인 워런 버핏"* 이라고 했는데
**버크셔 해서웨이의 설립자가 아닙니다** — 19세기 섬유회사이고 버핏은 **1965년에
경영을 맡았습니다.** 글에는 `1965년 … 경영을 맡은 뒤` 로 적었습니다. 그리고
**현재 직함을 쓰지 않았습니다** — 2025년 말에 CEO 자리를 그렉 아벨에게 넘겼고,
직함을 적으면 그때마다 낡습니다.

**살아 있는 사람의 얼굴입니다.** 이 문서에 이미 적힌 *"살아 있는 투자자의 이름을
간판에 걸지 마세요"*(퍼블리시티권·상표)가 **이름보다 얼굴에 더 세게 걸립니다.**
사이트에 후원 링크가 있다는 점도 같이 걸립니다. **어시스턴트가 대신 판단하지
않습니다** — 사실만 알리고 사용자가 정했습니다. 끄는 길은 `#titan-bio` 덩어리를
지우는 것 하나이고, 그림만 빼면 글은 그대로 남습니다.

**그림이 무엇을 말하는지는 따로 봐야 합니다.** 고른 그림은 **엄지를 세우고
콜라 캔을 든** 구도입니다. 엄지는 이 화면이 한 번도 쓴 적 없는 **추천 몸짓**이고,
바로 아래가 보유 종목 목록이라 `좋다/사라`로 읽힐 수 있습니다. 6번의
*"영어가 사라는 쪽으로 기운다"* 와 같은 종류의 자리입니다. **사용자에게
알리고 사용자가 정했습니다** — 바꾸려면 `portrait.webp` 한 장만 갈면 됩니다.
캔은 상표 그대로가 아니라 `COLA` 라고만 적힌 것이라 앞 후보보다 낫습니다.

**재 본 것**: 가로 넘침 320/360/390/641/700/810/900/1280px × 한국어·영어
**전부 0px** · 머리글과 분기 카드 사이에 정확히 들어감(8폭 × 두 언어) ·
한 화면에 한 언어만(영어 0자/한국어 0자) · 카드 밖으로 나간 조각 0 ·
그림 잘린 것 0(좌우 51·52px 여백) · 그림 없을 때 깨진 그림 0 · JS 오류 0 ·
대비 글 7.45 · 인용 17.59 · 출처 5.47 **전부 AA** · 11px 미만 글자 0개 ·
파이썬 45건 · 화면 28건 통과.

**한글 오타 하나를 사용자가 잡았습니다 (2026-09-23).** `경쟁자가 쉽게 흉내 내지
못하고` 를 **`횝내`** 로 적어 두고 있었습니다 — `흉`(U+D749) 대신 `횝`(U+D69D)
입니다. 제가 그 문단을 파이썬으로 써 넣으면서 **유니코드 이스케이프를 한 글자
틀리게** 적은 것입니다(`\ud69d` vs `\ud749`). 8번의 *"`\25B8` 은 삼각형이
아닙니다"* 와 **같은 종류의 사고**이고, 이번에는 제어문자가 아니라 **멀쩡한
한글 한 글자**라 `grep -nP '[\x00-\x1f]'` 같은 검사에 안 걸립니다.

**한글을 이스케이프로 써 넣지 마세요.** 파이썬 heredoc 에 한글을 그대로 적으면
눈으로 읽히고, 이스케이프로 적으면 **쓴 사람도 읽을 수 없습니다.** 지금 저장소
전체를 훑어 같은 글자가 더 있는지 봤고 **이 한 건뿐**이었습니다.

같은 줄에서 인용의 띄어쓰기도 고쳤습니다 — `'영원히' 입니다` → `'영원히'입니다`.

**Codex 검수에 넘기기 전에 낡은 지도 셋을 고쳤습니다 (2026-09-23, 사용자 요청).**
*"이제 틀은 완성인거 같아. 흠을 좀 잡고 싶은데 … Codex한테 뭘 줘야 하지?"*

**따로 줄 것이 없습니다 — 저장소가 인수인계 경로입니다**(0-3). 다만 이어받는
쪽이 지도 삼아 보는 자리 셋이 낡아 있었습니다.

```
1  다음에 할 일.md      9일 낡음 — 다 지어 놓은 것을 "아직 시작 안 함"이라고 적고 있었음
2  CLAUDE.md 구조 트리   검사 파일 5개를 포함해 9개가 빠짐
3  AGENTS.md            "상대에게 검수를 요청하지 않는다" 가 주인의 요청까지 막는 것처럼 읽힘
```

**`다음에 할 일.md` 에는 이제 사용자가 직접 할 일만 둡니다.** 끝난 일·정해진 것·
왜 그렇게 했나를 여기 한 벌 더 두었던 것이 **갈라진 원인**입니다 — 0-3 의
*"두 곳에 같은 말을 적으면 갈라집니다"* 가 이 파일에서 그대로 일어났습니다.
177줄 → 114줄이고, 남은 것은 Search Console 확인 · 후원 주소 · 장중 `실시간` ·
머지된 브랜치 치우기뿐입니다. 나머지는 이 문서를 가리킵니다.

**구조 트리에 `scripts/tests/` 가 통째로 없었습니다.** 검사 28+45건이 **글 속
문단에만** 적혀 있었고 트리에는 안 보였습니다 — 트리는 이어받는 쪽이 *"뭘 돌려
봐야 하나"* 를 찾는 자리라, **검사가 있는 줄 모르고 시작할 수 있는** 자리였습니다.
같이 빠져 있던 것: `check-titans.yml`·`probe-amendments.yml`·`probe_amendments.py`·
`portrait.webp`·`.gitignore`·`titans/__init__.py`. 스크립트로 대조해 **지금 0건**입니다.

**`data` 브랜치는 사용자가 이미 지웠습니다.** 문서만 "아직 남아 있습니다"라고
적고 있었습니다 — 제 로컬에 옛 표식이 남아 있어서 처음엔 살아 있는 줄 알았고,
GitHub 목록을 실제로 받아 보고 알았습니다.

**`AGENTS.md` 의 검수 금지는 어시스턴트끼리의 규칙입니다.** 그 줄이 막는 것은
*자기 검증을 상대에게 떠넘기는 것*이지 **주인이 두 번째 눈을 부르는 것이
아닙니다.** 구분을 안 적어 두면 Codex 가 그 줄을 읽고 되묻습니다.
**고치기 전에 먼저 목록으로 보고한다**도 같이 적었습니다 — 무엇을 고칠지는
주인이 정합니다.

**출처와 한계(각주)는 사용자 판단으로 나중에 손봅니다.**


**한국어·영어 표현을 훑고 열한 군데를 고쳤습니다 (2026-09-23, 사용자 요청).**
*"Codex에서 검수 돌리고 있고 그 중에 너는 한글/영어 표현 중에 어색한 부분이
있는지 좀 봐줘."* **본 사이트의 영어는 2026-09-13 에 제미나이·ChatGPT 로 두 번
봤지만, 이 화면의 영어는 한 번도 안 봤습니다**(6번). 그래서 여기만 봤습니다.

**사전을 읽지 않고 화면을 띄워 찍히는 글자를 받아 나란히 놓았습니다.**
6번의 *"글을 검수시킬 때는 화면에 실제로 찍히는 말과 맞는지 따로 보세요"* 입니다 —
실제로 사전에만 있고 **화면에는 안 나오는 문장 하나**(`lifeQuiet`)를 처음에
검수 목록에 올렸다가, 쓰이는지 `grep` 해 보고서야 죽은 키인 걸 알았습니다.

**영어가 실제로 틀린 것 넷.**

```
1 quarters          다섯 줄에 그렇게 찍혀 있었습니다 — 영어 단수 처리가 없었음
Sold out            영어로는 "품절". 게다가 계기판은 같은 사건을 EXITED 라고 셈
held                한 화면에서 세 가지 — Shares held · 안 움직임 · held 1.5 yr
Filing(열 머리글)    그 칸의 값은 분기 마지막 날. 머리에는 filed Aug 14 가 따로 있음
```

**`held` 가 제일 나빴습니다.** 한국어는 `보유 주식 수` / `유지` / `보유 1.5년`
으로 갈라져 있는데 영어만 한 낱말로 뭉쳐 있었습니다. 지금은 안 움직인 것이
`unchanged` 입니다(계기판도 같이).

**`Sold out` → `Exited`.** 바꾼 이유는 뜻보다 **한 사건이 두 이름을 갖고
있었다**는 쪽이 큽니다 — 계기판에 `1 EXITED` 라고 세어 놓고 바로 아래 사건
줄에서는 `Sold out` 이라고 불렀습니다. `Back in` → `Re-entered` 도 같은 자리
입니다(표의 `Entered` 와 짝).

**차트 막대와 표가 같은 수를 다르게 적고 있었습니다** (사용자 결정 —
*"차트를 표에 맞추자"*). 한 줄을 펼치면 차트는 `−389.37M`, 바로 밑 표는
`−3.89억주` 였습니다. 차트만 쓰던 `K·M·B` 는 **표가 생기기 전(09-22)의
결정**이라, 표가 생긴 뒤에는 위아래가 어긋납니다. 지금은 `compShares()` 가
표와 같은 `shortShares()` 를 부릅니다.

**그러면서 이름표 폭 계산을 고쳐야 했습니다.** `text.length*6.7` 이었는데
**한글은 고정폭에서도 두 칸을 씁니다** — `−1,029만주` 가 영문과 같은 폭으로
잡혀 이웃과 겹칠 자리였습니다. `labWidth()` 가 한글을 2로 셉니다.
실측: 900px 한국어에서 이름표가 61개 → 55개로 줄었습니다(겹칠 것을 미리 버림).

**한국어 띄어쓰기가 한 화면 안에서 갈려 있었습니다** — 열 머리글은
`보유 주식 수`, 안내문은 `보유 주식수`. 머리글 쪽으로 맞췄습니다(네 곳).

**그 밖에**: `estimates from` → `estimated from`, `1.5 yr` → `1.5 years`,
`only one filing…` 을 문장으로.

**눈썹줄 `Titans' picks` 는 그대로 둡니다 (사용자 판단).** 영어 `picks` 에는
'추천 종목' 냄새가 있고 한국어 `대가들의 선택` 은 중립이라 6번의 실패 이력과
같은 방향인데, **브랜드 이름이고 주소도 `/titans/` 입니다.** 알리고 뒀습니다.

**죽은 사전 키 여섯 개를 지웠습니다** — `lifeSum`·`lifeQuiet`·`lifeScroll`·
`aboutH`·`buy`·`sell`(두 언어 각각). `BUY`/`SELL` 스티커를 뺄 때(09-17)와
차트 아래 요약 줄을 뺄 때(09-18) 문구만 남은 것입니다. **화면에는 영향이
없지만 남겨 두면 지금 화면에 없는 말을 검수하게 됩니다** — 이번에 실제로
그랬습니다. **이제 검사가 막습니다**: 사전의 열쇠 37개를 전부 훑어 화면
코드가 안 읽는 것이 하나라도 있으면 실패하고, 두 언어의 열쇠 집합이 어긋나도
실패합니다.

**검사 넷을 더했고, 네 건 모두 되살려 확인했습니다** — `1 quarters` 를
되돌리면 32건 중 1건 실패, `same:"held"` 1건, `compShares` 를 옛것으로 1건,
`rcDate:"Filing"` 1건. 고치면 32건 통과입니다. 8번의 *"`hidden` 속성이 붙어
있어서 검사가 통과했다"* 와 같은 자리라 일부러 되돌려 봤습니다.

**재 본 것**: 가로 넘침 320/360/390/641/700/810/900/1280px × 한국어·영어
**전부 0px** · JS 오류 0 · 11px 미만 글자 0 · 막대 이름표가 그림 밖으로 0건 ·
이름표끼리 겹침 0건(두 언어 × 4폭 × 기간 4개) · 차트 이름표와 표의 `변화`
칸이 글자까지 같음(두 언어) · 파이썬 45건 · 화면 32건 통과.


**Codex 검수 결과에서 세 가지를 고쳤습니다 (2026-09-23, 사용자 요청).**
*"분할 고치고, #3+#4도 같이 해줘."* Codex 가 낸 9건을 전부 교차확인했고
(8건 맞음), 그중 **화면이 틀린 숫자를 조용히 말하는** 셋을 먼저 고쳤습니다.

**1. 진짜 3:2 분할을 2:1 로 읽고 있었습니다.**

```
허용차 ±30% · 1.5 ÷ 2 = 0.75 — 정확히 25% 차이라 밴드 안에 들어옵니다
실측: 비율 1.45 ~ 1.80 이 전부 f=2 로 잡혔습니다
```

**Codex 는 퍼싱 스퀘어에서 찾았지만 버크셔 28년에도 이미 네 건 있었습니다** —
컴캐스트·아이언마운틴(2007-03), 토치마크(2011-09·2014-09). 전부 지금은 판
종목이라 화면에는 안 보이지만, **엔진이 공통이라 새 투자자에서 바로 터집니다.**
잡아 놓고 틀린 배수로 '고치는' 것이라 **안 잡는 것보다 나쁩니다** — 33% 오차가
조용히 박힙니다.

**고친 방법은 재서 찾았습니다.** 주식수 비 1.30~1.70 구간을 전부 뽑아 보니
갈라졌습니다.

```
0.000%  Comcast 2007-03        8,000,000 → 12,000,000   가÷1.644   진짜 3:2
0.000%  Iron Mountain 2007     6,031,900 →  9,047,850   가÷1.582   진짜 3:2
0.000%  Torchmark 2011·2014                             가÷1.84·1.56  진짜 3:2
0.000%  GM 2012-09            10,000,000 → 15,000,000   가÷0.867   분할 아님!
──────────────────────────────────────────────────────────────────
1.2% ~  Pool · BNY Mellon · Precision · Apple 2016 …              전부 그냥 매수
```

**사람의 매매는 1.500000 에 안 떨어집니다. 분할은 떨어집니다.** 그래서
`f<2` 는 주식수 ±1%, `f>=2` 는 ±20% 이고, **먼저 맞는 것이 아니라 제일 잘
맞는 것**을 고릅니다. **동그란 숫자로 사면 똑같이 떨어지므로**(GM 은
10,000,000 → 15,000,000) 가격 거부권이 같이 필요합니다 — 그 분기에 주가가
올라서(가÷0.867) 걸러집니다.

**`5:4`·`4:3` 은 넣지 않았습니다 — 못 넣습니다.** 넣어 봤더니 12건이
오탐이었습니다(셰브런 2021-09 `주×1.2413 가÷1.032` 은 그냥 24% 더 산 것).
**1.25 근처에서는 가격 거부권이 힘을 못 씁니다** — 5:4 분할이면 주가가 20%
내리는데 분기 중 시세가 그만큼 움직이는 건 흔합니다. **13F 만으로는 풀 수
없는 자리**이고, 고칠 것이 아니라 **못 한다고 적어 둘 일**입니다.
같은 이유로 `NRG ×1.5441`(정확히 1.5 가 아님)도 고치는 대신 안 건드립니다 —
**안 잡으면 큰 매수 막대로 눈에 보이고, 틀린 배수로 고치면 안 보입니다.**
**역분할(1:10)도 안 잡습니다** — 후보가 전부 1보다 큽니다.

**28년 3,741쌍 중 달라지는 것은 6건이고, 지금 보유 26종목은 하나도 안
움직입니다**(애플 $39.59 / +631% 그대로). 나머지 둘은 USG 2006-09
(`주×2.5694` — 분할이 아니라 신주인수권)와 NRG 2008-09 가 **안 잡히는 쪽**으로
바뀐 것입니다.

**2. 한 분기라도 성공하면 초록불이었습니다.**

`fetch_13f.py` 의 조건이 `failed and not got` 이었습니다 — **새 분기 하나가
성공하고 다른 하나가 실패하면 종료코드 0** 이라, 빠진 분기가 있는 JSON 이
조용히 커밋되고 텔레그램도 안 울렸습니다. 정정 병합 실패도 `continue` 뿐이라
같았습니다.

지금은 **받은 것은 그대로 저장한 뒤 빨간불로 끝냅니다.** 6-2 의 *"데이터를
먼저 커밋하고 그 뒤에 본다"* 와 같은 순서이고, 워크플로의 저장 단계가
`if: always()` 라 이 종료코드가 자료를 막지 않습니다(실측으로 확인).

**`amend_gap`(2013년 이전 텍스트)은 실패로 안 셉니다.** 영영 못 합치는
**알려진 상태**라 넣으면 매주 43건이 울려 진짜 실패가 묻힙니다 — 6-2 의
*"매주 실패하면 아무도 안 봅니다"* 가 그대로 걸리는 자리입니다.
다만 **`amendmentType` 이 모르는 값인 정정은 고쳐질 때까지 매주 울립니다.**
그건 사람이 한 건 열어 봐야 하는 것이라 일부러 그대로 뒀습니다.

**로그가 거짓말을 하고 있었습니다.** `정정 공시가 있는 분기 N개 (원본 숫자를
쓰는 중)` 을 **합쳐 놓고도** 찍고 있었습니다(`if amended:` 조건이라 반영 여부를
안 봤습니다). 지금은 반영한 것과 못 한 것을 갈라 적습니다.

**변수 이름이 겹칠 뻔했습니다.** `apply_amendments` 가 돌려주는 목록을
`stuck` 으로 받았는데 **알림 블록에 이미 같은 이름이 있었습니다.** `tag`/`flag`/
`li` 사고와 같은 자리이고 이 저장소에서 네 번째입니다 — 지금 이름은 `unmerged`
입니다. **함수에 반환값을 더할 때는 받는 쪽 이름을 먼저 `grep` 하세요.**

**3. 빠진 분기를 `이번 분기` 라고 적고 있었습니다.**

`build()` 가 `qs[qs.length-2]` 를 직전 분기로 쓰면서 **그게 바로 앞 분기인지
안 봤습니다.** 2번과 겹치면 이렇게 됩니다 — 수집이 한 분기를 조용히 놓치고,
화면이 두 분기치 증감을 `이번 분기` 라고 적습니다(실측: 2026Q1 을 빼면 BoA
감소량이 3,023만주 대신 **3,390만주**).

지금은 `qGap()` 이 두 공시 사이 분기 수를 세고, **2 이상이면 화면이 스스로
밝힙니다.**

```
머리글   이번 분기  →  직전 공시 대비        (영어 This quarter → Since last filing)
카드     직전 공시가 바로 앞 분기가 아니라 2025년 4분기입니다 — 아래 증감은 2개 분기치입니다.
```

**머리글만 고치면 표가 자기 말을 어깁니다** — 카드의 안내 줄과 열 이름을
한 곳(`gapped`)에서 같이 정합니다.

**공시가 한 건뿐이면 전 종목이 `신규` 로 찍혔습니다.** 분기 카드는 이미 접히고
있었지만 **목록 줄은 그대로 거짓말을 하고 있었습니다.** `isNew:!p` 라 직전
공시가 없으면 전부 참이 됩니다. 지금은 `noPrev` 로 갈라 **가운데 칸에 줄표만**
적습니다 — 표의 전량매도 줄과 같은 어휘로, `0` 이 아니라 *쓸 값이 없다*는 뜻입니다.

**검사 다섯 건을 더했고, 되살려 전부 확인했습니다** — 분할 규칙 1건 · 구멍
감지 1건 · 신규 판정 1건 · 반쪽 실행 종료코드 1건. **네 번째는 처음에 안
잡혔습니다**: 제 시험이 새 코드가 아니라 **원래 있던 `failed and not got`
가드**에 걸리고 있었습니다. 하나는 성공하고 하나는 실패하는 진짜 상황으로
다시 짜고서야 잡혔습니다. 8번의 *"`hidden` 속성이 붙어 있어서 검사가
통과했다"* 와 같은 자리입니다.

**재 본 것**: 파이썬 46건 · 화면 36건 통과 · 지금 보유 26종목 추정가 변화
**0건** · 가로 넘침 320/360/390/641/700/810/900/1280px × 한국어·영어 **전부
0px** · JS 오류 0 · 11px 미만 글자 0 · 막대 이름표가 그림 밖으로 0건 ·
**구멍 난 자료와 공시 한 건짜리 자료를 브라우저에 실제로 띄워** 두 언어 ×
320/390/900/1280px 에서 안내 줄·머리글·줄표가 제대로 나오는 것 확인.

**아직 안 한 것 (Codex 검수의 나머지).** 옵션을 보통주에 합치는 것(P1,
버크셔는 28년치 `putCall` 이 전부 비어 있어 지금은 안 보임) · 티커·업종
수집이 실패 뒤 복구 안 되는 것(P2) · XML 부터 넣은 투자자의 옛 공시를 매번
새 것으로 보는 것(P2) · 금액 단위 경계조건(P2) · Windows CRLF 에서 검사
1건 실패(P3) · 문서 드리프트(P3).


**분할을 추측이 아니라 진짜 기록으로 바꿨습니다 (2026-09-23, 사용자 지적).**
*"13F만으로 구별이 안되면, 어딘가를 찾아봐야지. 불완전한거를 그냥 두고 넘어갈
수는 없지."* 맞습니다. **찾아보니 이미 저장소 안에 있었습니다.**

`fetch_prices.py` 가 종가를 받을 때 `events=splits` 로 **분할 이벤트를 같이
받아 `prices.json` 에 넣고 있었습니다.** 화면이 그 칸을 안 읽고 있었을 뿐입니다
(`acceptPrices()` 가 `ticker` 와 `values` 만 꺼냈습니다).

```
AAPL 2020-08-31  4:1        ← 우리가 추측으로 맞히던 것
KO   2012-08-13  2:1
SIRI 2024-09-10  1:10       ← 역분할. 추측기는 후보가 전부 1보다 커서 아예 못 잡음
GOOG 2022-07-18  20:1
```

**야후의 `splits` 는 분할 목록이 아닙니다 — 여기가 이번의 진짜 발견입니다.**
**'주가를 보정해야 하는 사건' 목록**이라 스핀오프·특별배당이 섞여 있습니다.
그대로 쓰면 **없는 분할을 적용하게 됩니다.** 실측으로 확인했습니다.

```
JEF 2023-01-17  "1046:1000"  ← 야후는 분할이라고 적어 둠
버크셔 주식수     2022-12-31  433,558주
                 2023-03-31  433,558주   ← 한 주도 안 움직였습니다
                 (제퍼리스가 Vitesse 를 떼어 낸 것 — 주가만 내려갔습니다)
```

**갈라내는 법은 기약분수입니다.** 진짜 분할은 줄이면 작은 정수가 됩니다.

```
받음    2:1 · 4:1 · 7:1 · 20:1 · 1:10
버림    1017:1000(레나) · 523:500(제퍼리스) · 267:250 · 51:50(레나B) ·
        10000:9983(옥시덴탈) · 310:1(Ally 전환) · 1001:500(구글 C주 배분)
```

`splitRatio()` 가 그 판정을 하고, 저장된 21건 중 **12건을 버리고 9건만**
받습니다. 검사로 못박아 뒀습니다(위 일곱 개를 전부 이름 그대로 넣어 둠).

**모르는 구간은 1 이라고 답하지 않습니다.** `realSplit()` 은 주가 파일이 그
구간을 덮을 때만 값을 주고, 못 덮으면 `undefined` 를 돌려 **추측기로
넘깁니다.** 1 이라고 답하면 아메리칸익스프레스 2000년 3:1 이 통째로 사라집니다.

**한 묶음에 종류가 여럿이면 가장 늦게 시작하는 쪽에 맞춥니다.** 알파벳 A·C 를
한 줄로 묶으므로 계산도 같은 열쇠(CUSIP 앞 여섯 자리)를 씁니다. 두 종류가
같은 날 **다른 배수**를 말하면 그 날은 버리고 추측기로 넘깁니다 — 한쪽만
쪼개진 것을 합산 주식수에 그대로 곱하면 틀립니다.

**처음에 켜지지 않는 버그를 냈습니다.** 커버 시작일 초기값을 `"9999"` 로 두고
`if(clean[0][0]>book.from)` 로 최대를 잡으려 했는데, 어떤 날짜도 `"9999"` 보다
크지 않아 **늘 추측기로 떨어졌습니다.** 재 보고 알았습니다 — 기능을 넣었는지
확인하려면 **새 길로 갔을 때 값이 달라지는 것을 실제로 봐야** 합니다.

**지금 보유 26종목은 숫자가 하나도 안 바뀝니다.** 덮이는 구간에서 진짜 기록과
(고친) 추측기의 판정이 **전부 일치**했습니다. 그러니 이번 변경은 **보이는 고침이
아니라 보험**입니다 — 추측을 없앤 것이고, 5:4·4:3·역분할·스핀오프처럼
추측으로는 못 풀던 자리가 닫힌 것입니다.

**아직 안 닫힌 구간이 하나 있습니다: 2011년 9월 이전.**

```
주가 파일이 덮는 구간   2011-09 ~ 지금  (15년)
공시가 있는 구간        1998-12 ~ 지금  (28년)
                        ↑ 이 사이는 아직 추측입니다
```

지금 보유 종목 중 여기 걸리는 것은 **둘**입니다 — 아메리칸익스프레스 3:1(2000)과
무디스 2:1(2005). 둘 다 주식수 비가 정확히 `3.0000`·`2.0000` 이라 추측기가
맞게 잡습니다. **그래도 추측입니다.**

**닫는 길은 수집기의 창을 넓히는 것 하나뿐인데, 야후 응답을 직접 봐야
합니다**(개발 환경에서 `query1.finance.yahoo.com` 은 프록시가 끊습니다 — 3번).
그래서 **짐작해서 고치지 않고 정찰을 먼저 만들었습니다** —
`scripts/probe_splits.py` + `.github/workflows/probe-splits.yml`.
`probe_sec.py`·`probe_amendments.py` 와 같은 요령이고, 답할 질문은 하나입니다:

```
period1 을 아주 옛날로 주면 야후가 그 시절 분할까지 돌려주는가?
일봉과 월봉의 분할 목록이 같은가?   ← 같으면 월봉으로 싸게 받을 수 있습니다
```

**그 정찰은 돌았고 구간도 닫았습니다** — 바로 아래 항목을 보세요.

**검사 세 건을 더했고, 세 건 모두 되살려 확인했습니다** — 스핀오프 필터를 빼면
39건 중 3건 실패, 모르는 구간을 1 로 답하게 하면 8건 실패, 커버 시작일을
아무 때나로 하면 2건 실패.

**재 본 것**: 화면 39건 · 파이썬 46건 통과 · 지금 보유 26종목 변화 **0건**
(추측 경로와 진짜 경로의 결과가 글자까지 같음) · 가로 넘침
320/360/390/641/700/810/900/1280px × 두 언어 **전부 0px** · JS 오류 0 ·
받아들인 분할 9건 / 버린 사건 12건.

**남은 구간도 닫았습니다 — 분할을 상장 때부터 훑습니다 (2026-09-23).**
사용자가 `Probe Splits` 를 눌렀고, **빨간불로 끝났는데 그것이 답이었습니다.**
정찰은 *다섯 종목 전부 HTTP 200* 으로 받았고, `1d` 와 `1mo` 의 분할 목록이
한 군데 달라서 종료코드 1 이 된 것입니다([실행 35827799749](https://github.com/Snoring-Prince/timing/actions/runs/35827799749)).

```
질문 1  옛날 분할까지 돌려주는가?    → 예. 상장 때까지 돌려줍니다
        AXP 1972-06-01 상장 · 분할 6건(1983년치까지) · KO 1962-01-02 상장 · 6건
        찾던 둘이 그 안에 있습니다   AXP 2000-05-11 3:1 · MCO 2005-05-19 2:1
질문 2  월봉으로 싸게 받아도 되는가? → 아니오
        AXP 1983-02-11 4:3 이 월봉 응답에만 통째로 빠집니다 (일봉 6건 · 월봉 5건)
```

**그래서 일봉으로 받고 창 밖의 옛 바는 버립니다.** `fetch_prices.py` 가
`DEEP = 1970-01-01` 부터 받고, 저장은 **예전 그대로 15년 창**입니다
(`merge_series` 가 `day >= start` 로 거릅니다). **파일이 커지지 않습니다.**

**상장 때부터는 종목마다 한 번만 훑습니다 (사용자 지적).**
*"주 1회 전체 재수집 때 상장 때부터 훑으면, 너무 로드가 세게 걸리는 거 아냐?"*
**요청 수는 안 늘어납니다** — 같은 29번을 부르되 한 번에 더 긴 구간을 달라고
하는 것뿐입니다. 그래도 지적이 맞습니다: **1983년 분할은 앞으로도 1983년
분할이라, 매주 40년치 바를 다시 받아 같은 답을 얻는 것은 낭비입니다.**

```
요청 수         29번 → 29번          (그대로)
받는 양(주 1회)  10.8MB → 33MB → 도로 10.8MB   ← 처음 한 번만 33MB
작업 시간       지금 30~37초 (체크아웃·커밋 포함, 실행 2회 실측)
```

**배수는 실측입니다** — 정찰 응답에서 바 하나당 106 B 이고, 상장부터 받으면
AXP 3.6배 · KO 3.8배 · AAPL 3.1배 · MCO 2.1배 · JEF 3.1배입니다.

이미 훑어 둔 종목(`splitsFrom` 이 `1970-01-01`)은 **예전처럼 15년 창만** 받고,
**창 밖 분할은 저장된 책에서 되살립니다** — 안 되살리면 그 주에 옛 분할이
통째로 지워집니다(검사로 못박음). 새 분할이 뜨면 기존 경로가 그대로 전체
재수집을 걸고, **티커가 바뀌면 표식을 물려받지 않습니다**(남의 분할을
이어받으면 안 됩니다).

**화면이 덮인 구간을 판단하는 기준도 바꿔야 했습니다.** `realSplit()` 은
`book.from` 보다 옛 분기면 추측기로 넘기는데, 그 값이 **가격 시작일**이라
분할을 아무리 깊이 훑어도 2011년 이전은 계속 추측이었습니다. 지금은
수집기가 적어 주는 `splitsFrom` 을 봅니다. **표식이 없는 옛 파일은 예전
그대로 가격 시작일로 봅니다** — 그래서 이 PR 이 머지돼도 봇이 한 번
돌기 전까지는 화면이 조금도 안 바뀝니다.

**옛 바까지 세면 그물이 헐거워집니다.** 반쪽 응답을 거르는 검사가
`len(incoming) < 저장분*0.98` 이었는데, 이제 `incoming` 에 40년치가 들어오니
**최근 15년이 통째로 비어도 통과**합니다. 세는 자리를 저장 창 안으로
좁혔습니다. **이 구멍은 제가 열고 제가 닫은 것이라 검사를 따로 만들었습니다** —
되돌려 보니 나머지 셋은 잡히는데 이것만 안 잡혔습니다.

**스핀오프가 여기서도 섞여 옵니다 — 그리고 이번 것은 공시 구간 안입니다.**

```
AXP 1994-05-31  10000:8825   리먼브라더스 분사
AXP 2005-10-03  10000:8753   아메리프라이즈 분사   ← 1998~2026 안에 있습니다
JEF 2013·2019·2023           앞서 확인한 셋
```

분할로 받았으면 아메리칸익스프레스 숫자가 틀어졌을 자리입니다.
`splitRatio()`(기약분수 ≤20)가 다섯 건 전부 버립니다 — **앞 회차에 만든
거름망이 새 자료에서 처음으로 실전 검증됐습니다.**

**봇이 돌고 난 뒤의 파일을 흉내 내어 26종목을 다시 재 봤습니다.**
정찰이 실제로 받아 온 목록을 그대로 넣고 화면을 돌렸더니 —
AXP·MCO 가 추측에서 진짜 기록으로 바뀌고, 스핀오프 다섯 건은 걸러지고,
**숫자가 달라진 종목은 0건**입니다(애플 +631% 그대로). 추측기가 맞히던
자리를 진짜 기록이 대신했을 뿐입니다. **보이는 고침이 아니라 보험입니다.**

**정찰의 빨간불 기준을 고쳤습니다.** "간격에 따라 목록이 다르다"는 이제
**알려진 답**이지 고장이 아닙니다. 그대로 두면 옛 분할이 있는 종목마다
빨간불이 떠서 진짜 고장이 묻힙니다(6-2 의 *"매주 실패하면 아무도 안
봅니다"*). 지금 빨간불은 **받아오지 못했을 때만**입니다.

**검사 여섯 건을 더했고, 여섯 건 모두 되돌려 확인했습니다** — 화면이
`splitsFrom` 을 안 읽게 하면 40건 중 1건 실패 · 15년부터만 받게 하면 1건 ·
표식을 안 적게 하면 2건 · 옛 바까지 세게 하면 1건 · **매주 상장 때부터 다시
받게 하면 1건 · 창 밖 분할을 안 되살리면 1건 · 티커가 바뀌어도 표식을
물려받게 하면 1건.** 고치면 전부 통과입니다.

**재 본 것**: 화면 40건 · 파이썬 51건 통과 · 지금 보유 26종목 변화 **0건** ·
받아들인 분할(흉내 실행) AXP 4건 · KO 6건 · AAPL 5건 · JEF 7건, 버린
스핀오프 5건 · 가로 넘침 320/360/390/641/700/810/900/1280px × 한국어·영어
**전부 0px** · JS 오류 0 · 26줄·차트·분기표 그대로.

**야후 응답 자체는 개발 환경에서 못 봅니다**(프록시 차단 — 3번). 위 목록은
러너가 받아 온 정찰 로그의 값이고, 수집기 변경은 흉내 응답으로만 쟀습니다.
**실제 수집은 머지 뒤 첫 전체 재수집(토요일)에서 확인해야 합니다** — 그때
`prices.json` 에 `splitsFrom` 이 붙고 옛 분할이 실립니다.

**분할 원칙을 정했습니다 (2026-09-23, 사용자가 원칙을 적어 줌).**
*"투자자별로 처음 등록할 때 / 처음 진입 혹은 재진입을 할 때 … 13F 공시부터
전부 읽어오고, 물적/인적·액면 분할을 자세히 분석한다. 그 후로는 증분으로
업데이트한다. 전량 매도한 것은 보관하되 업데이트 목록에서는 지운다."*

**넷 중 셋은 이미 그렇게 돌고 있었습니다.** 실제로 읽고 돌려서 확인했습니다 —
`required_cusips()` 는 **최신 분기 보유만** 모으고, 팔린 종목의 자료는
`series = dict(saved)` 로 **그대로 남습니다**. 재진입도 흉내로 돌려 봤습니다:
2020년에 팔린 종목이 2026년에 다시 들어오면 첫 요청이 빈 구간을 통째로 덮고,
그 안의 2022년 분할을 발견해 **스스로 전체 재수집을 겁니다**(요청 2번).

**1번은 말씀이 맞았고, 제 것이 필요보다 깊었습니다.** 화면이 분할을 묻는 자리는
**붙어 있는 두 공시 사이**뿐이고(`realSplit(key, QS[i-1], QS[i])`), 그것도
**그 종목을 들고 있던 두 분기**일 때만입니다. 그래서 깊이는 `1970년` 이 아니라
**그 종목이 공시에 처음 나온 분기**입니다 — 자료에서 셉니다(`first_seen()`).
*"그 전부터 갖고 있던 건 분할을 하든 문제없다"* 가 정확합니다.

```
깊이 = min( 그 종목이 처음 공시에 나온 분기,  15년 창의 시작 )
       ↑ 가격 캐시는 투자자들이 같이 쓰므로 가장 이른 투자자를 따릅니다
       ↑ 올해 들어온 종목도 가격선은 15년을 채워야 하므로 창보다 늦게 잡지 않습니다
```

**나중에 더 옛날 투자자가 들어오면 저절로 다시 팝니다** — `splitsFrom` 이
필요한 깊이보다 늦으면 다시 훑습니다(검사로 못박음). 표식만 보고 넘기면
그 종목은 영영 추측으로 남습니다.

**2번이 진짜 구멍이었고, 근거가 정확히 나왔습니다.**
액면분할은 이미 하고 있었지만 **인적·물적 분할은 버리고만** 있었습니다.

```
13F 값(금액÷주식수) ÷ (그 날짜 이후 사건 전부)  ==  야후 저장 종가
버크셔 465분기 전부 일치 · 어긋남 0건
```

**야후의 `splits` 칸은 분할 목록이 아니라 "종가를 이만큼 나눠 놨다"는
기록입니다.** 그러니 우리 13F 값에 같은 것을 적용하면 두 자료가 **정확히 같은
자**가 됩니다. 추측이 아니라 대응 관계입니다(검사로 못박음 — 465분기 대조).

**그래서 책을 둘로 나눴습니다.**

```
days (진짜 액면분할만)   주식수 × f · 가격 ÷ f      기약분수 ≤20 인 것만
adj  (모든 사건)         가격만 ÷ f                 인적·물적 분할 포함
```

**평균단가도 같이 옮겨야 합니다** — 평균 = 원가 ÷ 주식수이므로 주식수가 `fs`
배, 가격이 `1/fp` 배가 되면 원가는 `fs/fp` 배입니다. 액면분할은 둘이 같아
원가가 그대로이고(예전 그대로), 인적분할은 주식수가 안 변해 **원가만**
내려갑니다. 이 한 줄을 빠뜨리면 가격만 움직이고 수익률은 안 움직입니다.

**화면에서 실제로 바뀌는 줄은 하나입니다.**

```
제퍼리스   평균단가 $29.50 → $28.20   수익률 +69.4% → +77.2%
           (2023-01-17 Vitesse 분사 · 1046:1000 — 주식수는 433,558주 그대로)
나머지 25줄  한 톨도 안 움직임 (애플 +631% 그대로)
```

**레나는 안 움직입니다** — A·B 가 같은 날 다른 배수(1033:1000 / 1052:1000)를
말해서, 앞서 만든 *"한 묶음에 종류가 여럿이고 배수가 다르면 그 날은 버린다"*
규칙이 걸립니다. 합산 주식수에 한쪽 배수를 곱하면 틀리기 때문입니다.

**앞으로가 더 큽니다.** 구글 2014년 C주 배분이 `2002:1000`(약 2배)이라,
2013년부터 구글을 들고 있던 투자자를 넣으면 **수익률이 2배 어긋났을** 자리입니다.

**분기별 보유 변화 표의 `평가 금액` 칸은 이제 공시 금액과 다를 수 있습니다.**
그 칸은 `보유 주식수 × 분기말 가격`인데, 액면분할은 곱이 보존되지만 인적분할은
**주식수가 그대로라 곱이 그만큼 줄어듭니다.**

**"줄 안의 앞뒤를 맞춘다"는 이 뜻입니다** (2026-09-23 사용자 질문). 한 줄의
숫자 셋은 곱셈으로 이어져 있습니다.

```
화면    2022.09.30  진입  433,558주 × $28.20 = $12.2M
EDGAR                                          $12,790,000 = $12.8M   4.4% 차이
```

두 갈래뿐입니다 — **곱이 맞게** 두면(지금) 독자가 두드려 봐도 맞는 대신 EDGAR 와
어긋나고, **공시 금액을 그대로** 적으면 EDGAR 와 같은 대신 그 줄에서
`433,558 × $28.20` 이 $12.8M 이 안 나와 **화면이 틀린 것처럼 보입니다.**
가격을 분사 보정하는 순간 셋 다 맞출 방법은 없습니다. 제일 덜 말하는 칸이라
(좁은 화면에서 먼저 감추는 그 칸입니다) 곱이 맞는 쪽을 골랐습니다.

**걸리는 줄은 하나입니다** (2026-09-23 실측). 지금 보유 26종목에서 가격만
움직이는 자리는 **제퍼리스 하나**이고, 표에서는 진입 줄 1개(차트 가격선으로는
2022년 두 분기)입니다. 나머지 25종목은 전부 진짜 액면분할이라 주식수와 가격이
같이 움직여 곱이 보존됩니다. 레나는 A주와 B주가 같은 날 다른 배수를 말해 그
날을 버리므로 해당 없습니다. **여기 적혀 있던 `제퍼리스·레나B 9분기, 4.6~5.2%`
는 틀린 값이었습니다** — 재서 고쳤습니다.

**`prices.json` 은 방문자가 받는 파일입니다.**
*"전량 매도한 것은 보관하되"* 는 봇에게는 맞지만, **보관하는 곳이 방문자가
받는 파일이면 안 됩니다.** 재 봤습니다.

```
지금 (이번 분기 보유 29종목)              2.2 MB   ← 이미 가볍지 않습니다
버크셔가 28년간 들고 있던 것 전부 242종목   18.3 MB
투자자 8명 (겹침 감안 4배)      968종목    73.4 MB
```

종목 하나가 15년치 일별 종가로 **76KB** 입니다. 이 PR 의 코드는 팔린 종목을
`series` 에 그대로 남기므로 **파일이 조용히 자랍니다.** 숫자만 알리고 판단을
남겼고, **사용자가 버리는 쪽으로 정했습니다** — 바로 아래 항목입니다.

**검사 아홉 건을 더했고, 일곱 가지를 되돌려 전부 확인했습니다** — 깊이가 더
옛날로 필요해져도 안 파면 1건 실패 · 새 종목에 15년 창을 안 주면 1건 · 처음이
아니라 마지막 분기를 세면 1건 · 인적분할을 가격 보정에서 빼면 2건 · 평균단가를
같이 안 옮기면 1건 · 인적분할로 주식수까지 움직이면 1건 · 증감이 가격 배수를
쓰면 1건. **처음엔 셋이 안 잡혀서 검사를 세 개 더 만들었습니다.**

**재 본 것**: 화면 44건 · 파이썬 54건 통과 · 26줄 중 움직인 줄 **1개**(제퍼리스,
비가 정확히 1.046) · 주식수·증감은 26줄 전부 그대로 · 13F 값과 야후 종가 대조
465분기 전부 일치 · 가로 넘침 320/360/390/641/700/810/900/1280px × 한국어·영어
**전부 0px** · JS 오류 0 · 차트·분기표 그대로.

**이번에는 머지하면 바로 보입니다.** 앞 회차와 달리 제퍼리스 구간은 지금
`prices.json` 이 이미 덮고 있어서, 봇을 기다리지 않고 수익률이 바뀝니다.

**팔린 종목을 방문자 파일에 안 쌓습니다 (2026-09-23, 사용자 판단).**
*"팔린 종목을 굳이 방문자가 받는 파일에 쌓을 필요가 전혀 없지 않나?"* 맞습니다.

쌓고 있던 자리는 `fetch_prices.py` 의 **한 줄**(`series = dict(saved)`)이었습니다.
한 번 받은 종목은 투자자가 팔아도 파일에 영원히 남았습니다. 지금은 **이번 분기
보유만** 남깁니다(`{c: saved[c] for c in required if c in saved}`). 화면이 읽는
것도 그것뿐입니다 — 팔린 종목의 종가는 한 번도 안 읽힙니다.

**버리는 값은 재진입 때 한 번 다시 받는 것뿐입니다.**

```
재진입 빈도   28년 111분기에 37번 = 분기당 0.33건
그때 드는 것  요청 1번 · 74KB   ← 새 종목이 처음 들어올 때와 똑같은 일
```

그 길은 새로 만들 것도 없습니다 — 파일에 없으면 **그 종목이 공시에 처음 나온
분기까지** 다시 훑는 경로가 이미 있고(앞 회차에서 검사로 못박은 그것), 안 들고
있던 사이의 분할·분사도 그 한 번에 같이 들어옵니다.

**창고를 봇 전용 파일로 가르지 않았습니다.** 재진입이 이렇게 싼데 파일을 하나
더 두면 **같은 자료가 두 벌이 되고, 두 벌이 되면 갈라집니다.**

**받기에 실패한 종목은 옛 값을 그대로 지킵니다** — 버리는 것은 *보유 목록에 없는*
종목뿐이고, 실패한 종목은 목록에 있으므로 남습니다.

**공시책을 한 권도 못 읽으면 멈춥니다.** `books()` 는 파일이 없는 투자자를 조용히
건너뛰므로, 책을 못 읽으면 보유 목록이 비고 위 규칙이 **파일을 통째로 비웁니다.**
`fetch_long.py` 의 *"반쪽짜리 파일이 올라가지 않게 하는 장치"*(3번)와 같은 것을
`main()` 에 넣었습니다.

**그 가드를 되돌려 확인하다가 진짜 자료를 비웠습니다.** 검사가 `main()` 을 부르는데
`OUT` 을 안 갈아끼웠고, 가드를 뺀 채 돌리니 **저장소의 `prices.json` 이 실제로 빈
파일이 됐습니다**(화면 검사 44건 중 7건이 무너져서 알았습니다). `git checkout` 으로
되돌렸고, 검사는 이제 임시 폴더에만 씁니다. **검사가 진짜 자료를 건드릴 수 있으면
그 검사부터 고치세요** — 8번의 *"되돌려서 진짜 잡히는지 확인한다"* 가 바로 그
자리를 밟습니다.

**지금 당장은 아무것도 안 버려집니다.** 저장된 29종목이 전부 이번 분기 보유라
`prices.json` 은 한 바이트도 안 바뀝니다. **보이는 고침이 아니라 앞으로 쌓이는 것을
막는 장치**입니다.

**검사 세 건을 더했고, 두 가지를 되돌려 확인했습니다** — 팔린 종목을 도로 쌓게 하면
57건 중 1건 실패, 빈 목록 가드를 지우면 1건 실패. 고치면 57건 통과입니다.

**재 본 것**: 파이썬 57건 · 화면 44건 통과 · `prices.json` 바이트 변화 0 ·
저장된 29종목 그대로.

그다음 순서는 `docs/masters-13f.md` 11번에 있습니다.

### 9-3-1. 필수 오류 검수와 수정 (2026-09-18 · Codex)

현재 main(`3de46d8`)을 검수한 뒤 사용자가 **"반드시 수정해야 하는 것부터"**
진행하라고 했습니다. 화면·자료 구조는 유지하고 아래 여섯 오류를 고쳤습니다.

- **과거 기록 보존.** SEC 과거 목록 조각을 못 받으면 전체 목록을 실패로 돌리고
  기존 파일을 안 씁니다. 목록에서 접수번호가 빠져도 이미 저장한 분기·정정 표시를
  지우지 않습니다. 깨진 기존 JSON 위에 처음부터 다시 받지 않고, 파일 교체는
  임시 파일을 완전히 쓴 뒤에 합니다. 반쪽 목록 모의 시험: 111분기 그대로 보존.
- **신규·재진입 표시.** 직전 분기에 없던 종목의 `dn=null`이 `유지`로 읽히던 것을
  고쳤습니다. 목록에도 `신규`·`재진입`(영어 `New`·`Back in`)으로 적습니다.
  현재 D R 호턴은 사건 요약과 목록 모두 재진입입니다.
- **기간 경계.** N년은 4N개 점이 아니라 4N개 **간격**입니다. 경계 관측점까지
  4N+1개를 써서 `1년`은 2025.06.30~2026.06.30이 됩니다. 창의 첫 분기 거래는
  여전히 막대에서 빼고 이전 거래 카드에서 셉니다.
- **전량 매도 기록.** `lifeOf()`는 보유가 사라진 첫 공시에 가격 없는 매도를
  한 번 남깁니다(`p=null`, `exit=true`). 가격선·최저·최고에는 안 넣고, 매도
  막대·이전 거래 건수에는 넣습니다. 매도 표식은 종전처럼 마지막 보유 가격에
  둡니다. 옥시덴탈: 2020.06.30 공시에서 −18.93M주, 매도가를 만들어 넣지 않음.
- **분할 계산 일치.** 목록의 증감·계기판·최대 매수에도 차트와 같은 분할 보정을
  씁니다. 분할과 추가 매수가 함께 있으면 평균단가 계산의 직전 가격도 배수로
  나눕니다. 실제 다비타 추정 수익률은 +333%→+343%, 애플은 +631% 그대로입니다.
  2:1 분할만 있는 모의 자료는 목록·차트 모두 거래 0으로 검산했습니다.
- **실제 변경일만 갱신.** 분기·정정 표시 등 내용이 같으면 JSON 바이트와
  `updated`가 그대로입니다. 따라서 새 내용이 없는 실행은 커밋·사이트맵
  `lastmod` 갱신도 안 합니다. 정정 표시 추가는 실제 내용 변경으로 셉니다.

**자동 검증:** `python -m unittest discover -s scripts/tests -p 'test_*.py'`
(9건) · `node --test scripts/tests/titans.test.cjs`(8건). 외부 요청·실제 데이터
쓰기는 없습니다. Python 검사는 임시 폴더, 화면 검사는 실제 HTML의 함수와
2026.06.30까지의 자료를 씁니다. 새 분기가 자동으로 들어와도 기준 분기는 안 바뀝니다.

**실제 브라우저:** 한국어·영어 각각 320/360/390/900px, 26종목 전부 펼침:
가로 넘침 0 · 잘못된 SVG 좌표 0 · JS 오류 0. 영어 기간 네 개 전환 시 26차트가
함께 바뀜. 옥시덴탈 매도 막대 클릭: 날짜·전량매도·SELL 수량만 나오고 가격 없음.
SEC 실시간 수집과 러너 실행은 이 검증에 포함하지 않습니다.

**보류는 유지합니다.** 정정 원문 병합·연차보고서 대조·각주 수정·주별 시세·투자자
추가는 안 했습니다. 가격 눈금선·BUY/SELL 목록 스티커·3년·전체 기간·보유 연차
자는 되살리지 않았습니다. 실제 데이터 파일과 루트 대시보드는 안 바꿨습니다.

#### 개선 권장 후속 (2026-09-18 · Codex)

사용자가 개선 권장도 진행하라고 했고, **버크셔는 다른 투자자에게도 쓰는 샘플과
틀이며, 공시 검색→수집→화면 갱신의 자동화가 최종 목표**라고 다시 확인했습니다.
이번 변경도 이름·날짜·보유 종목을 손으로 고정하지 않습니다. **이 PR 당시에는**
수집기가 버크셔 CIK·파일에 연결돼 주 1회 검색했습니다. 이후 여러 투자자 감시·수집
모듈은 아래 별도 항목에서 만들었습니다. 정정 수치 병합은 2026-09-20 에 끝냈습니다
(같은 항목 아래 "정정 공시 수치 병합" 참고).

- **공시 기준 표현.** 한국어·영어 H1과 검색·공유 설명에서 현재 보유로 오해할
  `지금/holds now/right now`를 뺐습니다. 이름은 여전히 `window.TITAN`에서 옵니다.
- **정적 소개.** 머리글 아래 짧은 설명을 HTML에도 남깁니다. 분기말 기록·제출
  기한(45일 이내)·현금/공매도 미포함·평균가/수익률 추정·배당 제외를 설명합니다.
  실행 후 `intro(n)`가 언어와 투자자 이름을 채웁니다. 복제 시 head와 함께 정적
  H1·소개도 바꿉니다(`docs/design-system.md` 12-2). 보류한 기존 각주는 안 고쳤습니다.
- **모바일 안내와 크기.** 사라지는 열 머리글 대신 목록 위에서 숫자 순서를 한 번
  안내합니다. 매 줄 같은 라벨을 반복하지 않습니다. 계기판 이름표는 10.5px에서
  기존 `--t-micro`(11px)로 맞춥니다. 제목 크기와 목록 배치는 그대로입니다.
- **전량 매도 포함.** 최대 매도 비교에 `B.out`도 넣습니다(`netUSD=-직전 보유 금액`).
  남아 있는 종목은 증감 주식수×현재 분기말 가격, 전량 매도는 직전 분기말 금액으로
  평가하므로 계산 안내에 기준 차이와 추정을 밝힙니다. 실제 매도 대금은 아닙니다.
  현재 최대 매도는 여전히 BoA입니다. 전량 매도만 있는 모의 분기도 정상 표시합니다.
- **작은 비중.** 0보다 크고 0.1%보다 작은 값은 `<0.1%`로 적습니다. 실제 0과
  일반 비중의 한 자리 반올림은 유지합니다. 현재 메이시스·NVR·Jefferies·호턴이 해당.
- **대표 주소 일치.** 언어 전환/쿼리와 무관하게 투자자의 기본 URL을 canonical로
  둡니다. 정적 원본·실행 후·og:url·기존 사이트맵이 같은 기본 주소를 가리킵니다.
  언어 쿼리·hreflang·이동 안내는 유지하고, 언어별 별도 HTML/주소 이전은 안 했습니다.
  근거: [Google JavaScript SEO 공식 안내](https://developers.google.com/search/docs/crawling-indexing/javascript/javascript-seo-basics)
  — JS로 원본 HTML의 canonical을 다른 주소로 바꾸지 않으라는 지침.

**검증:** 기존 Python 9건과 화면 13건(총 22건) 통과. 새 분기 모의 자료에서
날짜·공시일·총액·종목·신규·전량매도·증감·차트 끝점이 따라오는 것을 검사합니다.
관련 PR에서는 `.github/workflows/check-titans.yml`이 같은 검사를 자동 실행합니다.
외부 서비스·알림·실제 데이터 쓰기는 없습니다.

브라우저: 한국어·영어 320/360/390/900px에서 26종목 전부 펼침, 페이지 가로
넘침 0·숫자 칸 겹침 0·계기판 라벨 11px·JS 오류 0. 다른 투자자 설정과 가짜 다음
공시(2026.09.30)를 별도 임시 페이지에서 확인: 제목·소개·출처·대표 주소의 이름과
날짜·사건이 바뀜. SEC 실시간 수집·정정 병합·연차보고서 대조는 이번 검증 범위 밖입니다.

#### 차트 디자인 후속 (2026-09-18 · Codex)

사용자가 예시를 승인하고 실제 화면 반영을 요청했습니다. 새 투자자도 같은 틀을
쓰며, 공시 갱신에 따라 숫자·막대·기간이 바뀌는 구조를 유지합니다.

- 매수·매도 막대 모두 X축에서 위로 그립니다. 높이는 선택한 기간의 최대 절대
  주식수 변화에 비례하며 최대 34.5px, 차트 폭 430px 미만에서는 27px입니다.
  사용자가 두 번 낮춘 최종 높이입니다. 주식 거래량이 아니라 13F 보유 수량의
  순변화입니다. 1년 창에서도 막대는 직전 공시부터 해당 공시까지 폭을 채웁니다.
- 색은 기존 언어별 `--up/--down` 그대로. 이전 매수·매도 건수의 숫자도 같은 색.
  이전 거래 카드는 제목·매수·매도 세 줄로 적습니다. 기간 왼쪽 경계의 변화는
  카드에 포함하고 창 안에 중복 막대를 그리지 않습니다.
- 차트 위 별도 제목·부제와 아래 중복 요약 줄을 두지 않습니다. 목록 줄·페이지
  제목과 공시 기준 소개는 유지합니다. 기간은 1/5/10/15년, 모든 종목이 함께 바뀝니다.
- 마우스·터치 위치에 X축까지 점선, 실제 관측점에 점, 그 근처에 날짜·가격을
  표시합니다. 현재 자료는 분기 관측값뿐이므로 말풍선에 **공시 기준 분기말 가격**
  (`Implied quarter-end price`)이라고 적습니다. 일별 종가로 부르지 않습니다.
- 막대 수량은 K/M/B 소수 둘째 자리, 중요 변화부터 제한된 수만 적습니다.
  날짜 눈금 수는 화면 폭에 맞춥니다. 좁은 목록의 숫자 세 칸은 축소·겹침 대신
  칸 사이에서 줄바꿈합니다. 정확한 수량은 기존 목록에서 확인합니다.

**일별 종가 연결은 미완성입니다.** 사용자는 지금 무료만, 나중에 유료 검토 가능이라고
답했습니다. 무료 다운로드와 공개 사이트 표시 권한을 혼동하지 마세요. 확인한 후보:
[Stooq 약관](https://stooq.com/terms.html) 5.3은 재배포에 동의를 요구하고,
[Alpha Vantage 약관](https://www.alphavantage.co/terms_of_service/)은 별도 합의 없이는
개인용 범위이며 무료 일별 API의 전체 이력도 제한됩니다.
[Twelve Data](https://twelvedata.com/pricing-business)의 무료 Basic은 내부 비표시용,
[Tiingo 약관](https://api.tiingo.com/tos/) 7.3도 외부 재배포에 별도 권한을 요구합니다.
공개 표시·15년 이력·무료 조건을 함께 충족하는 공급처는 아직 확인하지 못했습니다.
유료 계약·가입·외부 전송·실제 가격 수집은 하지 않았습니다.

독립 가격 자료를 받을 준비만 해 둡니다: 선택 설정 `window.TITAN.prices`는 기본
빈 문자열이며, **현재는 요청 자체가 없습니다**. 이후 허용된 자체 JSON URL을 넣으면
`{method:"split-adjusted-close",series:{회사키:{ticker:"AAPL",currency:"USD",
values:[["2026-06-29",289.36],…]}}}`를 읽습니다. 실제 날짜·양수 가격·USD만 받으며,
중복 날짜는 마지막 값, 날짜순 정렬입니다. 이 자료는 가격선에만 쓰고 평균가·수익률·
주식수 계산은 기존 13F를 유지합니다. 공시와 무관하게 창 전체를 그릴 수 있지만,
현재 실제 화면은 여전히 보유 시기의 분기 가격선입니다. **첫 매수 이전의 일별
주가선과 공시 이후 최신 종가는 아직 제공하지 않습니다.** 공급처 승인·출처/한계
표시·주식 종류별 가격 선택·수집 자동화·최신 가격 날짜 기준 기간 처리를 결정하고
검증한 뒤에만 설정을 켜세요. 모의 가격 자료의 통과가 실제 시세 검증은 아닙니다.

**검증:** Python 9건·Node 16건(총 25건) 통과. 전량 매도·분할·재진입·새 분기,
실제 애플 1년 막대의 분기 폭·상향 방향·최대 높이, 모의 독립 가격선과 가격 입력을
검사합니다. 브라우저에서 26종목×한국어/영어×1/5/10/15년×320/390/768/1280px
(832개 차트 상태) 날짜·수량 겹침/가로 넘침 0. 두 언어의 320px 가격 말풍선
각 26종목 잘림 0, 네 너비의 목록 숫자 겹침 0, 브라우저 JS 오류 0.

#### 일별 종가 저장·갱신·연결 (2026-09-19 · Codex)

사용자가 PR #54를 머지한 뒤 **무료 공급처 선택·15년치 저장과 자동 갱신·실제
주가선 연결까지 진행**하라고 했습니다. 버크셔에서 예외와 화면을 충분히 검증한
뒤 다른 투자자도 디자인 수정 없이 같은 틀을 쓰는 것이 목표입니다. 현재 무료만,
나중에 유료 검토 가능. **가격 공급처 이름을 화면에 추가하지 않는다는 사용자
선호**도 확인했습니다. 내부 구현·검증 문서의 실제 공급처는 숨기거나 바꾸지 않습니다.

**선택한 기술 경로는 Yahoo의 공개 chart 경로**입니다. 기존 대시보드 수집과 같은
서비스이며, 별도 계정·API 키·결제 없이 실제 데이터 수신을 확인했습니다. 이것이
공개 재배포 권한을 보장한다는 뜻은 아닙니다. 이전 후보 조사에서 말한 이용 조건은
없어지지 않습니다. 사실 가격의 저작권, 데이터베이스/계약 조건, 화면 출처 표시는
다른 문제이며 법률 판단은 소유자가 합니다(11번). 다운로드 성공을 허가로 보고하지
마세요. 비공식 경로여서 안정성 보장도 없습니다. 새로운 계약·가입·우회는 안 했습니다.

**캐시와 종목 종류:** `scripts/fetch_prices.py`가 registry에 등록된 투자자의 13F 책에서
최신 분기 보유 CUSIP를 모읍니다. 같은 CUSIP는 투자자가 달라도 한 번만 받습니다.
별도 `data/titans/prices.json`에 29개 주식 종류·101,963개 날짜/종가·약 2.2MB를
초기 저장했습니다(2026.09.17까지). 공시 JSON·섹터·로고용 티커 표는 안 고칩니다.
상장 전 값은 만들지 않습니다. ALLY/KHC/LLYVA/LLYVK처럼 짧은 이력은 실제 범위만.
15년 창 경계의 거래일 여유 7일을 더 받으며 이후 저장 이력도 이 범위로 유지합니다.

- OpenFIGI에서 CUSIP9+US 거래소로 **정확한 종류의 티커**를 받습니다. 로고용 이름
  대조는 주식 종류를 보장하지 않아 가격에 그대로 쓰지 않습니다. 구글 GOOG/GOOGL,
  레나 LEN/LEN-B, 리버티 LLYVA/LLYVK를 따로 저장합니다.
- CINS가 안 잡히는 해외 법인은 **공시의 유일한 일반 보통주 COM**이고 기존 티커의
  US Common Stock·정규화 회사명·단일 shareClassFIGI가 모두 맞을 때만 보조 조회를
  허용합니다. 실제 처브 H1467J104→CB를 이렇게 대조했습니다. A/B/C·우선주·ADR에
  이 보조 규칙을 적용하지 않습니다. 식별이 애매하면 새 가격을 만들지 않고 실패합니다.
- 목록은 기존대로 종류를 합치되 가격선은 그 줄의 **최신 분기 금액이 가장 큰
  대표 CUSIP**를 선택합니다. 티커를 가격 말풍선에 표시하므로 구글 합산 가격이나
  가상의 평균 가격으로 오해하지 않습니다. 13F 평균가·수익률·증감 계산은 유지합니다.

**가격과 저장 안전성:** Yahoo `quote.close`(분할 보정·배당 미반영)를 사용하며
`adjclose`를 가격선에 쓰지 않습니다. 종목/통화 일치, 날짜/가격 길이·양수 유한값을
확인합니다. 뉴욕 현지 20시 전에는 그날의 장중 바를 제외하고, 여름/겨울 시각을
구분합니다. 최신 종가가 7일 넘게 오래됐으면 실패합니다. Windows에서 음수 Unix
상장일(1960년대)을 `fromtimestamp`로 읽다 실패한 CVX/KO/KR는 epoch+timedelta로
고쳤습니다. 파일은 임시 파일+원자 교체, 내용이 같으면 다시 쓰지 않습니다.

**자동화:** `update-prices.yml`은 화~토 02:23 UTC(한국 오전 11:23)에 실행합니다.
미국 월~금 종가를 장 종료 후 받는 시각이며, GitHub 예약은 지연/누락될 수 있습니다.
평소 마지막 10일을 겹쳐 다시 받아 최근 정정을 덮고 과거를 보존합니다. 새 분할
이벤트/비율 변경이면 전체 이력을 다시 받으며, 토요일 UTC에는 티커·전체 가격을
재대조해 과거 정정도 반영합니다. 전체 재조회가 과거보다 크게 짧거나 저장된 최신
날짜를 지우면 거부합니다. 최초 전체 응답도 상장일 대비 시작일·일수 부족을 검사합니다.
티커 서비스 장애가 기존 티커 종가 갱신까지 막지 않게 했습니다. 성공한 종목은 저장,
실패 종목은 이전 값 보존, 작업은 빨간 실패로 남깁니다. 최초 PR #55에는 알림이
없었으나, 아래 후속으로 사용자 요청에 따라 텔레그램 실패 알림을 연결했습니다.
워크플로는 단일 실행 그룹이며 커밋 후 main을 rebase해 다른 자료 갱신과 충돌을
확인합니다. 공시 수집 주기·정정 처리에는 손대지 않았습니다.

**화면:** `window.TITAN.prices`에 공유 자체 JSON 경로를 연결합니다. 방문자는 외부
가격 API에 요청하지 않습니다. 모든 차트의 창은 최신 저장 종가 날짜 기준 정확한
1/5/10/15년이고 실제 날짜 간격으로 배치합니다(윤년 말일 보정). 보유 이전·미보유
기간에도 시장 가격선은 이어집니다. 상장 전이나 관측 범위 밖에서는 가격 말풍선을
만들지 않습니다. 최신 공시 이후에는 가격만 있고 추정 매매 막대는 없습니다.
막대는 실제 직전 공시~해당 공시 폭, 창 왼쪽에서 걸친 분기는 창 안 부분만 잘립니다.
그 수량은 분기 전체의 순변화입니다. 기존 상향 방향·최대 34.5/27px·방향 색·이전
거래 카드·금지한 제목/부제/아래 캡션·기간·가격 눈금선은 유지합니다. 말풍선 폭은
내용 기준으로 계산하되 화면 안에 제한해 포인터를 옮길 때 좁아지는 현상도 고쳤습니다.
출처와 한계에 가격 기준·종류 선택·공시 이후 매매 미추정만 추가하고 공급처 이름은
화면에 추가하지 않았습니다. 가격 파일이 없으면 기존 분기 가격으로 돌아갑니다.

**검증:** Python 20건·Node 18건. 실제 29개 종류 수집, 증분 재수집, 전체 재수집으로
보존을 확인합니다. 실제 AAPL/GOOG/GOOGL/CB의 2026.06.30 종가가 각 공시 금액÷
주식수와 같은 값을 보였습니다(289.36/353.33/357.37/340.74). 공급처 자체 정확성을
보증하는 검사는 아닙니다. 모의 수집은 장중 제외·겨울 시각·신규 분할 전체 재수집·
짧은 응답 거부·서비스 장애 보존·종류 구분·여러 투자자 캐시 공유를 검사합니다.
브라우저는 열린 26차트×두 언어×네 기간×320/360/390/900/1280px, 1,040개 상태에서
기간 버튼 상태·그림 표시·날짜/수량 겹침·가로 넘침을 검사했습니다. 모바일 두 언어
52개 종가 말풍선 잘림 0. 실제 GitHub 예약 실행은 머지 전이라 아직 확인 못 했습니다.
이 PR 당시 여러 투자자의 13F 자동 수집·정정 병합은 별도 미완성이었습니다.
이후 여러 투자자 감시·수집 모듈은 아래 항목에서 만들었고, 정정 병합은 아직 남았습니다.

**주가 오류 텔레그램 알림 후속 (2026-09-19 · Codex · 사용자 요청):**
`update-prices.yml`의 실패 실행은 기존 `notify.py`와 `TELEGRAM_TOKEN`/
`TELEGRAM_CHAT`으로 `[timing] 대가들의 선택 주가 갱신 오류`를 보냅니다.
종목 식별·종가 수집 실패(부분 실패 포함)와 가격 파일 커밋/rebase/push 실패를
구분해 적고, 마지막 수집 기록 20줄(CUSIP·티커·오류)과 실행 기록 링크를 넣습니다.
길면 기존 알림 길이 제한에 따라 잘리므로 전체 오류는 링크에서 확인합니다.
성공한 종목의 저장 단계가 끝난 뒤 보냅니다. 수집 로그의 `tee`에는 `pipefail`을
명시해 Python 실패가 성공으로 바뀌지 않게 합니다. 정상 실행·PR 검사에는 보내지
않습니다. 기존 13F 알림이나 텔레그램 설정은 바꾸지 않았습니다.
알림 서비스 장애/비밀값 누락 시 기존 `notify.py` 정책대로 로그를 남기고 건너뛰므로
수신을 보장할 수는 없습니다. 이번에는 토큰을 읽거나 실제 시험 메시지를 보내지
않았습니다. 오프라인으로 실제 Bash 본문을 실행해 수집 성공/실패 종료코드 보존,
수집 실패·저장 실패·둘 다 실패의 알림 내용, HTML escape·실행 링크를 확인했습니다.

#### 여러 투자자 13F 감시·수집 모듈 (2026-09-19 · Codex)

사용자가 버크셔를 완성된 샘플로 삼아 이후 모든 투자자에 같은 구조를 쓰고,
공시 검색부터 화면 자료 갱신까지 자동화하라고 했습니다. 각 부분을 따로 점검·수정할
수 있어야 한다는 요구도 명시했습니다.

**한 목록:** `data/titans/investors.json`이 자동 수집 대상의 단일 목록입니다.
slug·10자리 CIK·SEC 공시 법인명·한영 표시 이름·첫 연도·활성 여부를 검증합니다.
현재는 버크셔만 등록했습니다. 다음 투자자는 이 목록에 한 줄을 넣고 고유 HTML/SEO와
사이트맵을 추가합니다. 파이썬 상수나 GitHub Actions 단계를 투자자마다 복사하지 않습니다.

**네 부분:** `scripts/titans/sec.py`는 SEC 요청과 제출 목록 파싱, `registry.py`는
투자자 설정과 공시책 읽기, `watch_13f.py`는 새 접수번호 판단과 작업 분배,
`fetch_13f.py`는 선택된 투자자 한 명의 전체 목록·원문·검산·원자 저장만 맡습니다.
티커·업종·주가 수집기도 버크셔 파일 하나가 아니라 registry에 등록되어 실제 자료가
있는 모든 투자자 책을 읽습니다. 새 종목은 공시 JSON에 들어온 다음 이 공통 보조
작업과 공유 가격 캐시 대상으로 자동 편입됩니다.

**가벼운 감시:** `update-13f.yml`은 평일 12:15·23:15 UTC에 투자자별 최신
`submissions/CIK….json` 한 파일만 확인합니다. 새 `13F-HR` 또는 `13F-HR/A`
접수번호가 있을 때만 그 투자자의 무거운 전체 수집을 실행합니다. 일요일 01:00 UTC와
수동 실행은 모든 투자자를 전체 점검해 감시 누락과 과거 목록 변경을 보완합니다.
워크플로 중복 실행은 직렬화합니다. 보조 티커·업종 작업은 공시 자료가 실제로 바뀐
날에만 실행합니다.

**오류와 정정:** 한 투자자가 실패해도 다른 투자자의 성공 자료는 먼저 저장합니다.
감시·수집·커밋/rebase/push 실패는 마지막 로그와 함께 기존 텔레그램으로 알립니다.
티커·업종 보강 실패도 공시 실패로 오인하지 않게 별도 제목과 각 단계 결과로 알립니다.
정정 공시는 새 접수번호를 바로 감지하고 투자자 이름·SEC 원문 주소를 알립니다.
**수치 병합은 2026-09-20 에 끝냈습니다** — 바로 아래 항목을 보세요. 알림은 그대로
보냅니다: 값이 바뀐 사실을 사람이 알아야 하고, 합치지 못한 분기는 원본 숫자로
남기 때문입니다.

**검증 범위:** SEC·텔레그램 실서비스에는 연결하지 않고 모의 응답으로 registry 검증,
최신 목록 한 번만 요청, 기존 정규/정정 접수 무시, 새 접수만 수집, 주간 전체 확인,
수집 실패 전파, 기존 13F 보존·정정 알림·새 분기 추가를 검사합니다. 머지 후 첫 수동
실행에서 실제 SEC 응답과 Telegram 수신, 생성 커밋을 확인해야 합니다.

#### 정정 공시 수치 병합 (2026-09-20 · Claude)

**짐작하지 않았습니다. 원문을 받아 보니 공시가 스스로 종류를 적어 뒀습니다.**

`probe_amendments.py` + `probe-amendments.yml` 을 만들어 XML 시대 정정 8건의
원문을 받았습니다([run 35482954100](https://github.com/Snoring-Prince/timing/actions/runs/35482954100),
파일 60개·실패 0). 정찰은 **파서가 아닙니다** — 태그 이름을 전부 세고(자르면
표지의 드문 항목이 사라집니다) 잎 항목의 값을 글자 그대로 적을 뿐입니다.
`probe_sec.py` 와 같은 요령이고, 같은 이유로 따로 있습니다.

```
분기          원본줄   No   amendmentType   confDenied   정정줄
2013-06-30     127     1    NEW HOLDINGS      true          3
2014-09-30     146     1    NEW HOLDINGS      true          3
2015-06-30     147     1    NEW HOLDINGS      true          2
2020-09-30     145     1    NEW HOLDINGS      true          3
2023-09-30     152     1    RESTATEMENT       false       152
2023-09-30     152     2    NEW HOLDINGS      true          1
2023-12-31     138     1    NEW HOLDINGS      true          1
2025-03-31     110     1    NEW HOLDINGS      true          4
```

**신호 셋이 서로 일치합니다.** `amendmentType` 이 `RESTATEMENT`(갈아끼움) /
`NEW HOLDINGS`(더함)를 명시하고, `confDeniedExpired` 와
`reasonForNonConfidentiality`(비공개 기간 만료)가 NEW HOLDINGS 에만 붙습니다.
줄 수도 맞아떨어집니다 — 전체 재작성은 원본과 같은 152줄, 추가분은 1~4줄.
원본 7건 전부 `isConfidentialOmitted = true` 라 **원본이 스스로 "뭔가 빠져
있다"고 적고 있습니다.**

**차례는 `amended_by` 배열이 아니라 `amendmentNo` 입니다.** 이것이 이번에 찾은
진짜 함정입니다.

```
저장된 amended_by   ['0000950123-24-005653', '0000950123-23-011029']
실제                 24-005653 = No.2 NEW HOLDINGS (1줄)
                     23-011029 = No.1 RESTATEMENT (152줄)

배열 순서대로 적용   추가분(+1줄) → 전체 재작성(152줄 통째 교체)
                     = 뒤늦게 공개된 한 줄이 조용히 사라집니다
```

2023-09-30 이 두 모양이 겹치는 **유일한 분기**입니다. 이 한 건이 없었으면 모르고
지나갔을 자리라, 검사(`test_order_follows_amendment_number_not_the_stored_list`)
로 못박아 뒀습니다.

**모르는 값을 만나면 합치지 않고 그 분기를 그대로 둡니다.** `amendmentType` 이
위 둘 중 어느 것도 아니거나 원문을 못 받으면 **원본 숫자를 유지**합니다 —
반쯤 합친 분기를 남기는 것보다 안 합친 것이 낫습니다. RESTATEMENT 실물은
8건 중 **1건뿐**이라 그 경로는 예시 하나로 본 것입니다.

**이미 반영한 분기는 다시 받지 않습니다.** `amended_applied` 에 무엇을 언제
어떤 종류로 합쳤는지 적어 두고, `amended_by` 와 집합이 같으면 건너뜁니다.
매주 도는 작업이라 이것이 없으면 같은 원문을 영원히 다시 받습니다.

**2013년 이전 43분기에는 `amend_gap: "pre-xml"` 을 적고 다시 받지 않습니다.**
텍스트 공시라 자동으로 못 합칩니다 — 아래 일회성 변환 원칙이 걸리는 자리입니다.
**표시를 데이터에 남기는 이유**는 두 가지입니다: 매주 43건을 헛되이 받지 않게
하는 것과, 그 분기가 정정을 반영하지 못했다는 **사실 자체를 자료가 말하게**
하는 것입니다(화면이 나중에 밝힐 수 있습니다).

**금액 단위를 먼저 맞춘 뒤 합칩니다.** 옛 분기는 천 달러, 요즘은 달러입니다.
그대로 더하면 한쪽이 1000배 틀립니다. 각 문서의 줄을 제 단위로 달러화한 뒤
묶습니다(`one_doc` → `fold(rows, 1)`).

**예행으로 잰 결과**(실측 줄 수·종류를 그대로 넣고 `apply_amendments` 만 실행):
7분기 반영 · 43분기 공백 표시 · 2023-09-30 은 재작성(1번) 다음 추가분(2번)
차례로 153줄. 실제 SEC 실행은 머지 뒤 첫 주 1회 작업에서 확인해야 합니다.

**검사**: `scripts/tests/test_amendments.py` 12건(갈아끼움·더함·차례·모르는
종류 거부·단위 맞춤·두 번째 실행에서 안 받음·나중 정정에 다시 셈·2013년 이전
표시 한 번만·거부 시 원본 보존). 기존 Python 33건·화면 19건 그대로 통과합니다.
`test_fetch_13f.py` 의 `run_fetch` 에는 원문 받기 대역을 기본값으로 넣었습니다 —
그 파일은 **SEC 요청 없이 도는 것이 약속**인데 병합이 생기면서 받는 자리가
하나 늘었기 때문입니다. 검사를 무르게 한 것이 아니라 그 약속을 지킨 것입니다.

**2013년 이전 텍스트 공시는 반복 자동화 대상이 아닙니다 (2026-09-19 사용자 결정).**
새 투자자에게 그 시기 이력이 필요하면 동결된 원문을 받아 투자자별로 한 번만 변환하고,
총계·분기·행 수를 검산한 JSON을 커밋합니다. 공통 유지 파서나 예약 작업을 만들지
마세요. 사용한 형식 예외와 검산값은 `docs/masters-13f.md`와 작업 일지에 남겨 다음
어시스턴트가 같은 조사를 반복하지 않게 합니다.

### 9-3-2. 사무엘의 결정 (가제) — 적립식 곡선

**아직 러프한 아이디어입니다.** 사용자 말 그대로 "NASDAQ이나 S&P500을 분할 매수
했을 때 수익률 그래프가 어떻게 올라가는지". 언제든 바뀔 수 있습니다.

**이건 새 데이터가 한 톨도 필요 없습니다.** 이미 있는 `market-long.json` 의
SPY·QQQ 만으로 계산됩니다. 2026-09-14 에 실제로 돌려 봤습니다 — 매달 100씩,
그 달 첫 관측치로 사는 계산입니다.

```
SPY  1993-01 ~ 2026-09 (405개월)   넣은 돈 40,500
     적립식  →   362,616   +795%
     일시금  → 1,283,855  +3070%      일시금이 3.54배

QQQ  1999-03 ~ 2026-09 (331개월)   넣은 돈 33,100
     적립식  →   399,732  +1108%
     일시금  →   560,989  +1595%      일시금이 1.40배
```

**여기에 함정이 하나 있습니다.** "분할 매수하면 수익률이 올라간다"는 틀로 만들면
숫자가 반대로 나옵니다 — 오르는 시장에서는 **일시금이 거의 항상 이깁니다.** 적립식의
값어치는 수익률이 아니라 **결과의 폭이 좁아지는 것**과, 실제로 사람들이 그렇게
(월급에서) 산다는 데 있습니다. 6번의 "영어가 사라는 쪽으로 기운다"와 같은 종류의
함정이니, 문구를 쓸 때 **"더 낫다"가 아니라 "계속 넣으면 이렇게 된다"** 로 가세요.

**계산의 한계**: `market-long.json` 은 2021-09-14 이전이 주봉이라(3번) "그 달 첫
거래일"이 "그 달 첫 주"로 근사됩니다. 화면에 낼 숫자는 `backtest.py` 처럼 일봉을
따로 받아서 다시 계산해야 합니다. 위 숫자는 **된다는 것을 확인한 값**이지 발표용이 아닙니다.

### 9-4. 그 밖

1. ~~영어 검수~~ — **끝났습니다 (2026-09-13).** ⓘ 툴팁 8개 + 설명 글 7개 절, 6번 참고.
2. ~~320px 가로 넘침~~ — **고쳤습니다 (2026-09-13).** 아래 참고.
3. ~~두 잣대가 엇갈릴 때를 화면에 드러내기~~ — **안 하기로 했습니다 (2026-09-14).**
   사용자가 "패스"라고 했습니다. 다시 제안하지 마세요.

   현상 자체는 사실입니다. 공포탐욕 42(공포)인데 VIX 14.5(잠잠) 같은 날이 실제로
   있고, 버그가 아니라 **VIX 가 공포탐욕을 만드는 7개 재료 중 하나**이기 때문입니다.
   2026-09-14 에 하루 단위 구간(2021-09-14 이후 1,253일)만 다시 세어 보니,
   공포탐욕 45 미만이던 532일 중 **32일(6.0%)** 이 VIX 15 미만이었습니다.
   문서에 적혀 있던 `1,607일 중 249일(15.5%)` 은 2011년부터 전체를 잰 값인데,
   **개발 환경에서 야후가 막혀 다시 확인하지 못했습니다.**

   **여기 적혀 있던 "합친 차트에서 두 선을 같이 보여 주게 됐다"는 틀렸습니다.**
   `mainOn()` 이 심리선을 `MSHOW.gauge && btAx===k` 로 고르기 때문에 **공포탐욕과
   VIX 는 한 번에 하나만 켜집니다.** 즉 지금 화면에서는 엇갈림이 아예 안 보이고,
   만들려면 오른쪽 축이 0~100 과 10~45 를 동시에 써야 해서 "선은 최대 2개"라는
   4번의 설계 결정을 뒤집어야 합니다. 쉬워진 게 아니라 오히려 비쌉니다.
4. **SEO — 머리글·본문·제목 태그·검색어 방향(6-2) 다 끝났습니다.** 남은 것은 기다리는 일입니다.
   구글 Search Console 에 사이트맵을 냈고 색인 생성을 요청했습니다. 재판정까지
   며칠~몇 주 걸립니다. **`URL 검사` 에서 "크롤링됨 - 현재 색인이 생성되지 않음" 이
   계속 나오면** 본문을 더 늘려야 한다는 뜻입니다(4번에 아직 안 쓴 근거가 남아 있습니다).
   참고: 이 도메인은 2026-04-05 에 구글이 크롤링한 기록이 있습니다 —
   **전에 누가 쓰다 놓은 도메인**입니다. `보안 및 직접 조치` 를 한 번 확인하세요.
5. 다른 지수 추가 — `fetch_data.py` 의 `INDEX_SOURCES`, `fetch_long.py` 의 `QUOTES` 에 항목 추가.
6. **후원 한 줄 — 켰습니다 (2026-09-14).**
   `index.html` 위쪽 `window.SUPPORT.url` 에
   `https://buymeacoffee.com/snoringprince` 가 들어 있고, 각주 위에 조용한 한 줄이
   나옵니다. **주소를 비우면 줄 자체가 안 생깁니다** — `hidden` 이라 빈 여백도
   안 남습니다(실측 높이 0px). 끄고 싶으면 그 한 줄만 비우면 됩니다.
   GitHub Pages 약관이 기부·크라우드펀딩 링크를 **명시적으로 허용**합니다.

   **기대치를 낮게 잡으세요.** 개인 도구 사이트의 후원 전환율은 방문자 1,000명당
   1명 아래가 보통이고, 이 사이트는 아직 색인도 안 됐습니다. **지금은 후원이 0건이어도
   "안 좋아해서"인지 "본 사람이 없어서"인지 구분이 안 됩니다.** 그래서 `support`
   이벤트로 **누른 횟수**를 따로 셉니다 — 그 숫자가 있어야 둘을 나눌 수 있습니다.

   **색 버튼을 놓지 마세요.** 이 화면의 디자인 언어는 계측기·측량 도면이라 혼자 튑니다.
   각주와 같은 고정폭·같은 11px 로 두고 링크에만 밑줄을 남겼습니다.

### 도메인 — `itpaidoff.com` (2026-09-13 구입)

Namecheap. 첫해 $11.28, **갱신은 $14.98**. 자동 갱신 켜 둠 —
만료되면 검색 이력이 통째로 날아가므로 끄지 마세요.
WHOIS 비공개(Domain Privacy) 켜 둠 — 끄면 이름·집주소·전화번호가 공개됩니다.

이름을 이렇게 고른 이유: 메인이 "대가들의 선택"(9-3)이 될 수 있으므로
**이 대시보드 이름에 묶이지 않아야** 했고, "그 선택이 결실을 맺었다"는 뜻이라
결과 추적이라는 차별점이 이름에 그대로 들어갑니다. 탈락 후보는
`whattheykept.com`, `thelongview.com`(차별점이 안 드러남).

**살아 있는 투자자의 이름을 도메인에 넣지 마세요.** 퍼블리시티권·상표 문제가 생깁니다.
13F 데이터를 쓰는 것과 그 사람 이름을 간판에 거는 것은 완전히 다른 이야기입니다.

### DNS (Namecheap → Advanced DNS)

```
A      @     185.199.108.153
A      @     185.199.109.153
A      @     185.199.110.153
A      @     185.199.111.153
CNAME  www   snoring-prince.github.io.
```

**이 네 주소는 추측이 아니라 실측입니다** — `snoring-prince.github.io` 가 실제로
물려 있는 IPv4 를 조회해서 확인했습니다. 개발 환경에서 GitHub 문서 사이트는 막혀 있으니
(`docs.github.com` egress 차단) 다시 확인할 일이 있으면 같은 방법을 쓰세요.

```
getent ahostsv4 snoring-prince.github.io
```

---

## 10. 알려진 제약

- **공포탐욕지수는 2012년 봄에 나왔습니다.** 소급 가능한 값은 2011년까지가 가장 이르고
  그 이전은 어디에도 없습니다. 2008년은 방법이 없습니다. CNN 자체 API 는 최근 1년치만 줍니다.
- **전고점은 "화면에 보이는 구간 안의" 최고 종가**입니다. 사상 최고점이 아닙니다.
  옆으로 끌면 그 구간의 전고점으로 바뀝니다(각주 명시).
- **최근 5년 밖은 주봉입니다.** 10년·20년 구간에서 하루짜리 급락은 그 주의 종가에 묻힙니다.
- **장중 시세는 저장소에 두지 않습니다.** 방문자의 브라우저가 중계소에서 직접 받습니다(9-2).

- **60일 무활동 시 GitHub 이 예약 워크플로를 끕니다.** 봇이 매일 커밋하므로 현재는
  해당 없지만, 갱신이 멈추면 Actions 탭에서 재활성화가 필요합니다.
- **어시스턴트가 태그를 push 할 수 없습니다.** 조직 정책에서 막습니다(403).
  백업은 태그가 아니라 **브랜치**로 만드세요 — `backup/v1-3panel` 이 그 예입니다.

### GitHub 예약 실행은 "10분마다"를 지키지 않습니다 (2026-09-10 실측)

되살리자는 이야기가 나오면 **먼저 이 숫자를 보세요.** 예전 문서에는 "지연 30~60분,
원인은 GitHub 으로 추정"이라고만 적혀 있었는데, 지운 워크플로의 실행 기록
**140건(8/20~9/9, 평일 15일)** 을 실제로 세어 확인했습니다.

```
설정          평일 13~21시 10분마다  =  하루 54회
실제 실행     하루 1~41회, 최근 사흘은 3~4회

연속 실행 사이 간격
  중앙값 16분 · 평균 32분 · 최대 199분
  10분 이내로 지켜진 비율 …… 0%
```

원인은 스크립트도 야후도 아니고 **GitHub 이 무료 예약 실행을 미루고 건너뛰는 것**입니다.
우리가 고칠 수 있는 게 아닙니다. 장중 시세가 정말 필요하면 예약 실행이 아니라
**Cloudflare Worker 중계소**(무료, 방문자가 열 때 그 자리에서 받아옴)로 가야 합니다.

**"공포탐욕은 어차피 하루 한 번"이라고 적었던 것은 틀렸습니다 (2026-09-13 정정).**
GitHub 미러는 분명 하루 한 번(23:59 UTC)이지만, **CNN 본진은 클라우드플레어의 요청에
답합니다.** 러너만 막혔던 것입니다. 그래서 중계소로는 넷을 다 받습니다(9-2).
**막힌 곳이 하나 있다고 다른 경로도 막혔다고 넘겨짚지 마세요.**

---

## 11. 상업화 — 순서가 정해져 있습니다

기술보다 이게 먼저입니다. 2026-09-07~08 에 조사한 결과입니다.

### 냉정한 산수

정식 상업용 데이터 라이선스는 **EODHD 기준 월 $399부터**입니다. 개인용 플랜은
Polygon(현 Massive) 무료~$199 가 **전부 "Individual use only"** 입니다.
반면 개인 대시보드 애드센스 수익은 트래픽이 없으면 월 몇천 원입니다.

**즉 라이선스가 막아서 못 버는 게 아니라, 이 규모에서 광고 모델이 수지가 안 맞습니다.**
그래서 **광고는 첫 카드가 아니라 마지막 카드**입니다.

### 권장 순서

1. **후원 버튼** — 비용 0. GitHub Pages 약관이 기부 링크를 명시적으로 허용합니다.
   "사람들이 돈을 낼 만큼 좋아하는가"를 실측할 수 있습니다.
2. **ETF 전환** — **완료**. `market-long.json`(2026-09-09)과 `market.json`(2026-09-12)
   둘 다 SPY·QQQ 배당 반영가입니다. 지수 사업자 권리가 통째로 빠졌습니다.
3. **제휴(증권사 계좌 개설 등)** — 데이터를 파는 게 아니라 트래픽을 보내는 것이라
   층위가 다르고, 단가가 광고보다 훨씬 높습니다.
4. **광고 + 정식 라이선스** — 마지막.

**"대가들의 선택"(9-3)은 이 표에서 예외입니다** — 13F 는 퍼블릭 도메인이라
아래의 데이터 라이선스 문제가 걸리지 않습니다.

### 무료 서비스들이 동시에 걸립니다

| | 지금 | 광고 붙인 뒤 |
|---|---|---|
| GitHub Pages | 무료 | **비상업 전용** → 이전 필요 |
| Vercel Hobby | 무료 | **비상업 전용** → Pro $20/월 가능성 |
| Yahoo Finance | 무료 | 상업 재배포 금지 → 라이선스 |
| Neon / Umami 셀프호스팅 | 무료 | 그대로 무료 |

### 법률

- **지수 값 자체에는 저작권이 없습니다** (미국 판례). 그래서 문제는 저작권이 아니라
  **계약(약관)** 입니다. 계약이 문제라면 **상업 이용을 허용하는 공급처로 갈아타면 됩니다.**
- **인베스팅닷컴은 공개 API 가 없습니다.** 본인들이 "데이터 공급사와의 계약상 제공할 수
  없다"고 밝혔습니다. 거기서 긁으면 상황이 나빠집니다.
- **한국 유사투자자문업 신고**: 자본시장법 시행령 제102조는 "일정한 대가를 받고" 하는
  투자조언이 대상입니다. **광고 수익만 있으면 일반적으로 신고 대상이 아닙니다.**
  유료 구독·멤버십으로 넘어가면 달라집니다 — 그때는 변호사 확인이 필요합니다.
  **어시스턴트는 법률 자문을 하지 마세요.** 사실관계만 알리고 판단은 사용자에게 맡깁니다.
- 실제 리스크는 소송이 아니라 **데이터가 조용히 끊기는 것**입니다. 이 프로젝트에서
  이미 두 번 겪었습니다(Stooq 차단, CNN 418).

---

## 12. 참고 URL

```
사이트      https://itpaidoff.com
저장소      https://github.com/Snoring-Prince/timing
장기 이력    https://raw.githubusercontent.com/Snoring-Prince/timing/main/data/market-long.json
현재값      https://raw.githubusercontent.com/Snoring-Prince/timing/main/data/market.json
백테스트     https://raw.githubusercontent.com/Snoring-Prince/timing/main/data/backtest.json
옛 화면 백업  https://github.com/Snoring-Prince/timing/tree/backup/v1-3panel

F&G 미러(현재값)  https://raw.githubusercontent.com/whit3rabbit/fear-greed-data/main/json/cnn_output.json
F&G CSV (2021~)  https://raw.githubusercontent.com/whit3rabbit/fear-greed-data/main/datasets/cnn_fear_greed.csv
F&G CSV (2011~)  https://raw.githubusercontent.com/hackingthemarkets/sentiment-fear-and-greed/master/datasets/fear-greed.csv
VIX              https://query1.finance.yahoo.com/v8/finance/chart/%5EVIX?period1=631152000&period2=<now>&interval=1d

SEC 13F 검색      https://www.sec.gov/cgi-bin/browse-edgar?action=getcompany&type=13F
GitHub Pages 약관  https://docs.github.com/en/site-policy/github-terms/github-terms-for-additional-products-and-features
Vercel Hobby 약관  https://vercel.com/docs/plans/hobby
AdSense 자격 요건  https://support.google.com/adsense/answer/9724
Umami 대시보드     https://cloud.umami.is
```

사용자 로컬 작업 폴더: `C:\dev\timing`
